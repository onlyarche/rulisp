[package]
name = "rx"
version = "0.1.0"
edition = "2021"
description = "rulisp quickstart example: Rust's linear-time regex engine for Common Lisp"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
rulisp = { path = "../../crates/rulisp" }
regex = "1"
