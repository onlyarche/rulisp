//! Empty shell until M3. The target output of these macros is pinned by the
//! hand-written ABI oracle in `tests/m1-handwritten/` and the golden manifest
//! snapshot in `tests/golden/` (byte-identity gate, DESIGN.md §8 M3).
