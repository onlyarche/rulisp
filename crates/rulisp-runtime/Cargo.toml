[package]
name = "rulisp-runtime"
version = "0.3.0"
edition = "2021"
description = "Support runtime linked into every rulisp cdylib: status codes, last-error TLS, universal deallocator, shim helpers"
license = "MIT"
readme = "../../README.md"
repository = "https://github.com/onlyarche/rulisp"
keywords = ["lisp", "common-lisp", "ffi"]
categories = ["development-tools::ffi"]

[dependencies]
