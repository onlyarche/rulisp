[package]
name = "rulisp-runtime"
version = "0.1.0"
edition = "2021"
description = "Support runtime linked into every rulisp cdylib: status codes, last-error TLS, universal deallocator, shim helpers"
license = "MIT"

[dependencies]
