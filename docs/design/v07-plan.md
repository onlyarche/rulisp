# rulisp v0.7 — "the freeze rehearsal, and the flagship the ask named"

**Theme:** 0.7 is the last minor before 1.0 and the first cycle with a flagship on demand — the maintainer's own ask, under the new house rule. No surface moves: ABI 1 and `:schema` 1 untouched, the Lisp API golden (tests/golden/lisp-api.sexp, 47 rows) unchanged because 0.7 adds no export by design, `rulisp::runtime` public until the 1.0 major's first commit (stability.md:18-23), the semver gate as `--release-type minor` against 0.6.0 (ci.yml:35-43); every wire change would be an additive manifest key under BOUNDARY §11 — none is planned; nothing adds an ECL load-time toolchain requirement; every test stays hermetic and every example artifact passes tools/rulisp-audit.sh. What the cycle does: the two gates that go red on *allowed* changes are fixed before 1.0 freezes them (`make compat` vs one additive export — reproduced; the literal `"0.4.0"` in the manifest golden's two substitutions), the one reproduced loader defect lands (a corrupt artifact reaches `dlopen`: a 60 % cut *loads and runs* today, a 4 KiB head faults inside glibc and wedges every later load from another thread), the dist stops indexing a test program before Quicklisp would index it for good, the two promotions the written procedures have earned land by those procedures (the macOS fetch step now; arm at ECL's count with the deployment path rehearsed from a branch first), examples/wasm — the project's first reason, `SECURITY.md:42-44`'s "supported approach" for untrusted logic, three release blobs per host and zero tests — gets the suite it never had and grows into a WASI plugin sandbox with fuel, a memory cap, captured stdio and a capability-scoped filesystem, `make clean` cleans, and the 1.0 delta is measured and written down with the release-gate gap it exposes closed now. Budget 1 L + 1 M + 7 S; cut order at the end.

Verified today against the tree (read-only): HEAD is `2ee1614` = `v0.6.0-1`, working tree clean, CHANGELOG.md:8 reads `## Unreleased (0.7 development)`; `git ls-files '*.asd'` prints two files (lisp/rulisp.asd, tests/ecl-program/rulisp-ecl-smoke.asd) and the Ultralisp systems index carries three `onlyarche-rulisp` rows including `rulisp-ecl-smoke` (`sh tools/check-dist.sh` → 0.6.0, archive 20260920203140). `grep -n 'continue-on-error' .github/workflows/ci.yml` hits four lines — :173 (the macOS fetch step) and :248 (the arm job) are live, :216 and :244 are procedure comments — so every "count drops to 0/1" acceptance in the proposals was off; this plan anchors the pattern. Makefile: `PREV ?= v0.6.0` at :68, `compat` :70-82 (:79 copies `lisp/src/*.lisp` into the archived tree whose lisp/rulisp.asd names its files by hand; the old suite runs at :80-82), `dist-dryrun` :90-95 asserting `-eq 3` at :95, `clean` :119-121 (`cargo clean` plus tests/m1-handwritten's two targets — nothing else .gitignore lists). tools/required-ci-green.sh:20 `MIN_REQUIRED=6` and the awk at :34/:39 match only names ending `(required)` or starting `MSRV ` — the `cargo tests (golden manifest + trybuild)` job (ci.yml:19), which holds the semver gate (:35-43), `make audit` (:32) and the manifest golden, cannot refuse a tag. `:rulisp-version "0.4.0"` sits at tests/golden/wordbag.manifest.sexp:6, tests/suite/m2.lisp:141 and examples/wordbag/tests/manifest_golden.rs:14, and docs/releasing.md:16-22 tells the releaser to hand-edit the last two. `grep -rni wasm tests/ lisp/rulisp.asd` is empty while docs/claims.md:58-64 (six rows) cite examples/wasm/src/lib.rs line ranges and SECURITY.md:42-44 names examples/wasm "the supported approach"; examples/fetch/audit.sh:10-12 globs `.so`/`.dylib` only, so `make test-fetch` cannot run on Windows although Makefile:54 already audits `fetch.dll` there. The arm job (ci.yml:246-248) has 20 consecutive green runs since the gc-finalization fix e32e114 (2026-09-07) — 18 counted from ROADMAP.md:36-37's own restart date 2026-09-08 — and the macOS fetch step 37 consecutive, never itself failed. Scratch results this panel (scratchpad/panel7/, tree untouched): `compat-probe/probe-result.log` — one documented additive export makes `make compat PREV=v0.6.0` exit 2 (`V06.EXPORTED-API-GOLDEN … exported symbols not in the golden: (RULISP:COMPAT-PROBE)`, 469/470); `rehearsal-1.0/semver.log` — `#[doc(hidden)]` on the `rulisp::runtime` re-export alone is invisible to cargo-semver-checks 0.50.0 (`196 pass, 58 skip` even as a minor); `semver-runtime.log` — hiding rulisp-runtime's own 26 items fires exactly five `*_now_doc_hidden` lints (`191 pass, 5 fail … semver requires new major version`) and `--release-type major` runs `0 checks: 0 pass, 254 skip`; `eval/corrupt.lisp` on SBCL 2.1.11 — libwordbag.so (6,002,984 B) cut at 60 %, 95 % and −1 KiB **loads and answers `greet`** (the last PT_LOAD ends at byte 608,432; the section table starts at 6,000,424) while a 4,096-byte head faults inside `dlopen` (`CORRUPTION WARNING … Signal 7 … bus error`, caught as SIMPLE-ERROR, not a rulisp condition) and `eval/wedge.lisp` shows a `reload-crate` from another thread never returning afterwards; `eval/eclsmoke/result.log` and `rehearsal-1.0/ecl-run.log` — the smoke defsystem supplied at build time with no committed .asd prints `BUILD-DONE`, `Hello, program-op!`, `ECL-PROGRAM-OK` on ECL 21.2.1; `wasi-probe/` (wasmi 0.50 + wasmi_wasi 0.50 + cap-std 3, one cdylib) — 4/4 tests: a 56-line hand-written .wat WASI command exits 7 with stdout exactly `hello from wasi\n` + `guest\0alpha\0beta\0` + `inside\n`, the `../outside.txt` escape refused (one errno byte on stderr, content never seen), fuel 1,000,000 → 998,708, `(memory 100)` refused under 1 MiB, `memory.grow` past the limit answers −1, a spinning guest traps "all fuel consumed"; 117 lockfile packages, no C compiled, cold build 43 s (scratch/build-times.log), `tools/rulisp-audit.sh` → `audit ok` with **no** signal-ish import at all (scratch/pass2.log); `rmcp-duplex-probe/` — an rmcp 3.4 server and `()` client over `tokio::io::duplex` round-trip (`tools=["echo"]`, `echo: hi`), audit ok, tokio without `signal`/`process`; `scratch/std-command` — a cdylib whose only code is `std::process::Command::new("lscpu").output()` imports `signal@GLIBC_2.2.5` and FAILS the audit, which is why `candle-core` fails it (gemm-common 0.19.0 cache.rs:232); `arm-elf/libarm.so` — x86-64 `nm` reads an aarch64 ELF and the audit answers `audit ok (elf)`, so the Linux release job can re-audit an arm asset.

## Items, in order

### 1. `make compat` tells additive from breaking: the previous release's suite accepts a new export and a new source file, and still refuses a removed or changed one (S)

**Demand.** stability.md:40-46 says the old suite runs against this tree's loader and "a test that reaches `rulisp::` internals is recorded and skipped by name" — Makefile:62-67 repeats it and :70-82 has no skip mechanism; stability.md §1 says an additive export "regenerates the golden in the same commit". The two contradict, reproduced (scratchpad/panel7/compat-probe/probe-result.log): add a documented `rulisp:compat-probe`, regenerate the golden, `make compat PREV=v0.6.0` → old-loader leg OK, old suite `Fail: 1`, `v06.exported-api-golden` (tests/suite/v06.lisp:232-241 checks both directions) names the new export, exit 2. Second hole: Makefile:79 copies `lisp/src/*.lisp` into the archived tree whose lisp/rulisp.asd lists nine files by name and tests/run-m4.lisp loads through ASDF — a tenth loader file never loads there, silently. Every 1.x minor will add exports; the gate must permit what §1 permits before `PREV` becomes v1.0.0, and before any 0.7 item adds a loader file.

**Scope.** Makefile `compat` (:70-82): (a) assemble `$(COMPAT)/tree/lisp/rulisp.asd` from this tree's `rulisp` defsystem (`awk '/^\(asdf:defsystem #:rulisp\/test/{exit}{print}' lisp/rulisp.asd`) plus the previous release's `rulisp/test` defsystem (`awk '/^\(asdf:defsystem #:rulisp\/test/,0'` on the archived copy), with a grep guard that both forms were found (a reordered .asd fails loudly — wanted); keep the `lisp/src` copy. (b) Save the archived golden as `$(COMPAT)/prev-lisp-api.sexp`, copy this tree's tests/golden/lisp-api.sexp over the archived one, and run new `tests/compat/api-subset.lisp` (~20 lines: `read` both files with `*package*` = RULISP, every previous row must be `equal` to a row of the tree's — name, kind, superclasses, lambda list, exact, SBCL being where compat runs — print `API-SUBSET-OK` or the first offending row and exit 1) before the old suite. The cross-release meaning moves entirely to api-subset (the copied golden makes the old golden test redundant with the tree's own) — the Makefile comment (:62-67) and stability.md:40-46 say so: an export the previous release had must be present with the same kind, superclasses and lambda list; additions pass; a lambda-list reprint counts as a change, by intent. tools/check-1.0.sh:19's `make compat PREV=v[0-9]` needle keeps matching. CHANGELOG Unreleased → Changed. Surface: none.

**Acceptance.** In a scratch clone: add a documented `#:compat-probe` export → `make compat PREV=v0.6.0` exits 0 (exit 2 today, reproduced); remove `#:retry-build` from lisp/src/package.lisp and regenerate the golden → exit 1 with api-subset naming `RETRY-BUILD`; add `lisp/src/probe.lisp` to the `rulisp` defsystem with a function crate.lisp calls → the old-suite leg loads it (today the archived .asd never does).

### 2. A truncated or corrupt artifact is refused before `dlopen`, on the load and the restore path (S)

**Demand.** ROADMAP.md:141-152 recorded it at restore ("Found during item 1, not scheduled": SIGBUS inside glibc's `dlopen`, ld.so's load lock left held, CCL hangs at startup) and the maintainer's list names it. Reproduced today on the plain load path — docs/usage.md Case A's partial download (scratchpad/panel7/eval/corrupt.lisp, wedge.lisp; SBCL 2.1.11): a 4,096-byte head faults inside `dlopen` (`CORRUPTION WARNING in SBCL … Signal 7 … Continuing with fingers crossed`; `handler-case` sees SIMPLE-ERROR "bus error", not a rulisp condition) and a `reload-crate` from another thread afterwards never returns (15 s; the lock is recursive only for the faulting thread); worse for a 1.0 candidate, cuts at 60 %, 95 % and −1 KiB **load and run** because every PT_LOAD ends by byte 608,432 of 6,002,984 and only `.debug_*` and the section table are missing — the same cut inside a larger artifact's `.text` faults at first call. No size or header check exists in lisp/src (`grep -n 'file-length\|magic\|ELF' lisp/src/*.lisp` → nothing); `%restore-all-crates`' `handler-case serious-condition` (crate.lisp:475-482) cannot catch a SIGBUS raised inside `dlopen`.

**Scope.** lisp/src/crate.lisp: `%check-artifact-shape`, called in `%load-crate-locked` (:150-192) **before** `uiop:copy-file` (:162) — the check reads a few KB, the copy moves 6 MB. Magic dispatch as tools/rulisp-audit.sh:59-64: ELF (`7f 45 4c 46`, class 2, little-endian) — `e_shoff + e_shnum × e_shentsize ≤ size` when `e_shnum > 0`, and every PT_LOAD's `p_offset + p_filesz ≤ size`; Mach-O (`cf fa ed fe` / `fe ed fa cf`) — every `LC_SEGMENT_64`'s `fileoff + filesize ≤ size`, a FAT magic refused by name; PE (`4d 5a` → `e_lfanew` → `PE\0\0`) — every section's `PointerToRawData + SizeOfRawData ≤ size`; anything else passes through to `dlopen*` (ffi.lisp:82) as today. Failure signals the EXISTING `crate-not-loaded-error` with `:message "artifact is truncated or corrupt: <detail> (<size> bytes)"` — no new condition, no new export, the golden untouched, item 1's gate green. ~15 lines per format; falsified against all twelve v0.6.0 release assets (every format) before landing. `%restore-all-crates` (:464-483) goes through the same function, so the restore case is covered by construction. New `tests/suite/v07.lisp` (`def-suite* :rulisp-v07`, registered in lisp/rulisp.asd after `v06` (:31) and as r10 in tests/run-m4.lisp:23-34; tools/check-1.0.sh:23-24 gains the `:rulisp-v07` needle beside `:rulisp-v06`): `v07.truncated-artifact-is-refused` — copy the wordbag artifact to a per-process temp name cut at 60 %, `load-crate … :crate "wordbag"`, assert `crate-not-loaded-error` whose message contains "truncated", nothing in `rulisp::*crates*` for it, no cache copy left, and the real artifact still loads afterwards in the same image; safe in-process on all seven hosts (no fault involved); `v07.header-only-artifact-is-refused-without-a-fault` — `#+(or sbcl ccl)`, the v06.lisp:19-44 subprocess shape, a 4,096-byte cut, assert the condition and that the captured log carries neither `Signal 7` nor `CORRUPTION WARNING` nor `Memory fault`. BOUNDARY §9 (:194-203) one sentence (the shape rule: a section-stripped ELF is refused — no cargo profile emits one, `strip = true` keeps section headers) and a §12 row citing both tests; docs/usage.md Case A one sentence; CHANGELOG Unreleased → Fixed; ROADMAP.md:141-152 closes. Surface: Lisp API additive — a load that returned a working crate (or faulted) for a corrupt file now signals the documented condition.

**Acceptance.** Against the unmodified loader the first test fails because the load SUCCEEDS (today: `LOADED #<CRATE "wordbag" gen 1 abi 1 :: 35 fns, 2 handles, package WORDBAG>`, `greet -> Hello, x!`) and the second's log contains `Signal 7`; after: both green on all seven jobs, `grep -c 'Signal 7\|CORRUPTION WARNING' <log>` = 0, the SBCL/Linux `load-blob-crate` step (ci.yml:122-135) still prints `BLOB-ASSET-OK`, and a local run of the check alone over the darwin and windows v0.6.0 assets accepts all twelve.

### 3. One `.asd` in the tarball: `rulisp-ecl-smoke.asd` becomes a template copied at build time, never committed (S)

**Demand.** The maintainer's candidate; stability.md §9 (:171-177) and what a dist builder does: Ultralisp's systems index carries `onlyarche-rulisp rulisp-ecl-smoke rulisp-ecl-smoke rulisp` and its release line names `tests/ecl-program/rulisp-ecl-smoke.asd` (fact sheet, `sh tools/check-dist.sh` at 0.6.0) — a program-op CI consumer is a quickload-able public system today, and the Quicklisp submission (criterion 5) would index it for good. Verified on ECL 21.2.1 twice (scratchpad/panel7/rehearsal-1.0/ecl-{build,run}.log; eval/eclsmoke/result.log): with no committed .asd and the same defsystem text present at build time, `ecl --norc --load tests/ecl-program/build.lisp` prints `BUILD-DONE` and the executable prints `Hello, program-op!` and `ECL-PROGRAM-OK`.

**Scope.** `git mv tests/ecl-program/rulisp-ecl-smoke.asd tests/ecl-program/rulisp-ecl-smoke.asd.in` (byte-identical, so docs/claims.md:279-290's nine `rulisp-ecl-smoke.asd:N` citations become `.asd.in:N` with the same numbers and the reader still has the recipe file docs/distribution.md:135 Pattern B′ points at); tests/ecl-program/build.lisp copies it to `rulisp-ecl-smoke.asd` (three lines, before pushing `here` onto `asdf:*central-registry*` at :11) with a header comment "GENERATED at test time — not committed, not in any dist"; .gitignore gains `tests/ecl-program/rulisp-ecl-smoke.asd` next to the executable and the logs; Makefile:95 `-eq 3` → `-eq 2` and the :84-88 comment ("three today" → two); the ci.yml dist-dry-run step comment (:112-116); docs/stability.md:171-176 names `rulisp` and `rulisp/test` and adds the sentence that the ECL smoke consumer's system file is generated by its build script and is in no tarball; tools/check-dist.sh additionally prints the dist's system rows for `onlyarche-rulisp` so the next poll is visible; CHANGELOG Unreleased → Changed (a user who quickloaded `rulisp-ecl-smoke` from Ultralisp — none known — loses it). Item 8's clean list gets the generated file. Surface: none — a test consumer leaves the tarball; it was never a documented entry point.

**Acceptance.** `git ls-files '*.asd'` prints only lisp/rulisp.asd (two files today); `make dist-dryrun` passes asserting exactly 2 `DRYRUN-OK` (asserts 3 today, so the count change is the failing-first edit); `make test-ecl-program` stays green on the ECL job with `ECL-PROGRAM-OK`; after Ultralisp's next poll of a tagged 0.7.0, `sh tools/check-dist.sh` shows two system rows (external, recorded in CHANGELOG, not gated).

### 4. Version-agnostic manifest-golden comparison: the `:rulisp-version "0.4.0"` placeholder and releasing.md step 3's hand edit go away (S)

**Demand.** The maintainer's candidate. docs/releasing.md:16-22 step 3 tells the releaser to update two literal substitution strings (tests/suite/m2.lisp:141-143 `string-replace-once ":rulisp-version \"0.4.0\""`; examples/wordbag/tests/manifest_golden.rs:12-16 `.replace(":rulisp-version \"0.4.0\"", …)`) whenever a regenerated golden carries a different version; wordbag's exports changed twice in 0.6 (golden regenerated 2026-09-09), so a 0.7 wordbag change followed by the 1.0.0 bump would rewrite the placeholder on two more sites — a gate whose correctness rests on a literal in two test files is exactly what lapses on release day. manifest_golden.rs is also the file an example author copies for their own golden test, and it reads as if 0.4.0 meant something.

**Scope.** Both tests substitute by pattern, not literal: locate `:rulisp-version "` in the golden text and replace through the closing quote with the running version — m2.lisp: a six-line `%substitute-rulisp-version` (a `search` and the next `#\"`, no regex library in the ASDF tree) still using `built-with` = `manifest-rulisp-version` of the loaded crate (m2.lisp:139-143), so a renderer that stopped emitting the key still fails; manifest_golden.rs: the same with `str::find` and `rulisp::runtime::RULISP_VERSION` (no regex crate in wordbag's dev-deps). The byte-identical comparison after substitution is unchanged (stability §1's manifest gate keeps its bytes); the `x86_64-unknown-linux-gnu` target substitution stays. tests/golden/wordbag.manifest.sexp regenerated once so it carries a real version and its header comment says the value is substituted whatever it is; docs/releasing.md step 3 shrinks to "regenerate only when the renderer's output changed". Surface: none — the manifest bytes a crate embeds are unchanged; only the comparison is.

**Acceptance.** `sed -i 's/:rulisp-version "[^"]*"/:rulisp-version "9.9.9"/' tests/golden/wordbag.manifest.sexp` then `make test-m2` and `cargo test -p wordbag --test manifest_golden` stay green (both red today — the substitution matches only `"0.4.0"`); any other byte changed in the golden still turns both red; `grep -c '0\.4\.0' tests/suite/m2.lisp examples/wordbag/tests/manifest_golden.rs docs/releasing.md` is 0; a scratch bump of the three Cargo.tomls to 0.7.0 leaves both green without touching the golden.

### 5. Every attached asset has a job that runs its suite on its host: promote the macOS fetch step by its streak; fetch on Windows starts best-effort (S)

**Demand.** The maintainer's candidates; ci.yml:170-171's own comment ("promoted by the written procedure … once it has a streak"). The step `fetch suite (best-effort on macOS)` (:172-174) has succeeded in 37 consecutive runs since 2026-09-03 and has never itself failed — its only two non-success entries were skips after an earlier macOS step failed (fact sheet); the latest log shows `Did 117 checks. Pass: 117` and `audit ok: …/libfetch.dylib`; ECL's precedent was 24. Windows: blobs.yml attaches `fetch-windows-x86_64.dll` to every release and no job has ever loaded it — examples/fetch/audit.sh:10-12 globs only `.so`/`.dylib` and exits `FAIL: libfetch not built`, while the Windows job already prints `audit ok: target/debug/fetch.dll` from `make audit` (Makefile:54) and runs m7.dump-restore's `sbcl --non-interactive` spawn (467 checks) — the same spawn tests/suite/fetch.lisp:567-569 uses; the test server binds `127.0.0.1:0` through tokio. A blob without a test host is the mistake v0.4 fixed; v06-plan refused this as "not a surface question" — with the flagship's suite on Windows (item 7) it becomes a consistency question.

**Scope.** (a) ci.yml:172-174: drop `continue-on-error: true` (:173), rename the step `fetch suite (async HTTPS example)`, the comment records the 37-run streak and the demotion procedure (a step demotes as a job does: one commit, wording downgraded, cause in ROADMAP); README.md:126-128 → "SBCL on Linux and macOS, and CCL on Linux, also run the async HTTPS example"; docs/claims.md:70 and :148 and BOUNDARY §12 rows :362, :366, :392 add the macOS job to their CI citation. (b) examples/fetch/audit.sh:10-12: add `../../target/debug/fetch.dll ../../target/release/fetch.dll` to the glob (Makefile:54's shape); ci.yml Windows job (:177-209, already `shell: bash` at :181, llvm-tools at :191): after `make test-m4` (:209) a step `fetch suite (best-effort on Windows)` / `continue-on-error: true` / `run: make test-fetch`, carrying the promotion-by-streak comment the macOS step had. If the step is red for a Windows-specific cause (the dump test's `sbcl.exe` spawn, path quoting, the second 140-package fetch build into examples/fetch/target), the cause goes to ROADMAP and the step stays best-effort — never a dropped step. CHANGELOG. Surface: none.

**Acceptance.** `grep -c '^ *continue-on-error: true' .github/workflows/ci.yml` stays 2 (the macOS line leaves, the Windows line arrives; the comment mentions at :216/:244 do not match the anchored pattern); the macOS required job's fetch step has no `continue-on-error`, so a deliberately broken fetch assertion on a branch turns the macOS job red; the Windows job log shows `audit ok: ../../target/debug/fetch.dll (pe)` and either `Did 117 checks. Pass: 117 (100%)` or the first Windows-specific fetch failure, recorded in ROADMAP — red or green, the streak starts. `tools/required-ci-green.sh` unchanged (steps are not jobs).

### 6. aarch64: the deployment path exercised from a branch, then the one-commit promotion when the streak reaches ECL's count (S)

**Demand.** The maintainer's candidate; ROADMAP.md:36-46 holds the one-commit checklist; stability.md:99-103 requires "a documented green streak with the platform's deployment path exercised". The streak: 20 consecutive green post-fix runs (34097612195 … 34814492197, fetch step green in every one), 18 since ROADMAP's own restart date 2026-09-08, against ECL's precedent of 24 (ci.yml:212-213). The deployment path has never run on arm: blobs.yml:40-53 has three legs and the arm job has no blob step (ci.yml:246-260). The checklist's `load-blob-crate` step cannot exist before an arm asset does — `gh release download v0.6.0 --pattern libwordbag-linux-arm64.so` finds nothing (v0.6.0 has exactly 12 assets) — so the checklist as written would red a newly required job on every push until the tag. Verified for the release job: x86-64 `nm` lists a cross-linked aarch64 ELF (scratchpad/panel7/arm-elf/libarm.so, e_machine 0xb7) and tools/rulisp-audit.sh answers `audit ok (elf)`, so the Linux re-audit of 16 downloaded assets works with the tools it has.

**Scope.** Commit A, on a branch, now: blobs.yml matrix gains `ubuntu-24.04-arm / linux-arm64 / so / lib`; `gh workflow run blobs.yml --ref <branch> -f tag=v0.6.0 -f publish=false` (inputs at blobs.yml:16-24) builds v0.6.0's four examples on arm, audits them there, and the release job re-audits 16 downloaded assets with the publish step skipped (:132) — the deployment path exercised, no release touched; the branch is not merged before B (a blob without a required host is the v0.4 mistake). Commit B, when the count from 2026-09-08 reaches 24 consecutive green runs — six more pushes, which items 1-5 and 7 supply mid-cycle (if the maintainer reads the precedent as 35 days: 2026-10-13, also inside the cycle): ci.yml:246 `(required)`, :248 drop `continue-on-error`, the :240-245 comment rewritten into the ECL-style promotion/demotion record with run ids and dates; the leg from A; blobs.yml:138 `-eq 12` → 16 and its message (`4 hosts × 4 crates`); docs/releasing.md:42,45 twelve → sixteen with `lib<crate>-linux-arm64.so` in the list; README.md:121 and :126; docs/stability.md:96-97 (no best-effort job today); docs/usage.md:44-46; docs/installation.md's host table; docs/claims.md:219-220 and :275 plus a host row; tools/required-ci-green.sh:20 `MIN_REQUIRED` +1 and its comment (7, or 8 if item 9's counting of the cargo-tests job has landed first); a ROADMAP entry with the run count and ids; CHANGELOG. The arm `load-blob-crate` step (ci.yml:122-135's shape) lands in the post-0.7.0 "open the next cycle" commit (docs/releasing.md:56 step 9) pinned to v0.7.0, the first release with an arm asset; until then run-m4's `v02.blob-loading` (467 checks on arm) exercises `host-blob-suffix` → `linux-arm64`. A second, different arm failure before the count: the job stays best-effort and ROADMAP records how many runs it has. Surface: none.

**Acceptance.** A: the dispatched blobs run shows `audit ok` ×4 on the arm leg and `re-audited 16 assets` with no release step run. B: the job name ends in `(required)`, `sh tools/required-ci-green.sh <B's sha>` counts one more job and passes, `grep -c '^ *continue-on-error: true' .github/workflows/ci.yml` drops by one, README's aarch64 cell reads `required`, and `gh release view v0.7.0 --json assets --jq '.assets|length'` → 16 (docs/releasing.md step 7 says sixteen). After step 9: the arm job's blob step prints `BLOB-ASSET-OK Hello, release!` from `libwordbag-linux-arm64.so`.

### 7. The flagship: examples/wasm gets the suite it never had, then grows a WASI plugin sandbox (L)

**Demand.** The maintainer's explicit ask, the new house rule's first case: "the very first reason I made this project was wasm" and "the AI-agent programs being built in Rust". The candidates were probed, built and audited (scratchpad/panel7/, fact sheet) and ranked on BOUNDARY §7 signal risk, footprint/CI build time, boundary-vocabulary fit and hermeticity: (a) **wasm + WASI — chosen.** wasmi 0.50 + wasmi_wasi 0.50 (WASI preview1; wasi-common/wiggle 34 from the wasmtime repository, the `wasmtime` crate itself absent from the tree) + cap-std 3: 117 lockfile packages (+91 over today's 26), 32 build scripts, no C compiled, cold build 43 s on 4 vCPU, 5.4 MB release cdylib, `audit ok` with no signal-ish import at all, no wasmtime/cranelift/signal-hook anywhere; the probe's 56-line hand-written `.wat` guest (fd_write, args_get, path_open under preopen fd 3, the `../` escape, proc_exit) runs end to end with exact bytes — no wasm-targeting toolchain, since `Wasm::load` already parses `.wat` (examples/wasm/src/lib.rs:56-62) and the `wat` crate is a dependency. It needs no thread, no runtime, no `on_dump`, no new boundary feature, and no fifth crate (blobs.yml:72/:138, releasing.md:42/45, claims.md:219/275, usage.md:44-46 all count four examples — the sites item 6 already moves). It pays a debt the fact sheet exposes: examples/wasm has ZERO tests (`grep -rni wasm tests/ lisp/rulisp.asd` → nothing) while README.md:94-104 makes six claims cited to source lines (docs/claims.md:58-64), SECURITY.md:42-44 calls it "the supported approach" for untrusted logic, and three libwasm blobs ship per release that no job has ever loaded. What a Lisp user gets: run third-party or untrusted code compiled from Rust/C/Zig/Go (`wasm32-wasip1`) from the REPL with a CPU budget, a memory cap, a chosen directory as its whole filesystem, its stdin/stdout/stderr as Lisp bytes and its exit code as a value — the shape of an agent's tool sandbox. (b) **MCP on rmcp 3.4** — feasible for later, verified: server + `()` client over `tokio::io::duplex` in one cdylib round-trips, 75 packages, 58 s, audit ok; but it is fetch's whole pattern again (tokio, a pull queue with capped waits, `on_dump`, dump subprocess tests, an adversarial pass — v0.3 took 11 days and 18 findings) as a fifth crate, its two standard transports are exactly what §7 refuses or duplicates (child-process → tokio `process` + signal-hook-registry + nix, fails the audit's tokio rule; streamable-HTTP client → reqwest 0.13 → aws-lc-rs, a second TLS stack and a cmake C build beside fetch's reqwest 0.12 + ring), in-image stdio takes the REPL's fd 0/1, and messages cross as JSON `:string` with no JSON reader in rulisp's ASDF tree. The 1.1 candidate, with the finding below. (c) **tokenizers 0.23** pure-Rust (`default-features = false, features = ["fancy-regex"]`): 90 packages, 42 s, audit ok (sigaltstack only), hermetic by in-crate BPE training, ids fit `(:vec :u32)` — the clean second example for 1.x, not the flagship the ask names. (d) **candle-core** FAILS the audit today (gemm-common's `lscpu` spawn imports `signal@GLIBC_2.2.5`; a transitive default feature a downstream cannot switch off); **llama-cpp-2** builds through cmake + bindgen/libclang + C++ (unmeasured; cmake absent here), `ggml_abort()` is `abort()` (SECURITY.md's crash-isolation bullet), cpp-httplib's `signal(SIGPIPE, SIG_IGN)` compiles under the default `common` feature, and no model can be in CI; **rig-core / async-openai** audit ok but pull reqwest 0.13 + aws-lc-sys (216/241 packages, 120 s/85 s) for a deliverable — HTTPS + JSON to an API — that fetch plus a CL JSON library already gives. Sizing: fetch was 796+163+40 Rust lines, a 256-line veneer, a 583-line 23-test suite and 18 adversarial findings in one cycle; this is roughly a thousand lines with no threads, but a suite written from nothing, a sandbox claim that deserves fetch's attack pass, and six hosts getting a new step — L, honestly, and the only L.

**Scope.** Two commits and one pass, in this order. **Commit 1 — pin what ships (the existing API, the unchanged 26-package crate).** New `tests/suite/wasm.lisp` (`def-suite* :rulisp-wasm`; a lazy `ensure-wasm` doing `(rulisp:use-crate (asdf:system-relative-pathname :rulisp "../examples/wasm/"))` exactly as tests/suite/fetch.lisp:20-25 — never at top level, `make dist-dryrun` loads `rulisp/test` with cargo unreachable; `wf` via `find-symbol` on the generated WASM package), ~13 tests over the observations of scratchpad/panel7/eval/wasm-probe.lisp (SBCL 2.1.11), each stating what a failure means: `wasm.exports-lists-functions` (fib.wat → `"add,boom,fib,sum_mem"`, as wasmi lists them); `wasm.calls-coerce-i32` (add 2 3 → 5, fib 20 → 6765, and the documented wrap `add 2147483647 1` → −2147483648, lib.rs:205 `a as i32`, pinned as behaviour so a later range check is a knowing change); `wasm.trap-is-a-condition` (boom → `wasm:wasm-error` "unreachable"; the same handle then answers add); `wasm.fuel-exhaustion-traps-and-refuels` (fuel 1000, fib 30 → "all fuel consumed", `wasm-fuel-left` 0, `wasm-refuel` 10⁸, fib 15 → 610, fuel-left < 10⁸); `wasm.unmetered-fuel-api-is-a-condition` (fuel 0 → fuel-left/refuel signal "fuel metering is disabled"); `wasm.memory-roundtrip-and-bounds` (write `#(1 2 3 4)` at 0, sum_mem 0 4 → 10, read 0 4 → `#(1 2 3 4)`; read 100@65530 and write 2@65535 → wasm-error naming the bound; offset 2⁶⁴−1 refused); `wasm.host-callback-into-lisp` (guest.wat; `(rulisp:callback (lambda (x) (push x seen)))` to `wasm-on-notify`, emit_squares 5 → (0 1 4 9 16), `bt:current-thread` inside the closure is the caller's — why no `#+ecl` guard is needed); `wasm.callback-condition-becomes-trap` ("lisp callback failed (see warnings)" after the warning; the image continues); `wasm.no-callback-set-traps` ("call wasm-on-notify first"); `wasm.dead-token-traps` (`rulisp:unregister-callback` → T, then emit_squares → the dead-id warning and a trap); `wasm.freed-handle-is-refused` (`rulisp:free` → `freed-handle-error`); `wasm.missing-file-is-a-condition`; `wasm.manifest-declares-one-stored-callback` (`rulisp::crate-manifest-source` contains `:stored-callback` exactly once — the mirror of fetch.manifest-has-no-stored-callbacks, fetch.lisp:60-66, documenting the ECL trampoline cost wasm already pays). Registered in lisp/rulisp.asd after `(:file "fetch")` (:32) and as r11 in tests/run-m4.lisp:23-34 — the run-m4 route, all seven jobs including ECL (wordbag's stored callback already makes ECL `compile-file` a trampoline in run-m4 on every push, codegen.lisp:105-124 — no new toolchain requirement), Windows (`make audit` already builds wasm.dll there) and arm. docs/claims.md:58-64 re-cited to `wasm.*` tests (the audit row :64 stays); a new row re-citing SECURITY.md:42-44; CHANGELOG Unreleased → Added. The seven jobs' answer — does wasmi alone load and run through `use-crate` on Windows, ECL and arm — arrives before any WASI dependency is added. **Commit 2 — WASI, pushed on a branch first.** examples/wasm/Cargo.toml: `wasmi_wasi = "0.50"`, `cap-std = "3"` (wasmi stays 0.50; `edition = "2021"` stays; no new workspace member; the MSRV job's `-p` list, ci.yml:47-53, does not include wasm and the lockfile already carries rust-version 1.86/1.88 packages green). src/lib.rs: the existing `Wasm` handle and `Wasm::load(path, fuel)` untouched byte for byte — a 0.6.0 user's `wasm:make-wasm` keeps its signature and semantics; a second `#[rulisp::handle] Wasi` for command modules with `HostState { wasi: WasiCtx, limits: StoreLimits, stdout/stderr: Arc<RwLock<CappedBuf>> }` (the probe's shape, wasi-probe/src/lib.rs): `#[rulisp(constructor)] Wasi::load(path, fuel: u64, memory_limit: u64)` — `fuel == 0` refused with `WasmError` (the run is synchronous on the calling thread; only the fuel bound makes it finite — BOUNDARY §7's cap rule, stated in the docstring); `StoreLimitsBuilder::memory_size(memory_limit)` via `store.limiter`, and the same number caps captured stdout + stderr (fetch's uncapped-body lesson: a write past the cap fails with a WASI errno inside the guest, never a hang or a Lisp heap exhaustion) — one number bounds the sandbox; a default WasiCtx with no args, no env, no preopen, empty stdin, in-memory stdout/stderr — never inherited; `arg(&str)`, `env(key, value)`, `preopen(host_dir, guest_path)` via `cap_std::fs::Dir::open_ambient_dir` + `push_preopened_dir`, `stdin(&[u8])` → `ReadPipe`, accumulating before `run() -> Result<i64>` (calls `_start` once: Ok → 0; `i32_exit_status()` → the code as a value; any other trap → `WasmError` → `wasm:wasm-error`; a second `run` → wasm-error), `stdout() -> Vec<u8>`, `stderr() -> Vec<u8>`, `fuel_left() -> u64`. Generated names by docs/quickstart.md:96-97: `wasm:make-wasi`, `wasm:wasi-arg`, `wasi-env`, `wasi-preopen`, `wasi-stdin`, `wasi-run`, `wasi-stdout`, `wasi-stderr`, `wasi-fuel-left`; one condition class, `wasm:wasm-error`, as today. **Veneer shape: the generated package is the veneer** — nine functions and one condition; the README shows the six-line `unwind-protect`/`rulisp:free` idiom; no separate package, no JSON, no `:string` list — `exports` stays comma-joined (lib.rs:95-102), args/env/preopens are one call each, stdio crosses as `:bytes`, the exit code as `i64`. Guests, committed text: keep fib.wat/guest.wat; add `examples/wasm/wasi-hello.wat` (the probe's guest verbatim) and `wasi-cat.wat` (stdin → stdout). Tests appended to tests/suite/wasm.lisp, scratch dirs under `uiop:temporary-directory` with `rulisp::*process-tag*` names (fetch.lisp:504-506): `wasm.wasi-stdout-args-exit` (exit 7 is a value, not a condition; stdout starts `hello from wasi`); `wasm.wasi-env`; `wasm.wasi-stdin-roundtrip` (wasi-cat); `wasm.wasi-preopen-reads-inside`; `wasm.wasi-escape-is-refused` (one errno byte, the outside content absent from stdout); `wasm.wasi-symlink-out-of-the-preopen-is-refused` (`#-windows`; cap-std resolves under the dir handle — test it, do not assume it); `wasm.wasi-no-preopen-is-ebadf`; `wasm.wasi-memory-limit-refuses-and-grow-answers-minus-one` (`(memory 100)` under 1 MiB; `memory.grow` at the limit → −1); `wasm.wasi-output-past-the-cap-fails-in-the-guest`; `wasm.wasi-unmetered-is-refused` (fuel 0 → wasm-error at `make-wasi`); `wasm.wasi-fuel-exhaustion-is-a-condition` (a spinning `_start`, under 1 s); `wasm.wasi-run-is-once`. The branch push is the cross-host compile check the Linux probe cannot give: the macOS required job never compiles examples/wasm today (only blobs.yml does, at release) and Windows compiles it inside `cargo test --workspace` — run-m4's `use-crate` build of the +91-package tree on both is the test. Green everywhere → merge as is. A red host → the wasm suite moves out of run-m4 into its own runner (`tests/run-wasm.lisp` = tests/run-fetch.lisp with `:rulisp-wasm`; Makefile `test-wasm`/`test-wasm-ccl` after :28-30 with a `$(CARGO) tree --manifest-path examples/wasm/Cargo.toml | grep -Eq '(^| )(wasmtime|cranelift)'` refusal; steps on every job that runs `make test-m4`) with the red host's step `continue-on-error: true` and the run linked in ROADMAP — the written procedure, never a dropped test. README.md:94-104 gains the WASI sentence and "with its own suite in CI"; docs/claims.md rows for the new claims; CHANGELOG. blobs.yml untouched (four crates; the wasm blob grows to ~5-6 MB). **The pass — before the 0.7.0 tag:** an adversarial review of the sandbox claims as fetch got (ROADMAP.md:349-359, 18 findings): symlinks and `..` inside the preopen, a preopen that is itself a symlink, output at exactly the cap, `memory.grow` at exactly the limit, fuel reached through every export, a guest that never returns from a host import; acceptance: every finding is a `wasm.*` test or a documented limit. SECURITY.md:42-44 re-cited to the suite and **bounded** in the same commit: the sandbox is a CPU/memory/capability budget for a guest, not isolation from a wasmi or host-function bug (still in-process, per the crash-isolation bullet); the preopens are the whole filesystem; stdio is never inherited; `make-wasm` (fuel 0 allowed, uncapped) is for trusted modules and says so. **Findings for 1.x, not 0.7 wire changes** (the v0.3 rule: the flagship needs none, and it needs none): `(:vec :string)` would make args/env/exports one call instead of one-per-item or comma-joined; a structured trap payload (kind + exit code in one condition) instead of a message the caller parses; multiple values for `(exit stdout stderr)` instead of three calls; a stored callback that returns a value to the guest (host imports today are `:unit`-only, codegen.lisp:167-170) — the door to Lisp-implemented host functions with results. Each is a new type token, load-bearing, `:schema` 2 per stability §7 — additive in 1.x, refused in 0.7 as in v0.3–v0.6. Surface: none — Rust API, Lisp API golden, manifest schema and ABI untouched; the example is not a versioned surface.

**Acceptance.** Commit 1: `grep -rni wasm tests/suite lisp/rulisp.asd` non-empty (empty on HEAD); `make test-m4` prints `:rulisp-wasm` with `Fail: 0` on SBCL locally and every job's run-m4 log shows the check count above today's (470 SBCL/Linux, 457 ECL, 467 Windows/macOS/arm); mutation: `boom`'s `unreachable` replaced by `(i32.const 0) drop` fails `wasm.trap-is-a-condition`, the fib assertion written as 6766 fails naming the test; `grep -c 'examples/wasm/src/lib.rs:' docs/claims.md` drops from 6 to the audit row alone. Commit 2: from the REPL `(let ((w (wasm:make-wasi "examples/wasm/wasi-hello.wat" 1000000 1048576))) (wasm:wasi-arg w "alpha") (wasm:wasi-arg w "beta") (wasm:wasi-preopen w "/tmp/sandbox/" "/") (wasm:wasi-run w))` → 7, `wasi-stdout` a byte vector starting `hello from wasi`, `wasi-stderr` one byte, `wasi-fuel-left` < 1000000 (the probe: 998,708); `make audit` still prints `audit ok: target/debug/libwasm.so` (and `.dylib`, `wasm.dll` on their jobs) and `cargo tree --manifest-path examples/wasm/Cargo.toml -e features | grep -E 'tokio feature|wasmtime|cranelift'` is empty; removing `push_preopened_dir` fails `wasm.wasi-preopen-reads-inside`; the branch run is green on all seven jobs or the fallback runner is in place with exactly the red host best-effort; the blobs `publish: false` dispatch still counts four crates per host; docs/claims.md's footer still says `0 unverified`. The pass: its findings list is in ROADMAP with each one's test or limit named.

### 8. `make clean` removes every regenerable artifact the suite leaves in the tree; `clean-cache` is the opt-in sweep of the loader's cache (S)

**Demand.** The maintainer's observation: a 5.7 GB checkout over 37 MB of source. Makefile:119-121 removes root `target/` and tests/m1-handwritten's pair only, while .gitignore hides the rest: `use-crate` builds every crate into `<crate-dir>/target/` (lisp/src/build.lisp:47-51,:115 — a full dependency tree each: fetch 140 packages, rx, wordbag, tests/abi-fixture, and item 7's wasm at 117; the panel's probe targets ran 264-860 MB), examples/wordbag/target-abort-check (tests/suite/m1.lisp:65-77), tools/audit-fixture/target (the selftest), tests/ecl-program/rulisp-ecl-smoke and `ecl-program-*.log`, root `target/{compat,dist,audit-fixture}`; `git status` stays clean while it grows. The loader's cache (`uiop:xdg-cache-home "rulisp/"`, crate.lisp:107; one copy per load, :162) is outside the tree and shared with other checkouts and running images — its own sweep (crate.lisp:416-437) keeps other processes' copies an hour, so a Makefile deleting it by default is a surprise, not hygiene; Windows cannot unlink a mapped DLL.

**Scope.** Makefile `clean`: keep `$(CARGO) clean`, then `rm -rf` exactly .gitignore's regenerable entries, with a comment naming which target leaves each: `examples/*/target examples/*/target-abort-check tests/m1-handwritten/target tests/m1-handwritten/target-abort-check tests/abi-fixture/target tools/audit-fixture/target tests/ecl-program/rulisp-ecl-smoke tests/ecl-program/rulisp-ecl-smoke.asd ecl-program-*.log` (the generated .asd from item 3). A separate `clean-cache` target — `$(SBCL) --non-interactive --eval '(uiop:delete-directory-tree (uiop:xdg-cache-home "rulisp/") :validate t)'` — that prints the directory's size first and touches it only when invoked; docs/installation.md:160-162 ("Library cache") gains the two-line note. Not `git clean -fdX` (honours the global excludes file and deletes a developer's untracked local files). Nothing under `$HOME` moves on plain `make clean`. CHANGELOG. Surface: none.

**Acceptance.** After `make test-m4 test-fetch audit test-ecl-program compat dist-dryrun` on this host, `make clean && git clean -Xdn | grep -v '\.fasl$\|\.fas$'` prints nothing (today it lists examples/{wordbag,rx,fetch}/target, examples/wordbag/target-abort-check, tests/abi-fixture/target, tools/audit-fixture/target, the smoke executable and the ECL logs) and `du -sh .` is within a few MB of the 38 MB checkout; `make clean-cache` → `ls ~/.cache/rulisp | wc -l` is 0; the CI runners are unaffected (ephemeral).

### 9. Close the cycle: the release gate counts the semver/audit/golden job, the 1.0.0 plan is written from the measured hide, and stability §3/§8 read "as of 0.7" (M)

**Demand.** docs/stability.md:18-23 ("hiding `rulisp::runtime` is the 1.0 major's first commit, where the tool permits it") and §8 criterion 5; the maintainer's candidate ("a scratch check of cargo-semver-checks --release-type major over the runtime hide; a 1.0.0 plan document"). Measured this panel with cargo-semver-checks 0.50.0 against the crates.io 0.6.0 baseline (scratchpad/panel7/rehearsal-1.0/semver.log, semver-runtime.log; eval/semver-rehearsal.log): (a) `#[doc(hidden)]` on `pub use rulisp_runtime as runtime;` (crates/rulisp/src/lib.rs:53) is INVISIBLE to the tool — `196 pass, 58 skip` even as a minor; a re-export of an external crate carries no items, so v06-plan.md:97's "some thirty findings" came from the items, not the path, and the path hide alone frees nothing — rulisp-runtime stays checked as its own package in the action's list (ci.yml:39-43); (b) `#[doc(hidden)]` on rulisp-runtime's 26 `pub` items plus its `pub use meta::…` line fires exactly five lints under minor (`struct/enum/function/pub_static/pub_module_level_const_now_doc_hidden`, "semver requires new major version") and passes under major with `0 checks: 0 pass, 254 skip` — the Rust gate is vacuous on the 1.0 push; the run is not vacuous in general (renaming `Error::msg` in the same copy fails `inherent_method_missing`, rc 100); `cargo doc -D warnings` and `cargo test --workspace` stay green with both hides (the macros emit `rulisp::runtime` paths at 57 sites — doc(hidden) is not removal); (c) crates/rulisp/Cargo.toml:14-15 pins rulisp-macros/rulisp-runtime as caret `"0.6.0"` — exact within 0.x, but `^1.0.0` spans every 1.x, so stability §1's "helpers may change in any minor" becomes falsifiable by `cargo update -p rulisp-runtime` in a consumer unless the pins become `=X.Y.Z`; (d) tools/required-ci-green.sh:20,:34,:39 count only `(required)` hosts + MSRV — a 1.0.0 tag could be attached to a commit whose semver gate, audit or manifest golden was red. And docs/releasing.md step 9 assumes a minor: nothing says what the gates do on the major's day.

**Scope.** In the tree now, first commit of the item, any day: tools/required-ci-green.sh — the job named `cargo tests (golden manifest + trybuild)` (ci.yml:19) joins the two awk patterns (:34, :39), `MIN_REQUIRED` +1 with its comment (:20); docs/releasing.md step 4 and stability.md:88-92 name it (this makes the crates.io baseline lookup release-gating — it already runs on every push). Then `docs/design/v10-plan.md` in this format: (1) the measured section, reproducible on demand — the commands, the `196 pass` / `191 pass, 5 fail` / `0 checks, 254 skip` lines, the non-vacuity check, and the two measured options for the helpers (hide the items; or drop rulisp-runtime from the action's package list) with a decision; (2) the 1.0.0 commit's exact diff — `#[doc(hidden)]` on the re-export and on rulisp-runtime's items (`missing_docs` on that crate falls with it), `version = "=1.0.0"` pins with tools/check-versions.sh:21-22 accepting `\"=?$V\"`, every site check-versions.sh lists (docs `rulisp = "1.0"`), ci.yml:43 `release-type: major` for that one push and a documented manual `cargo semver-checks --release-type minor` over the same diff whose only findings must be the `*_now_doc_hidden` family, CHANGELOG 1.0.0 surfaces paragraph, stability §1/§3 prose, every `pre-1.0`/`before 1.0`/`until 1.0` sentence (`git grep` across README, docs/, BOUNDARY, SECURITY) that flips; (3) the release-day sequence — publish order, tag, 16 assets (item 6), required-ci-green, check-dist, a three-host docs/benchmarks.md refresh at the tag (the code users will run; benchmarks.md:101-103's procedure), then the quicklisp-projects issue text: repository URL, MIT, the two systems `rulisp` and `rulisp/test` (item 3's dry run at 2), "loads without cargo" with the CI step as evidence, the tarball being the whole tree; (4) the "open 1.1" commit — pins to v1.0.0, `release-type: minor` back with a releasing.md step that flips it once 1.0.0 is on crates.io, `make compat PREV=v1.0.0` with item 1's subset check as the Lisp-surface promise; (5) "explicitly not in 1.0" — vocabulary round 2 stays 1.x-additive with `:schema` 2 (stability §7), MCP and tokenizers as 1.1's examples. At cycle close: stability.md:64 "Reviewed for 0.6, the last minor before 1.0" → 0.7 with the review repeated (0.7 adds no export by design; every export still used; nothing to deprecate — one sentence), §8 status "as of 0.7.0" per criterion, a ROADMAP `## 1.0` stub pointing at the plan, CHANGELOG 0.7 surfaces paragraph (*Lisp API*: additive — a refusal where a fault or a false success was, item 2; no export added or removed; *Rust API*: none; *Manifest*: none; *ABI*: 1). Surface: none — documents and a shell script; the plan describes the 1.0 Rust-surface change but does not make it.

**Acceptance.** `printf 'SBCL / Linux x86-64 (required)\tcompleted\tsuccess\n…(the other required rows and MSRV, success)…\ncargo tests (golden manifest + trybuild)\tcompleted\tfailure\n' > <scratch>/j.tsv; JOBS_TSV=<scratch>/j.tsv sh tools/required-ci-green.sh deadbeef` exits 1 naming the cargo tests job (exits 0 today); `test -f docs/design/v10-plan.md && grep -q 'release-type: major' docs/design/v10-plan.md` and the file quotes both `196 pass` and `254 skip`; in a scratch export with rulisp-runtime's items hidden, `cargo semver-checks check-release -p rulisp-runtime --release-type minor` fails and `--release-type major` passes; `git grep -c 'release-type: minor' .github/workflows/ci.yml` = 1 (the flip is 1.0's commit, not 0.7's); `grep -n 'Reviewed for 0.6' docs/stability.md` empty; `sh tools/check-1.0.sh` exits 0.

**Cut order if the cycle runs long:** item 7's commit 2 and its pass (the WASI half slips to 1.1 whole — it touches no surface and its blob registration is already nil; commit 1, the suite, stays: it is the debt, not the feature); then item 5's Windows half (the best-effort step and the glob); then item 9's plan document folds into a ROADMAP `## 1.0` paragraph carrying the measured numbers (the required-ci-green change and the §3/§8 sentences stay). Item 6 B follows its count and costs nothing to defer; item 6 A is a dispatch. Items 1, 2, 3, 4 and 8 are a day each and are not cut: each is a reproduced defect, a gate that reds on an allowed change, or a dist row the submission would freeze.

## Explicitly not in v0.7

- **An MCP example on the official rmcp crate — Lisp as a tool host or as a client — as the flagship.** Verified feasible for LATER (scratchpad/panel7/rmcp-duplex-probe: rmcp 3.4 server + `()` client over `tokio::io::duplex`, `tools=["echo"]`, `echo: hi`; 75 packages, 58 s, audit ok, tokio without `signal`/`process`). Not now, for causes not size alone: the two STANDARD MCP transports are the ones the boundary refuses or duplicates — child-process stdio pulls tokio `process` + signal-hook-registry + nix 0.31 (fails tools/rulisp-audit.sh's tokio rule; `std::process::Command` alone imports `signal@GLIBC_2.2.5`, the std-command probe) and the streamable-HTTP client pulls reqwest 0.13 → aws-lc-rs (a second TLS stack and a cmake/nasm C build beside fetch's reqwest 0.12 + ring); in-image stdio (`rmcp::transport::stdio()`, io.rs:4) takes fd 0/1 the REPL owns; server-side tool calls arrive on tokio workers and must reach Lisp through fetch's pull design (queue, capped waits, `on_dump`, refuse re-entry — a callback cannot return a value, codegen.lisp:167-170, and would force `compile-file` on ECL) — v0.3's whole cycle again, plus a fifth crate reopening every asset-count site item 6 edits; messages are JSON `:string` and rulisp's ASDF tree (cffi, babel, trivial-garbage, bordeaux-threads, uiop, fiveam) has no JSON reader, so veneer and suite would add a Quicklisp dependency no example has had. **Finding for 1.x, no wire change:** an MCP client from Lisp wants Lisp to spawn the server (`uiop:launch-program`) and hand Rust the pipe descriptors as integers (`i64` fits; Windows HANDLEs too) for the `transport-async-rw` path. The 1.1 candidate, first example after the runtime is hidden and the arm leg has shipped once — a fifth blob is then a plain additive release change.
- **tokenizers (HuggingFace) as a second 0.7 example.** One flagship per cycle (v0.3 was one). Ready when wanted: pure-Rust features (`default-features = false, features = ["fancy-regex"]`) → 90 packages, 42 s, no C/C++, audit ok (sigaltstack only); hermetic by training a tiny BPE in-crate at test time (BpeTrainer; vocab > 256); ids `(:vec :u32)`, tokens-as-strings by per-index accessors on an Encoding handle. Vendoring a tokenizer.json is refused on size and licence: bert-base-uncased 466 KB, gpt2 1.36 MB, t5-small 1.39 MB against the tree's largest fixture (7,500 B), and the two "tiny" candidates carry no licence field. An S/M for 1.x, after MCP.
- **llama-cpp-2 (llama.cpp bindings).** llama-cpp-sys-2 vendors llama.cpp and builds through cmake + bindgen (libclang) + a C++ toolchain on every job, build time unmeasured (cmake absent on the probe host; ubuntu-24.04 runners ship CMake 3.31 and clang 18, but the macOS and Windows legs each pay it); `ggml_abort()` is `abort()` — a guest fault takes the image down (SECURITY.md's crash-isolation bullet); cpp-httplib's `signal(SIGPIPE, SIG_IGN)` (httplib.cpp:7608) compiles under the default `common` feature (build.rs:657-658) and whether it links into a glue artifact is unverified; no hermetic model — CI cannot download weights and no tiny GGUF is generated in-tree. Reopen only with a measured build, `default-features = false`, and a generated fixture model.
- **candle-core (CPU tensors).** FAILS tools/rulisp-audit.sh today: gemm-common 0.19.0 src/cache.rs:232 runs `std::process::Command::new("lscpu")` under its default `std` feature (cache.rs:74-80), a transitive default a downstream Cargo.toml cannot switch off, so the real-usage artifact imports `U signal@GLIBC_2.2.5` (scratch/pass2.log). No handler is installed, but the symbol rule is what BOUNDARY §12:370 cites, and relaxing it for "spawn paths" would excuse a real offender. Also 202 packages, 860 MB target, tokenizers with the onig C build pulled unconditionally, and weights that cannot be in CI. Reopen on an upstream gemm feature that skips `lscpu`.
- **rig-core or async-openai (LLM-API clients) with a loopback stub.** Both audit ok (aws-lc's console.c `sigaction` is compiled into libcrypto but not linked), but each pulls reqwest 0.13 → aws-lc-rs/aws-lc-sys (a cmake C build; nasm or prebuilt objects on Windows MSVC) beside fetch's reqwest 0.12 + ring in one `cargo test --workspace` (rig-core 216 packages/120 s with a tree floor of rust-version 1.90; async-openai 241 packages/85 s); the deliverable — HTTPS + JSON to an API — is what fetch plus any CL JSON library already gives; the typed schemas would cross as JSON `:string` anyway; the hermetic test is fetch's testserver.rs again. A recipe on top of fetch (docs), not an example.
- **A new `examples/wasi` crate instead of deepening examples/wasm.** blobs.yml builds the four crates by name (:72) and asserts 12 assets (:138); releasing.md:42/45, claims.md:219/275, usage.md:44-46 and the release message count four — a fifth reopens every site item 6 moves for arm, and the examples stay four-per-host for load-blob-crate users. The ask names wasm; its six README claims have no test; the `wasm` package is what a 0.6.0 user already calls, and `make-wasm` keeps its signature.
- **Upgrading to wasmi 2.0 / wasmi_wasi 2.0 while adding WASI.** The example pins 0.50 and the probe verified the 0.50 pair end to end; 2.0 (2026-09-01, rust-version 1.86) changes defaults (memory64, auto-dispatch) and the Store/Linker/Config API, and wasmi_wasi 2.0 pulls wasi-common 36. A separate S once item 7's suite exists to catch the regressions — the suite first, the bump after.
- **Compiling a real-world .wasm inside the suite (`rustup target add wasm32-wasip1`, `clang --target=wasm32-wasi`), a CI step that installs the target, or a committed prebuilt .wasm.** A toolchain download inside `make test-m4` breaks the hermetic rule and a developer's local run; `Wasm::load` already parses `.wat` (lib.rs:56-62) and the 56-line guest exercises fd_write, args_get, path_open and proc_exit; a committed binary is unreproducible bytes at 100 KB-2 MB against a 7.5 KB largest fixture; the optional rustup CI step was cut by every judge. The real-world flow is a README recipe; reopen when a user brings a .wasm that fails to run.
- **WASI preview2 / the component model.** wasmi_wasi implements preview1 only (0.50 and 2.0 alike); Rust's `wasm32-wasip1` emits preview1 modules, which is what the plugin story needs; preview2 needs wasmtime's component machinery — the crate BOUNDARY §7:173 refuses by name.
- **WASI sockets (`preopened_socket`), `inherit_stdio`, environment inheritance, wasi-threads.** No demand; inheriting the image's stdio is the fd-sharing hazard the sandbox exists to avoid; sockets would let the sandbox reach the network — outside the plugin story and untestable hermetically without a loopback peer; wasmi has no threads proposal. args/env are explicit pushes only.
- **Vocabulary round 2 for the flagship — `(:vec :string)`, structured trap payloads, multiple values, callback results.** The flagship needs none (item 7 records what it would want as 1.x findings); a new type token is load-bearing and raises `:schema` per stability §7 — additive in 1.x, never a 0.7 wire change; refused with cause in v0.3, v0.4, v0.5 and v0.6.
- **A separate WASI veneer package, or a second output-size knob distinct from the memory limit.** Two judges cut the veneer: the generated package (nine functions, one condition) is the API and the README shows the idiom; a veneer file would be a second thing to keep documented in the last minor. One number bounds the sandbox — linear memory and captured output alike; a second parameter is configurability nobody asked for. `make-wasm` (unmetered, uncapped) stays for trusted modules and says so.
- **A per-example `examples/wasm/audit.sh` like fetch's.** `make audit` (Makefile:50-55) already runs tools/rulisp-audit.sh over libwasm.{so,dylib,dll} on every host through the magic-dispatching reader, and blobs.yml re-audits the release bytes; fetch's wrapper exists for its OpenSSL rule and is itself the Windows blocker item 5 fixes. wasm's analogue — no wasmtime/cranelift in the tree — is one `cargo tree` grep, in the Makefile target if the fallback runner is needed.
- **Hiding `rulisp::runtime` in 0.7 "because the tool is clean".** Measured: the re-export hide is invisible to cargo-semver-checks even as a minor — but stability §3, CHANGELOG 0.6.0 and v06-plan promise it as the major's first commit, and the path hide alone leaves rulisp-runtime's 26 items checked as their own package; the hide that frees the helpers fires five `*_now_doc_hidden` lints and requires the major. Recorded with the numbers in item 9's plan; landed at 1.0.0.
- **Exact `=X.Y.Z` pins between the three crates in 0.7; dropping rulisp-runtime from the semver action's package list in 0.7.** On 0.x, `^0.6.0` already means `<0.7.0`, so nothing can drift today; the hazard opens at `^1.0.0` and the pin lands in the 1.0.0 commit with the check-versions.sh regex change. Changing the gate's package list in 0.7 removes a check for nothing; both options are measured in item 9's plan for the 1.0 decision.
- **docs/api.md — a generated Lisp API reference with a staleness test.** No user, issue or 1.0 criterion asks for it; v06 item 8's docstrings are pinned by `v06.exports-are-documented` and reachable from the REPL; a generator plus a new SBCL-only test in the last minor is scope its own proposer listed first to cut. A 1.0.0 release-day docs item.
- **fetch on Windows as a required step this cycle; the fetch or wasm suites on ECL beyond run-m4.** Windows fetch has never run there — it starts best-effort (item 5) and follows the same streak procedure the macOS step did; a required job must not flake on the first Windows-specific socket or path difference. fetch on ECL: its dump tests are `#+(or sbcl ccl)` named passes there, no §12 or claims.md row cites it, and ECL is the slowest required host (3.7 min); the wasm suite reaches ECL through run-m4 already.
- **Scheduling the arm promotion by calendar or effort; promoting on today's count; attaching arm assets to v0.6.0 retroactively.** The streak decides (v06's refusal stands; ECL's number was 24; ROADMAP.md:36-37 dates the restart 2026-09-08). Item 6 only sequences the deployment rehearsal the procedure requires and names the count so the decision is mechanical. A `publish=true` dispatch putting a blob on a release cut before the host was required is the v0.4 mistake and muddles the record; the arm `load-blob-crate` step waits for v0.7.0 and the step-9 pin move.
- **A Rust build cache action (Swatinem/rust-cache).** Jobs run 1.1-3.7 min today (the Windows outliers were the choco feed, not cargo); item 7 adds about a minute per Lisp job; a cache is a third-party action with staleness modes and nothing demanded it. Reconsider when a job passes ten minutes.
- **Raising the MSRV for the flagship's dependencies.** Not needed: the MSRV job checks only the three crates plus wordbag (ci.yml:47-53) and the lockfile already carries rust-version 1.86-1.88 packages green; raising it is a minor even in 1.x (stability §6). Reopen on the first user report of a 1.78 build failure.
- **A three-host benchmarks.md refresh.** No measured row changed since 43f3c9c (CHANGELOG 0.6.0 has no perf entry; item 2's check reads a few KB before a 6 MB copy; the WASI deepening is not a boundary cost — tests/bench.lisp measures wordbag and rx crossings); "not a CI gate". The right date for a new baseline is the 1.0.0 release, which item 9's plan schedules as a release-day step.
- **The Quicklisp submission itself.** The 1.0 decision (stability §9, v06's refusal); item 9 drafts the issue text so the day discovers nothing and item 3 removes the one system it would wrongly index.
- **`git clean -fdX` as the clean implementation; `make clean` sweeping `~/.cache/rulisp` by default.** The former honours the global excludes file and deletes a developer's untracked local files; the latter reaches a cache shared with other checkouts and running images, whose own sweep keeps other processes' copies an hour (crate.lisp:416-437) and cannot unlink a mapped DLL on Windows. The explicit list is what .gitignore enumerates; the cache is `clean-cache`, opt-in.
- **The ECL smoke defsystem inlined into build.lisp, or a `.gitattributes export-ignore`, instead of the `.asd.in` template.** Inlining is verified too (eval/eclsmoke) but turns the documented Pattern B′ recipe file into code and orphans docs/claims.md:279-290's nine line citations; `export-ignore` helps builders that use `git archive`, while the dist actually serving rulisp (Ultralisp) tars its own checkout — the served archive is the whole tree, 324,760 B — so the one builder in use may ignore it.
- **An `@RULISP_VERSION@` token in the manifest golden, or regenerating the golden at every release.** A magic token reintroduces a hand step at every regeneration (the rendered output must be re-tokenised) and the renderer must never emit it; the pattern substitution in item 4 needs neither. A golden diff on every release carries no information — the v0.5 decision stands.
- **Reload memory growth as a defect.** Measured this panel: 200 reloads of wordbag cost +115 MB RSS (≈575 KB each, 360-560 ms per reload) while the cache sweep holds at 12 files — the accepted one-mapping-per-reload leak BOUNDARY §9:196-198 documents ("zero in production"). A number for the docs, not a code change.
- **Changing examples/wasm's i64→i32 argument coercion into a range check.** lib.rs:205's `a as i32` is documented ("values are coerced per the function's actual signature"); no user asked; item 7 pins the documented behaviour so a later change is a knowing one.
- **Refusing only truncations that cut a PT_LOAD (accepting debug-only truncation).** An ELF without its section-header table is corrupt by construction — no cargo profile emits one — and the debug-only case is exactly the one that becomes a fault at first call on a larger artifact; item 2 refuses both with one rule per format.
- **Renaming the `cargo tests` job to end in `(required)`; a repository ruleset forcing PRs on main.** The suffix would muddle stability §5's meaning of required (= a supported host) — item 9 counts the job by its own name instead. The ruleset is the maintainer's call, recorded in v0.6 item 5 and unchanged; zero pull requests exist and every cycle has landed by direct commits with CI and the release gate as the check.
- **A deprecation round, any rename, or an export added "while we still can".** Nothing to deprecate (stability §3's review; every export used); 0.7 adds no export by design, so the §3 review at close is one sentence (item 9); "while we still can" is the speculative-signature case v06 refused.
- **Miri over the shims, cargo-fuzz, ASan/TSan, a soak workflow, LispWorks/Allegro/ABCL/ocicl, macOS x86_64 blobs, any ABI bump, a retroactive `:schema 2`, `dlclose`, constructor `&key`, push/doorbell callbacks, on_dump reload semantics, print-object polish.** The v0.4-v0.6 refusals stand unchanged (v04-plan.md:90-101, v05-plan.md:75-87, v06-plan.md "Explicitly not in v0.6"); no new demand case appeared; sanitizers cannot coexist with SBCL's signal-driven GC; Miri is re-proposed only with a concrete UB hypothesis — none appeared (item 2's hazard is a corrupt input to glibc, not UB in the shims).

## Start here

**Task:** item 1, one commit. Files: `/home/arche/devel/lisper/rulisp/Makefile` `compat` target (:70-82) — after `git archive $(PREV) | tar -x` (:78) and the `cp lisp/src/*.lisp` (:79): assemble `$(COMPAT)/tree/lisp/rulisp.asd` from this tree's `rulisp` defsystem (`awk '/^\(asdf:defsystem #:rulisp\/test/{exit}{print}' lisp/rulisp.asd`) plus the archived `rulisp/test` defsystem (`mv` the archived file to `rulisp.asd.orig` first, then `awk '/^\(asdf:defsystem #:rulisp\/test/,0' $(COMPAT)/tree/lisp/rulisp.asd.orig`), guard that both forms were found; save `$(COMPAT)/tree/tests/golden/lisp-api.sexp` as `$(COMPAT)/prev-lisp-api.sexp` and copy this tree's `tests/golden/lisp-api.sexp` over it; run new `/home/arche/devel/lisper/rulisp/tests/compat/api-subset.lisp` with the two golden paths (read both with `*package*` = RULISP, every previous row `equal` to a row of the tree's, print `API-SUBSET-OK` or the first offending row, exit 1) before the old-suite leg (:80-82). Rewrite the Makefile comment (:62-67) and the compat paragraph of `/home/arche/devel/lisper/rulisp/docs/stability.md` §1 (:40-46) so the rule reads: an export the previous release had must be present with the same kind, superclasses and lambda list; additions pass; a lambda-list reprint counts. `/home/arche/devel/lisper/rulisp/CHANGELOG.md` Unreleased → Changed.

**Failing-first check** (already reproduced this panel, scratchpad/panel7/compat-probe/probe-result.log): in a scratch clone add `#:compat-probe` to `lisp/src/package.lisp` and a documented defun, regenerate the golden — `make compat PREV=v0.6.0` exits 2 with `V06.EXPORTED-API-GOLDEN … exported symbols not in the golden: (RULISP:COMPAT-PROBE)`; after the change it exits 0. Then drop `#:retry-build` and regenerate — it must exit 1 with api-subset naming `RETRY-BUILD`. Then add `lisp/src/probe.lisp` to the `rulisp` defsystem with a function crate.lisp calls — the old-suite leg must load it (today the archived .asd never does).

**Second commit, the same day:** item 5 (the macOS fetch step's promotion and the Windows best-effort step — earned, one commit, no dependency), and item 6 A's `publish=false` dispatch from a branch so the arm deployment path has run before its count is reached. **Next day:** item 2 (the loader check, before `copy-file`, with tests/suite/v07.lisp — the 60 % test fails on the unmodified loader because the load succeeds), then item 7's commit 1 (tests/suite/wasm.lisp on the run-m4 route: `make test-wasm`-style acceptance is `make test-m4` printing `:rulisp-wasm` with `Fail: 0` on SBCL, and the seven jobs' run-m4 logs decide whether commit 2 keeps the run-m4 route or gets the fetch-style runner). Every push from here on adds a run to the arm count.
