# rulisp v0.6 — "the no-break cycle, measured"

**Theme:** 0.6 is the 1.0-candidate cycle that turns docs/stability.md §8 criterion 3 into a CI fact instead of a sentence: each of the four surfaces gets a gate keyed to the 0.5.0 release that goes red the first time anything moves (cargo-semver-checks with `--release-type minor`; a golden of the exported Lisp API; pinned three-way loader/crate/suite compatibility plus an `abi_version()=2` fixture), the last BOUNDARY §12 GAP closes, the arm test's GC-timing assumption is fixed so the promotion clock can start, "required" becomes something the release job checks, and the reproduced loader defects that only a pre-freeze minor may fix land as changes classified up front — additive under §1's letter (a host error becomes the documented condition; NIL becomes None; 15 exports), or the one soundness fix under §4 (the exported `setf` that defeats the generation gate). No new boundary feature, no flagship, no hide of `rulisp::runtime` (that is the 1.0 major's first commit). ABI 1 and `:schema` 1 untouched; every wire change would be an additive manifest key under BOUNDARY §11 — none is planned; nothing adds an ECL load-time toolchain requirement; the deprecation policy (§3) is not exercised because the review found nothing to deprecate. Budget 2 M + 12 S; cut order at the end.

Verified today against the tree (read-only): HEAD is `beaf9fa` = `v0.5.0-1`, working tree clean, CHANGELOG.md:8 already reads `## Unreleased (0.6 development)`. `lisp/src/package.lisp` exports **32** symbols; `rulisp-version-skew` (conditions.lisp:78) is :INTERNAL and `tests/suite/v05.lisp:103` reaches it as `rulisp::`; eleven condition readers (conditions.lisp:16,22-23,30,48-49,57,64,69-71) are unexported while their siblings are; `grep -n :documentation lisp/src` hits only handle.lisp:28, conditions.lisp:89 and the generated classes at crate.lisp:314; `crates/rulisp/src/lib.rs:70` still documents `Error::msg` as signaling `<crate>:rust-error`. `cargo semver-checks --version` and `cargo public-api --version` → "no such command" (rc=101); `pub use rulisp_runtime as runtime` (lib.rs:53) carries no `#[doc(hidden)]`; the macros name `::rulisp::runtime::` at 57 sites (macros lib.rs:492-1421). `.github/workflows/ci.yml:99` downloads the *latest* release with no tag and no checkout has `fetch-depth`; branch protection is absent (`gh api …/branches/main/protection` → 404) and rulesets are `[]`. `grep -c '^|.*GAP' BOUNDARY.md` = 1 (:363, the Windows audit; :405 still says "All five GAP rows"); `tools/rulisp-audit.sh:31-35` dispatches on `uname -s` and SKIPs with exit 0 elsewhere; the four v0.5.0 `.dll` assets import none of the Windows handler installers (panel/pe-imports.py). The arm job's one failure (job 100926334693) reads `(LIVE-BAGS) evaluated to 893 which is not = to 1000` — the pre-GC assertion at `tests/suite/m1.lisp:142`, not the post-GC one ROADMAP.md:56-61 describes — and reproduces on x86-64 SBCL with a 256 KiB nursery (panel/gc-mutation.lisp, 4/5 runs). `%stub-crate` (crate.lisp:384-390) replaces symbol-functions only while `%run-crate-dump-hooks` (:417) tests the slots it leaves populated — a restore without the artifact followed by a dump jumps into the dead library (panel/dump-phase1.lisp: SBCL `CORRUPTION WARNING … Memory fault`). crate.lisp:20 declares `crate-generation` with `:accessor`; `(setf (rulisp:crate-generation c) 0)` then `reload-crate` lets a gen-1 handle into the new library (panel/gen-setf-hole.lisp). codegen.lisp:464-468 passes the literal `0` as the Option placeholder and SBCL/CCL reject a fixnum for `:double` (panel/probe1.lisp, probe2.lisp). crate.lisp:151 copies into the cache before :152 verifies, and a failed load of a renamed artifact left an unsweepable 5.9 MB copy (panel/probe1.lisp). build.lisp:41-56 signals `build-error` only on a non-zero exit and `(find-cargo)` runs at :98 outside the restart-case at :99-105; with `RULISP_CARGO=/nonexistent/cargo` SBCL signals SIMPLE-ERROR, CCL `build-error` with empty stderr, ECL `build-error` with "exec: No such file or directory" (panel/missing-cargo.lisp). stability.md:124-125 says `rulisp/test` "is excluded from what a dist user loads", yet it lives in the same lisp/rulisp.asd and Ultralisp's systems.txt lists it — while every system in every .asd of `git archive HEAD` loads with cargo off PATH (panel/dist-dryrun.lisp). Ultralisp serves the 0.3.0 loader three days after the v0.5.0 tag (dist 20260902080501 → onlyarche-rulisp-20260815001256, `:version "0.3.0"`; project page: source 2525 last seen ddba5be, queue position 651; source 2526 "No checks yet"). The tracker holds one closed issue and no PR; crates.io has 0.5.0 for all three crates.

## Items, in order

### 1. A failed reload on image restore leaves dead-session pointers on the crate object; the next dump jumps into an unmapped library (S)

**Demand.** Reproduced (panel/dump-phase1.lisp, SBCL 2.1.11): load wordbag from a scratch path, `uiop:dump-image`, delete the artifact, run the image. `%restore-all-crates` (crate.lisp:392-408) warns "could not reload crate wordbag" and `%stub-crate` (:384-390) replaces only the symbol-functions; `crate-lib-handle` and `crate-on-dump-ptr` still hold the dead image's SAPs. `%run-crate-dump-hooks` — what the next `uiop:dump-image` runs through the hook at :431 — passes its guard at :417 `(and crate (crate-on-dump-ptr crate) (crate-lib-handle crate))` and calls the dead pointer: SBCL prints `CORRUPTION WARNING … Memory fault at 0x746dc92fc9b0 … The integrity of this image is possibly compromised` and the `serious-condition` handler at :427 warns "dump hook of wordbag signaled Unhandled memory fault …; the dump proceeds". BOUNDARY §10 (:201-207) promises "signaling, never dereferencing"; §12 pins that for cells (:379, handle.lisp:38-44) and captured wrappers (:380, codegen.lisp:559-562), not for the crate object's own slots. Realistic path: a Deploy-style image restored where the artifact is absent (cleared cache, a build runner), then re-saved. SECURITY.md's in-scope class; a 1.0 candidate cannot carry it.
**Scope.** `%stub-crate` also sets `lib-handle`, `on-dump-ptr`, `last-error-ptr`, `dealloc-ptr`, `handle-frees` and `cache-path` to nil (the :417 guard then skips the crate); `describe-object` (:333) prints "stubbed: reload failed on restore" when `lib-handle` is nil. New `tests/suite/v06.lisp` (`def-suite* :rulisp-v06`, registered in lisp/rulisp.asd:23-31 and tests/run-m4.lisp:23-31 — the file every later item adds to) with `v06.restore-failure-leaves-no-live-foreign-pointer`, cloned from `m7.dump-restore` (m1.lisp:274-): copy `target/debug/libwordbag.so` to a per-process temp name, `load-crate … :crate "wordbag"` in the subprocess script, dump an executable whose `*image-entry-point*` calls `(rulisp::%run-crate-dump-hooks)` and prints `RESTORE-STUB-OK`, delete the copy, run it capturing stdout+stderr, assert exit 0, the marker, and neither `Memory fault` nor `CORRUPTION WARNING` in the log; `#-(or sbcl ccl)` named pass with cause (fetch.lisp:504-505 style). BOUNDARY §10 gains one sentence ("a crate whose reload failed on restore is inert until `reload-crate` succeeds: its declared hook is not run") and a §12 row citing the test; CHANGELOG Fixed. Surface: none.
**Acceptance.** Red on the unmodified loader — the subprocess log carries exactly today's `CORRUPTION WARNING … Memory fault` — and green after; `make test-m4` on SBCL and `make test-ccl` on CCL; `grep -c 'CORRUPTION WARNING'` over the captured log = 0.

### 2. `m4.gc-finalization` holds its handles until it counts them — the arm "not understood" failure is a nursery-timing flake, reproducible on x86-64 (S)

**Demand.** ROADMAP.md:56-61 says the aarch64 failure is "Not understood yet — it blocks promotion". The failing assertion (job 100926334693: `(LIVE-BAGS) evaluated to 893 which is not = to 1000`) is the FIRST one, `(is (= (+ before 1000) (live-bags)))` at m1.lisp:142, which asserts that no collection ran during 1000 constructor calls whose handles are dropped as they are made (:139-141). That is not a boundary claim: SBCL runs finalizers on `sb-impl::*finalizer-thread*` and a nursery GC inside the loop legitimately finalizes some before the count is read. ROADMAP's wording ("893 of 1000 still live after 100 full GCs") describes the post-GC assertion at :149, which prints "not <= to" and has never failed on any host. Reproduced on x86-64 SBCL 2.1.11 two ways: `(setf (sb-ext:bytes-consed-between-gcs) (* 256 1024))` fails 4 of 5 runs with the identical shape (panel/gc-mutation.lisp: "787 … which is not = to 1000"); `(tg:gc :full t)` every 250 constructors gives 250 after the loop — first assertion fails, second passes (panel/gc-in-loop.lisp). The loop conses ~434 KB against a 53 MB nursery (panel/consing.lisp): whether a GC lands inside it depends on where the nursery stands, on any host. 6 of 7 arm runs are green and the same test passed on the next run.
**Scope.** m1.lisp:136-149: the compiled lambda keeps the 1000 handles in a vector, the test asserts `(= (+ before 1000) (live-bags))` while they are live, drops the vector, then runs the existing bounded GC loop and `<=`-with-slack assertion unchanged (keep `(compile nil …)` so no dead frame pins a handle; CCL slack at :145 unchanged). ROADMAP.md:56-61 rewritten to the verified account (first assertion, GC timing, reproduced on x86-64, never an arm difference); the promotion streak restarts at the fix. Promotion itself is not plan work: the one-commit procedure at ci.yml:205-211 / stability.md:68-74 runs when the post-fix streak is documented (ECL's precedent: 24 consecutive runs over 35 days, ROADMAP.md:144-147). ROADMAP records that commit's checklist so nothing is missed: drop `continue-on-error`, rename "(required)", blobs.yml leg `ubuntu-24.04-arm / linux-arm64 / so / lib`, blobs.yml:101 `-eq 12` → 16, releasing.md:39 "twelve" → sixteen, README.md:120, stability.md:63-64, usage.md:46, claims.md:217 and :273, a `load-blob-crate` step on the arm job (Case A exercised on arm). Surface: none.
**Acceptance.** Both mutation runs pass after the change (5/5 with the 256 KiB nursery; both assertions with the in-loop GC) and fail before it; `make test-m4` green on all hosts; `grep -n 'Not understood\|after 100 full GCs' ROADMAP.md` empty; the following arm runs green with nothing else changed. A second, *different* arm failure is a real finding: the job stays best-effort and ROADMAP says how many runs it has.

### 3. Cross-version gates pinned to v0.5.0 — old loader/new crate, old crate/new loader, old suite/new loader — plus an `abi_version()=2` fixture (M)

**Demand.** stability.md:15-16 define manifest and ABI breaks by what a *released* macro emitted and what an *older* loader accepts; criterion 3 (:108-109) has no mechanism. ci.yml:99 `gh release download --pattern 'libwordbag-linux-x86_64.so'` names no tag: today it loads a 0.5.0-built crate on the tree loader, and it becomes a same-version test the day 0.6.0 ships. Nothing loads a tree-built crate with the 0.5.0 loader. Nothing runs the 0.5.0 suite against the tree's loader — the enumerated set of "calls that worked", which is stability.md:14's literal break definition. The ABI surface's only guard, the refusal at crate.lisp:180-182, is untested (BOUNDARY.md:389: "no suite test simulates a mismatch"). Skew is the downstream norm, not the edge case: Ultralisp serves the 0.3.0 loader against crates.io 0.5.0 macros. Mechanisms verified today: panel/old-loader.lisp printed `OLD-LOADER-OK Hello, 0.5.0 loader!; warnings: NIL` against `git archive v0.5.0 lisp` and the tree-built libwordbag.so; panel/compat-backward.lisp loaded the v0.4.0 (31 fns, no `:rulisp-version`) and v0.5.0 wordbag and fetch assets on the tree loader with zero warnings (greet, handle add/len/free, always-panic → rust-panic, docstrings, fetch on-dump); panel/compat-forward.lisp loaded the tree's wordbag and fetch on the archived 0.5.0, 0.4.0 and 0.3.0 loaders with zero warnings. The gates pass today and go red on the first load-bearing key, ABI drift or renamed wire symbol.
**Scope.** ci.yml, job `SBCL / Linux x86-64 (required)`: (a) :99 becomes `gh release download v0.5.0 --pattern …` with a comment that the pin is the previous release; the checkout at :54 gains `fetch-depth: 0` (tags). (b) New step after `make test-m4` (:65), "load the tree's wordbag with the v0.5.0 loader": `git archive v0.5.0 lisp | tar -x -C /tmp/loader-0.5.0`, a fresh `env -i` sbcl with that `lisp/` pushed onto `asdf:*central-registry*`, `(ql:quickload :rulisp)`, `load-crate` of `target/debug/libwordbag.so :crate "wordbag"` under a `handler-bind` that errors on any warning other than `rulisp-version-skew` (looked up with `find-symbol`, since 0.5.0 does not export it), `grep -q 'OLD-LOADER-OK Hello, 0.5.0 loader!'`. (c) New step "the 0.5.0 suite against this loader": `git worktree add /tmp/v050 v0.5.0`, copy the tree's `lisp/src/*.lisp` over `/tmp/v050/lisp/src/` — the worktree keeps its own `rulisp.asd` (whose file list this plan does not change) and its own `tests/suite/` (the 0.5.0 calls) — then `make -C /tmp/v050 test-m4`. Classification rule, written into the step comment and stability.md §1: a failure in a test that reaches `rulisp::` internals (v04.lisp:119 `gethash` on `*crates*`, v05.lisp:103) is recorded and that test skipped by name; a failure through an exported symbol is a break and does not land in 0.6. (d) `tests/abi-fixture/`: a standalone `[workspace]` cdylib like tools/audit-fixture, exporting `abifix_rulisp_abi_version() -> u32 { 2 }`; `v06.abi-mismatch-refused` in v06.lisp builds it with cargo (as `ensure-crate` does) and expects `abi-mismatch-error` with `:expected 1 :actual 2` — `%open-and-verify` (crate.lisp:173-182) reads `abi_version` before the manifest, so the fixture needs nothing else. (e) docs/releasing.md step 9 gains "move the three v0.5.0 pins in ci.yml (download tag, archive tag, worktree tag) to the release just made" — step 9, not tools/check-versions.sh, because at step 4 the new release does not exist. (f) BOUNDARY.md:389 and :393 and stability.md §8 criterion 3 name the steps and the test as their evidence. Surface: none (checked, not changed).
**Acceptance.** `grep -n 'gh release download v0.5.0' .github/workflows/ci.yml` exits 1 today and 0 after; the job log shows `OLD-LOADER-OK` and the 0.5.0 suite's `Fail: 0`; `v06.abi-mismatch-refused` green on all five hosts (fails today: no fixture). Failing-first for the forward direction: a scratch renderer emitting `:schema 2` (crates/rulisp-runtime/src/meta.rs:281) turns step (b) red — the 0.5.0 loader refuses with `manifest-error` (manifest.lisp:129) — where `fx.golden-manifest` would only ask for a golden update. Mutation for (c): item 9's tightening makes no 0.5.0 test red (the tree only *reads* `crate-generation`: m1.lisp:237,240; m2.lisp:106,115), while changing `free`'s return value does. If step (c) needs more than three by-name skips on HEAD, the classification rule is wrong, not the tests.

### 4. Close the last BOUNDARY §12 GAP: a PE branch in `tools/rulisp-audit.sh`, self-tested on Windows, every release asset re-audited (M)

**Demand.** BOUNDARY.md:363 is the only GAP row (`grep -c '^|.*GAP' BOUNDARY.md` = 1; :405's "All five GAP rows were re-hunted" is stale) and criterion 4 (stability.md:110-111) says "no gap". tools/rulisp-audit.sh:31-35 dispatches on `uname -s` and prints SKIP with exit 0 for anything but Linux/Darwin; tools/rulisp-audit-selftest.sh:8 SKIPs the same way and :24 globs only `.so`/`.dylib`; so blobs.yml:66 attaches four `.dll` assets per release unswept (blobs.yml:6-7 and releasing.md:37 say so). Negative control done on the real v0.5.0 assets (panel/pe-imports.py): `wordbag-windows-x86_64.dll` imports 70 symbols from 6 DLLs, `fetch-windows-x86_64.dll` 115 from 13, and neither imports `signal`, `raise`, `SetConsoleCtrlHandler`, `SetUnhandledExceptionFilter`, `AddVectoredExceptionHandler`, `AddVectoredContinueHandler`, `RtlAddVectoredExceptionHandler` or `RaiseException`; the audit-fixture's `signal()` arrives via ucrt (`api-ms-win-crt-runtime-l1-1-0.dll`), so the branch has a known clean pass and a known reject.
**Scope.** rulisp-audit.sh dispatches on the artifact's magic (`MZ` → PE, `\x7fELF`, Mach-O) instead of `uname`, so a `.dll` is auditable from Linux too; the PE branch lists imports with `llvm-readobj --coff-imports` from rustup's `llvm-tools` component (`$(rustc --print sysroot)/lib/rustlib/*/bin/llvm-readobj` — no runner-image dependency; fallback: `llvm-readobj` on PATH) and matches the Windows set above (ucrt `signal`/`raise` are §7's direct analogues; `SetConsoleCtrlHandler` is Windows' SIGINT ownership, mirroring the :364 row — one sentence added to §7); the `--match` self-test learns the `Symbol:` line shape so a format drift fails loudly instead of going inert. The selftest drops its `Linux|Darwin` guard and finds `audit_fixture.dll`. ci.yml `SBCL / Windows x86-64 (required)` (:148-176): `rustup component add llvm-tools`, then `make audit`. blobs.yml: the windows leg installs `llvm-tools` so :66 audits for real, and the release job re-audits all twelve downloaded blobs on ubuntu so the gate covers the exact bytes attached. BOUNDARY.md:363 → `test` with the job names; :405 corrected; blobs.yml:6-7 and releasing.md:37 lose the SKIP clause; CHANGELOG 0.5.0's sentence stays as history. Surface: none.
**Acceptance.** `sh tools/rulisp-audit.sh panel/blobs-0.5.0/wordbag-windows-x86_64.dll` on Linux prints `audit ok` (today: SKIP, exit 0 — the check is inert); the Windows job log shows the selftest rejecting the fixture DLL and `audit ok` for the four debug DLLs; a `workflow_dispatch` of blobs.yml for `v0.5.0` logs four `audit ok` on the windows leg and twelve in the release job; `grep -c '^|.*GAP' BOUNDARY.md` prints 0. If a future Rust std imports one of the kernel32 installers, record it in §7's Windows text rather than widen the pattern silently.

### 5. "Required" becomes a release gate, not a job-name convention (S)

**Demand.** stability.md:61: "A host is supported exactly when it is a **required** CI job"; releasing.md:23-24: "Never tag on a red run". Nothing enforces either: GitHub branch protection is absent (`gh api repos/onlyarche/rulisp/branches/main/protection` → 404 "Branch not protected"), rulesets are `[]`, and blobs.yml's release job (:96-104) checks only `--verify-tag` and the asset count — a tag on a red commit still produces a release with twelve audited assets. This is the documented-claim-enforced-by-nothing pattern criterion 4 exists to remove, and every gate this cycle adds is advisory until it is closed.
**Scope.** blobs.yml release job, before `gh release create`: resolve the tagged commit (`git rev-parse "$TAG^{commit}"` — `GITHUB_SHA` may name an annotated tag object), find its ci.yml run (`gh run list --workflow ci.yml --commit <sha> --json databaseId,conclusion,status`), list its jobs (`gh api repos/$GITHUB_REPOSITORY/actions/runs/<id>/jobs`), and fail naming the job unless every job whose name ends in `(required)` plus `MSRV (cargo check on the declared rust-version)` (ci.yml:39) concluded `success` — an in-progress run counts as a failure (step 4 says wait; the tag comes after). A `workflow_dispatch` boolean input `skip-ci-gate` for a documented re-run, echoed loudly in the log. releasing.md step 4 cites the step; stability.md §5 gains the sentence; docs/claims.md's "required" rows cite it. Maintainer action outside the tree, recorded in the commit message: a repository ruleset on `main` requiring the same named checks, so `gh api repos/onlyarche/rulisp/rulesets` is no longer `[]`. Surface: none.
**Acceptance.** A `workflow_dispatch` of blobs.yml for a scratch tag on a commit whose CI has a red required job stops before the release step with the job name in the log (today it releases); a re-dispatch for `v0.5.0` still succeeds (run 33842108198's required jobs are green — its arm failure is best-effort and must not count); v0.6.0's release log shows the gate passing.

### 6. Rust API gate: cargo-semver-checks against the crates.io 0.5.0 baseline, with `--release-type minor` (S)

**Demand.** stability.md:13 defines a Rust-API break and criterion 3 demands none in 0.6; nothing checks it — neither tool is installed (rc=101) and the `rust` job (ci.yml:18-32) runs tests, the oracle build, `cargo doc -D warnings`, the doctest and `make audit`. Not hypothetical: `git diff v0.4.0 v0.5.0 -- crates/rulisp-runtime/src/meta.rs` added `pub doc: Option<&'static str>` to `FnMeta` and `HandleMeta`, reachable as `rulisp::runtime::FnMeta` through `pub use rulisp_runtime as runtime` (lib.rs:53, no `#[doc(hidden)]`); a pub field added to a constructible struct is *major* to the tool while stability.md:18-21 calls the runtime helpers "not a surface". The tool and the contract disagree today and no one would notice.
**Scope.** ci.yml `rust` job after `make audit` (:32): `obi1kenobi/cargo-semver-checks-action@v2` (prebuilt binary; `cargo install` takes minutes) over `rulisp`, `rulisp-macros`, `rulisp-runtime` with `--release-type minor`. The flag is load-bearing: under Cargo's 0.x rule a 0.5→0.6 bump licenses breaking changes, and the tree stays at 0.5.0 until releasing.md step 1, so only `--release-type minor` asks criterion 3's question ("does this tree break a 0.5.0 consumer"). Baseline defaults to the latest crates.io release (0.5.0 now; 0.6.0 after step 5, which precedes the tag). **Decision (three proposals wanted the hide; this resolves it):** `rulisp::runtime` stays public and *checked* in 0.6 — hiding it against the 0.5.0 baseline fires the tool's per-item `*_now_doc_hidden`/`*_missing` findings for some thirty runtime items, and a cycle-long allow-list for that family would also mask hiding a real item such as `Error`; generated code names `::rulisp::runtime::*` explicitly (57 sites, macros lib.rs:492-1421), so neither the check nor the later hide touches glue crates. One sentence added at stability.md:18-21: "until 1.0 hides the path, the semver job checks it too; hiding it is the 1.0 major's first commit". §1's Rust row names its gates: this job, `fx.golden-manifest` (m2.lisp:125-146), `manifest_matches_golden`, `make test-m2`. Every lint stays at deny. Surface: none.
**Acceptance.** Green on HEAD (nothing has moved since 0.5.0). Failing-first, locally: `cargo install cargo-semver-checks --locked`; on a scratch commit rename `Error::msg` → `Error::message` (lib.rs:71) and run `cargo semver-checks -p rulisp --release-type minor` — it reports `inherent_method_missing` and exits non-zero; restore, it passes. If the HEAD run itself flags something in 0.5.0-vs-0.5.0, that is the cycle's first finding to record, never a flag to soften. Risk: the baseline is fetched from crates.io (the same exposure as the quicklisp installer steps).

### 7. Lisp API gate: `tests/golden/lisp-api.sexp`, checked on every host (S)

**Demand.** stability.md:14 freezes "the 32 symbols `lisp/src/package.lisp` exports"; the number is prose (also CHANGELOG.md:64) compared to nothing. The manifest surface has `fx.golden-manifest` pinning bytes; the Lisp surface has nothing, and items 8-11 change it, so a co-updated golden is the discipline the cycle needs before the first of them. package.lisp has not changed since v0.2, so the gate encodes existing stability — exactly what criterion 3 needs. Verified (panel/lisp-api-snapshot.lisp): SBCL 2.1.11, CCL 1.13 and ECL 21.2.1 enumerate the same 32 with identical kinds (11 conditions, 3 classes, 5 functions, 12 reader generic functions, 1 restart name) and identical direct superclasses; only reader-GF lambda lists (`(SB-PCL::OBJECT)` / `(CLOS::SELF)`) and CCL's keyword rendering (`&KEY :PACKAGE`) differ.
**Scope.** `tests/golden/lisp-api.sexp`: one sorted entry per external symbol — name, kind (`:function :generic-function :macro :class :condition :restart-name`), direct superclasses for classes and conditions (`#+sbcl sb-mop #+ccl ccl #+ecl clos`, no new dependency), the lambda list for plain functions printed with `*package*` bound to RULISP and defaults kept (e.g. `use-crate (crate-dir &key (profile :dev) package features)`). `v06.exported-api-golden` in v06.lisp: symbol set, kinds and superclasses exact on every host; lambda lists exact on SBCL via `sb-introspect:function-lambda-list` (a standard contrib in the apt/brew/choco builds CI installs), names-and-&-markers on CCL (`ccl:arglist`) and ECL (`ext:function-lambda-list`) — non-vacuous everywhere, per m7's rule. A regeneration form in the test file prints the golden from SBCL. Rule written into stability.md §1: an additive change regenerates the golden in the same commit with a CHANGELOG line; a removal or signature change fails the test and does not land in 0.6. stability.md:14 points at the golden file instead of the number. Surface: none.
**Acceptance.** Green on all five hosts with the 32-entry golden. Failing-first: write the golden with the `#:retry-build` entry missing and run `make test-m4` — the test fails naming that entry; then commit the true golden. From this commit on every package.lisp change co-updates the golden, as `fx.golden-manifest` does for the manifest bytes; item 8 is the first.

### 8. Export what the docs already name, let every export describe itself, fix the stale Rust doc (S)

**Demand.** `rulisp-version-skew` is what CHANGELOG.md:57, BOUNDARY.md:268 and stability.md §7 tell a user they will see and may muffle, yet `(handler-bind ((rulisp:rulisp-version-skew #'muffle-warning)) …)` does not read — the symbol is :INTERNAL and the suite itself writes `rulisp::rulisp-version-skew` (v05.lisp:103). The condition readers are half-exported: conditions.lisp defines eleven with no export — `rust-panic-function-name` (:16), `invalid-argument-message`/`-function-name` (:22-23), `invalid-handle-function-name` (:30), `crate-not-loaded-name`/`-message` (:48-49), `build-error-command` (:57), `manifest-error-message` (:64), `abi-mismatch-expected`/`-actual`/`-message` (:69-71) — while their siblings are (package.lisp:33-39); v04.lisp:166 reaches `rulisp::manifest-error-message`, and the fetch veneer reads `rulisp:rust-error-message` (examples/fetch/http.lisp:54-56), so consumers do read slots. After v0.5 item 6 made every *generated* function and class self-describing, the loader's own API is mute: `(documentation 'rulisp:stale-handle-error 'type)` is NIL for all 11 exported condition classes and for `crate` and `callback-token` (`grep -n :documentation lisp/src` → handle.lisp:28, conditions.lisp:89 — the unexported skew — and the generated classes at crate.lisp:314), and the exported readers have no function documentation. crates/rulisp/src/lib.rs:70 still says `Error::msg` signals `<crate>:rust-error`, the symbol CHANGELOG.md:72-74 says does not exist — fixed on the Lisp side in 0.5.0, missed in the Rust doc.
**Scope.** package.lisp: export `rulisp-version-skew`, its three readers (conditions.lisp:79-81) and the eleven readers (32 → 47, all additive). `(:documentation …)` on every condition class stating when it is signaled and which restart is offered (`use-value` on `rust-error` per `fx.use-value-restart`, m2.lisp:84; `retry-build` on `build-error`), on `crate` and `callback-token`; reader docs through the existing ECL-aware `%set-function-doc` (crate.lisp:326-331) in one `dolist`. lib.rs:70 → `rulisp:rust-error`. `v06.exports-are-documented`: every fbound external symbol has a non-empty `(documentation sym 'function)`, every class and condition a non-empty `(documentation class t)`. The golden regenerated in the same commit (item 7's rule; 47 rows); v05.lisp:103 and v04.lisp:166 lose their `::`; stability.md:14 and CHANGELOG Added list the additions. Surface: Lisp API additive (15 exports, documentation strings); Rust API doc text only. The docstring half is the part to trim if the cycle runs long — the exports stay.
**Acceptance.** `v06.exports-are-documented` is red today (13 classes and 10 readers return NIL) and green on SBCL, CCL and ECL after; `(find-symbol "RULISP-VERSION-SKEW" :rulisp)` returns :EXTERNAL; `grep -n '<crate>:rust-error' crates/rulisp/src/lib.rs` exits 1; the golden has 47 entries; items 6 and 7 green — the first additive change of the cycle passing through the gates, on record.

### 9. `crate-generation` becomes a reader: the exported `setf` defeats the generation gate (S)

**Demand.** Reproduced (panel/gen-setf-hole.lisp, SBCL 2.1.11, examples/wordbag): `(fboundp '(setf rulisp:crate-generation))` is a STANDARD-GENERIC-FUNCTION because crate.lisp:20 declares the slot with `:accessor`. Load wordbag, make a handle, `reload-crate` — the gen-1 handle is correctly refused with `stale-handle-error`; then `(setf (rulisp:crate-generation c) 0)` and `reload-crate` again: the crate is generation 1 again and the old gen-1 handle passes `cell-begin-call` (handle.lisp:42 compares birth-generation numbers only) into the *new* library's `word-bag-len`, which returned 0 instead of signaling — a Box from library copy A dereferenced by copy B's shim, type confusion once the Rust struct changes between builds. package.lisp:16-18 lists the symbol under "crate readers"; the only writer in the tree is crate.lisp:233; no doc, test or example setfs it (the suite only reads it: m1.lisp:237,240; m2.lisp:106,115). BOUNDARY §5's "accepted only by wrappers of that same birth generation" is defeated through an exported symbol — issue #1's class — and 1.x would freeze it.
**Scope.** crate.lisp:20 → `(generation :initform 0 :reader crate-generation :accessor %crate-generation)`; :233 → `(setf (%crate-generation crate) gen …)`. `v06.crate-generation-is-read-only`: `(null (fboundp '(setf rulisp:crate-generation)))` and no external symbol of RULISP names a writer. Golden entry unchanged (the reader stays `:generic-function`). **Classification, decided here:** a soundness fix under stability.md §4 ("fix forward and say so in CHANGELOG.md"), not a §3 deprecation — the writer is undocumented, its only effect is to defeat a safety gate, and a style-warning stage would keep the hole open one more minor. CHANGELOG 0.6 "Changed" names it with the reproduction as the cycle's one non-additive Lisp change; item 3's step (c) proves no 0.5.0 call depended on it, so the criterion-3 clock is not reset. BOUNDARY §12's §5 birth-generation row gains "the generation counter is not writable through the exported API". Surface: Lisp API — an undocumented `setf` stops working; no documented call, return type or condition changes.
**Acceptance.** `(fboundp '(setf rulisp:crate-generation))` → NIL on SBCL, CCL and ECL; the probe now stops at the `setf` with an undefined-function error instead of printing 0 for the old handle; `m6.reload` (m1.lisp:237-240) and `fx.partial-generation-ban` (m2.lisp:106-115) stay green; item 3's old-suite step stays `Fail: 0`.

### 10. A cargo that cannot run is `build-error`, with a `retry-build` that re-resolves cargo, on every host (S)

**Demand.** `use-crate`'s docstring (build.lisp:88-92) promises "Signals BUILD-ERROR with cargo's stderr on failure, offering a RETRY-BUILD restart". `%run-cargo-build` (:41-56) signals `build-error` only on a non-zero exit code; a failed exec never reaches that branch. Reproduced today with `RULISP_CARGO=/nonexistent/cargo` (panel/missing-cargo.lisp): SBCL 2.1.11 signals SIMPLE-ERROR "Couldn't execute …" — `(typep e 'rulisp:rulisp-error)` NIL, no restart; CCL 1.13 signals `build-error` with an *empty* stderr, so the report reads "cargo build failed (…):" and nothing; ECL 21.2.1 signals `build-error` with "exec: No such file or directory". Three required hosts, three behaviours, for the newcomer's most common failure — the inconsistency stability.md:14's break definition is about. The restart is inert for this case: `(find-cargo)` runs once at :98, outside the restart-case at :99-105, so setting `RULISP_CARGO` from the debugger and invoking `retry-build` re-runs the old path. installation.md:147-151 and :164 document the SBCL behaviour as *the* behaviour; claims.md:155 cites it.
**Scope.** `%run-cargo-build`: `handler-case` around `uiop:run-program` (:50) re-signaling `build-error :command … :stderr` = the host's message; when the exit status is non-zero and stderr is empty (the CCL spawn shape), stderr becomes "exit status ~A and no output — is ~A executable?". `(find-cargo)` moves inside the restart-case loop so `retry-build` re-resolves. An internal, unexported `rulisp::*cargo*` override consulted first by `find-cargo`, so the test names a nonexistent executable without touching the process environment (the surface is unchanged — item 7's golden proves it — and the test runs on Windows too). `v06.missing-cargo-is-a-build-error`: `handler-bind` asserting the class, a non-empty `build-error-stderr` and `(find-restart 'rulisp:retry-build)` non-nil; a second leg points `*cargo*` at the real cargo from the handler and invokes `retry-build`, asserting `use-crate` returns a crate. installation.md:147-151 and the :164 troubleshooting row rewritten (symptom `rulisp:build-error` carrying the host message; fix: install rustup or set `RULISP_CARGO`, then `retry-build`); claims.md:155 re-cited to the test; CHANGELOG Fixed with the migration line ("code that handled the host's error class around `use-crate` handles `rulisp:build-error`"). Surface: Lisp API, additive — a documented condition class is now signaled where a host error was; nothing that signaled a rulisp condition stops.
**Acceptance.** The test is red on SBCL today (SIMPLE-ERROR escapes the handler) and on CCL (empty stderr), green on all five hosts after; `make test-m4` green; `grep -n "host's own\|Couldn't execute" docs/installation.md` empty; items 6 and 7 green. The class and the restart are asserted, never the text (Windows SBCL's `run-program` message differs).

### 11. `Option<f32>`/`Option<f64>` NIL is a host TYPE-ERROR on SBCL and CCL (S)

**Demand.** README.md:149 and quickstart.md:177 promise "NIL ↔ None" for `Option<T>`. Reproduced with a scratch glue crate built against the tree (panel/optf: `opt_f64(x: Option<f64>)`, `opt_f32`; panel/probe2.lisp on three hosts): SBCL 2.1.11 and CCL 1.13 signal a host TYPE-ERROR for `(optf:opt-f64 nil)` and `(optf:opt-f32 nil)`; ECL coerces. Cause: codegen.lisp:464-468 passes the literal integer `0` as the value placeholder — `(if (null ,sym) 0 ,sym)` — and SBCL/CCL's alien deport rejects a fixnum for `:double`/`:float` (`(cffi:foreign-funcall "fabs" :double 0 :double)` → SIMPLE-TYPE-ERROR, panel/probe1.lisp). Invisible to the suite because no wordbag export takes an `Option<scalar>` parameter (`find` *returns* `Option<u64>`, examples/wordbag/src/lib.rs:98; `greet_opt` takes `Option<&str>`, :103 — the string branch at codegen.lisp:460-463).
**Scope.** codegen.lisp:464-468: a typed placeholder computed at codegen time (`0d0`/`0f0` for floats, `0` for integers). Add `opt_scale(k: Option<f64>) -> f64` to examples/wordbag with the oracle (tests/m1-handwritten/src/lib.rs) and `tests/golden/wordbag.manifest.sexp` co-updated in one commit per releasing.md:16-22. quickstart.md:175-179 gains one sentence stating the plain-scalar policy *as it is*: a wrong-typed scalar (an integer for an `f64` on SBCL/CCL, an out-of-range integer) is the host's type-error — host-checked — while `:vec` elements and `:bytes` signal `rulisp:invalid-argument` (ffi.lisp); documented, not changed (see not-in). claims.md rows for README.md:149 and quickstart.md:177 re-cited to the new test. Surface: Lisp API additive (a call that signaled a host error now returns None); manifest fixture only, schema unchanged.
**Acceptance.** `v06.option-float-nil-is-none` — `(wordbag:opt-scale nil)` → the None value, `(wordbag:opt-scale 2.5d0)` round-trips — green on SBCL, CCL and ECL; today it fails on SBCL and CCL with a host type-error. `fx.golden-manifest` and `cargo test -p wordbag` byte-identical after the co-update.

### 12. Renamed artifacts: a misleading refusal, and a cache copy leaked per failed load (S)

**Demand.** docs/distribution.md:20-31 tells library authors to commit `static/libmycrate-lin-amd64.so` and "load with `rulisp:load-crate`". Reproduced (panel/probe1.lisp with panel/libwordbag-lin-amd64.so): `(rulisp:load-crate ".../libwordbag-lin-amd64.so")` signals `abi-mismatch-error` "wordbag_lin_amd64 is not a rulisp crate (no wordbag_lin_amd64_rulisp_abi_version); expected 1, got NIL" — `derive-crate-name` (crate.lisp:102-106) guesses the export prefix from the file name (:144-145) and the message (:176-179) never says `:crate` is the fix; with `:crate "wordbag"` the same file loads. And `uiop:copy-file` (:151) runs before `%open-and-verify` (:152): the failed load left `wordbag_lin_amd64-p<pid>-c1-<n>.so` (5.9 MB) in the cache, which `%sweep-crate-cache` (:359-378, prefix `<canonical>-`) can never match — one artifact-sized file leaked forever per failed attempt, against installation.md:157-158 "Older copies are swept automatically" (claims.md:152). claims.md:258 cites "load-crate accepts any artifact path" as the enforcement for distribution.md:31; the row is wrong as it stands.
**Scope.** `%load-crate-locked` (:139): `unwind-protect` around `%open-and-verify` + `%commit-generation` that `(ignore-errors (delete-file copy))` when the load does not commit (POSIX: safe even after a successful `dlopen`, the mapping keeps the inode; Windows: best-effort and ignored, as the sweep already is at :365-366); the :178-179 message gains "; if the artifact was renamed, pass :crate \"<name>\"". distribution.md:31 shows `(rulisp:load-crate path :crate "mycrate")`; claims.md:258 re-cited. `v06.renamed-artifact-names-the-fix` (copy the wordbag artifact under a suffixed name in `uiop:temporary-directory`; assert the `abi-mismatch-error` message contains ":crate") and `v06.failed-load-leaves-no-cache-copy` (count `(uiop:directory-files (rulisp::cache-directory))` before and after the failing load; `#-windows`). Surface: none (message text of an existing condition; internal cleanup; docs).
**Acceptance.** Both tests in `make test-m4` on every host; today the first fails (message lacks ":crate") and the second fails (one extra file); `grep -n ':crate' docs/distribution.md` non-empty.

### 13. Quicklisp dist dry run in CI; stability §9(c) rewritten to what the tarball does (S)

**Demand.** stability.md:124-125 says `rulisp/test` "is excluded from what a dist user loads"; it is defined in the same lisp/rulisp.asd (:19-31) and Ultralisp's systems.txt advertises `onlyarche-rulisp rulisp rulisp/test`. A dist builder loads every system of every `.asd` in the tarball, and the tree has two: lisp/rulisp.asd and tests/ecl-program/rulisp-ecl-smoke.asd (added in v0.4; absent from the 0.3.0-era archive Ultralisp still serves, so it has never been through any builder). ci.yml:71-81 proves only `:rulisp`. Verified today (panel/dist-dryrun.lisp): `git archive HEAD` extracted, every `.asd` enumerated, each system quickloaded in a fresh SBCL under `env -i PATH=/bin:/usr/bin` → DIST-OK for `rulisp`, `rulisp/test`, `rulisp-ecl-smoke` — because `ensure-crate` (m1.lisp:16-17) is lazy and the top-level `(rulisp:use-crate ~S)` hits in the suites (m1.lisp:304, fetch.lisp:526) are inside format strings for subprocess scripts. Exit criterion 5 rests on §9, one bullet of which is false as written while the true fact is proven by no CI step.
**Scope.** `tests/dist-dryrun.lisp` (enumerate `*.asd` under an archive, read each `defsystem` name — both files hold only `defsystem` forms — `ql:quickload` each, print `DRYRUN-OK <system>`), `make dist-dryrun`, and a step "Quicklisp dist dry run (every system in every .asd, no cargo)" in the SBCL/Linux job extending :71-81: `git archive HEAD | tar -x -C /tmp/dist`, the runner under `env -i HOME="$HOME" PATH=/bin:/usr/bin /usr/bin/sbcl`, grep for exactly three `DRYRUN-OK` lines. stability.md:124-125 → "every system in every .asd of the tarball — `rulisp`, `rulisp/test`, `rulisp-ecl-smoke` — loads without cargo (the CI step named); *running* `rulisp/test` needs cargo (docs/installation.md)". claims.md:138 and :315 gain the step as citation. Surface: none.
**Acceptance.** `grep -n 'dist dry run' .github/workflows/ci.yml` exits 1 today; after, the step log shows three `DRYRUN-OK` lines and `make dist-dryrun` passes locally; mutation: a top-level `(rulisp:use-crate …)` added to any tests/suite/*.lisp turns the step red; `grep -n 'excluded from what a dist user loads' docs/stability.md` is empty.

### 14. Close the cycle: `tools/check-1.0.sh`, the §12 citation refresh, and the stale prose (S)

**Demand.** stability.md:98-112 says "Exit criteria, all checkable" and nothing checks any of them. Prose has drifted: ROADMAP.md:278-279 lists the Deploy example as "still open" (shipped v0.3, :217-219); :291-292 says m7 "passes vacuously off SBCL" (fixed v0.4); :308-309 says wasm host functions are "still pending" while :259 says the demand case closed; ROADMAP.md:75 and CHANGELOG 0.5.0 count "371 claims" while docs/claims.md's own footer says 347 rows. releasing.md:44-48 step 8 promises Ultralisp "after about an hour" while the live dist serves 0.3.0 three days after the tag and installation.md:97-101 sends users there. BOUNDARY §12 breaks its own rule ("rebuild it whenever a section changes"): :302 and :317 cite `lib.rs:148-162` for `str_arg` (now :154-168); :376 cites `crate.lisp:282-292` for the sweep (now :359-378) and says "no test exercises the sweep" though `v04.cache-sweep-spares-other-processes-fresh-copies` (v04.lisp:287) does; :317 says "no interior-NUL test" though `v05.utf8-fastpath-boundary` walks one (v05.lisp:204); :380 cites `codegen.lisp:469-474` for the wrapper session gate (now :559-562); :302's "no e2e test drives Rust-side status 3" is two lines away (panel/probe3.lisp: on SBCL babel encodes `(string (code-char #xD800))` as `ED A0 80` and `(wordbag:echo s)` signals `rulisp:invalid-argument` "InvalidUtf8"). And criterion 3 needs its record: every Lisp/Rust API change of the cycle classified, and the API review's negative result — nothing to deprecate, nothing to rename — written where a stranger can read it, as v0.5's not-list was.
**Scope.** `tools/check-1.0.sh`, `make check-1.0`, run by the `msrv` job next to `make check-versions` (ci.yml:48): criterion 1 — `pub const ABI_VERSION: u32 = 1` (crates/rulisp-runtime/src/lib.rs:30) and `(defconstant +abi-version+ 1)` (lisp/src/ffi.lisp:8); criterion 3 — the gates exist by name in ci.yml, tests/run-m4.lisp and the Makefile (the semver step, `:rulisp-v06`, the three v0.5.0 pins, `check-dist`) as a tripwire against quiet removal; criterion 4 — `grep -c '^|.*GAP' BOUNDARY.md` = 0 and the claims.md footer says `0 unverified`; the Lisp-surface count — externals in package.lisp = rows in tests/golden/lisp-api.sexp = the number stability.md §1 states; criterion 5 — the dry-run step named. Only constants and counts on lines the docs already own; nothing parses prose. `tools/check-dist.sh` (≤ 20 lines: curl `ultralisp.txt` → `releases.txt` → the rulisp archive's lisp/rulisp.asd `:version`; exit 1 unless equal to the version tools/check-versions.sh reads) and releasing.md step 8 → "run `sh tools/check-dist.sh` until it passes; if it has not within a day, the place to look is the Ultralisp project page — its two sources and its check queue — not the tree"; installation.md:97 gains the honest sentence (the dist can lag the tag; check `(asdf:component-version (asdf:find-system :rulisp))` or use the local-projects route at :103-106); the maintainer action outside the tree (remove the never-checked source 2526, re-trigger 2525) recorded in the commit message. BOUNDARY §12: every `file:line` re-verified and the four stale cells above corrected (:376 cites the v04 test; :317 cites v05.lisp:204), plus `v06.invalid-utf8-is-invalid-argument` (guarded by `(code-char #xD800)`, which is NIL on CCL) cited at :302. ROADMAP's three stale lines and the claims count corrected. CHANGELOG 0.6: a "Lisp API" paragraph (the 15 additions, item 8; `build-error` where a host error was, item 10; `Option<float>` NIL, item 11; the one tightening, item 9, with its reproduction) and a "Rust API" paragraph (doc text only; the semver gate). stability.md §3 keeps "nothing is deprecated today" and adds "reviewed for 0.6: no rename or removal found necessary" with the causes (every export is used in README, BOUNDARY §2, quickstart or the suite; a class name in a `handler-case` cannot be staged by a style-warning); §8 gains, per criterion, the command that checks it and its status as of 0.6.0. Surface: none.
**Acceptance.** `sh tools/check-1.0.sh` exits 1 today (the GAP row) and 0 once item 4 is in; the `msrv` job runs it green; `sh tools/check-dist.sh 0.5.0` exits 1 today printing "ultralisp serves 0.3.0 (onlyarche-rulisp-20260815001256)"; `grep -n 'Not understood\|Still open: a worked Deploy\|passes vacuously\|after about an hour' ROADMAP.md docs/releasing.md` is empty; `grep -cE 'no e2e test|no interior-NUL test|no test exercises the sweep' BOUNDARY.md` = 0; `grep -n '32 symbols\|32-symbol' docs/stability.md CHANGELOG.md` matches only the 0.5.0 history entry; `sh tools/check-versions.sh` rc=0. Land after items 4 and 8, or the `msrv` job is red between them.

**Cut order if the cycle runs long:** 13 folds into 14 as one CI step; then item 8's docstring half (the exports stay); then item 3's step (c) (the two pinned steps and the fixture stay); then the release-job re-audit half of item 4 (the Windows branch and `make audit` stay). Nothing else is cut: every remaining item is a reproduced defect, a gate criterion 3 needs, or criterion 4's GAP.

## Explicitly not in v0.6

- **A flagship example** (sqlite/duckdb, tokenizer, image codec). v05-plan.md:77's reopen trigger — "an external issue naming the crate" — is unmet: `gh issue list --state all` shows only closed #1 and no PR. L by definition and the v0.3 cycle again.
- **Hiding `rulisp::runtime` with `#[doc(hidden)]` in 0.6** (three proposals wanted it). Against the 0.5.0 baseline the hide fires cargo-semver-checks' per-item `*_now_doc_hidden`/`*_missing` findings for some thirty runtime items; a cycle-long allow-list for that family would also mask hiding a real item. Not a break by stability.md:13 (generated code names `::rulisp::runtime::*` explicitly and hand-written callers keep compiling), but not tool-clean either. It is the 1.0 major's first commit, where the tool permits it; 0.6 checks the path as-is (item 6). `missing_docs` on rulisp-runtime and doc lines on `STATUS_*` fall with it — polish on a declared non-surface.
- **Declaring 0.7, not 0.6, the criterion-3 cycle** (one proposal's view: the fixes spend the pre-1.0 minor on changes). Decided the other way: every change in this plan is additive under §1's letter — a host error becomes the documented condition (item 10), a host type-error becomes None (item 11), fifteen exports (item 8) — except item 9, classified up front as a §4 soundness fix and proven by item 3's step (c) to break no 0.5.0 call. If the maintainer takes the strict reading, 0.6 is where every API delta lands and 0.7 is the clean cycle; the gates are the same either way.
- **Running the full run-m4 suite against released blobs under a compat mode** (the M half of one proposal's corpus item). The released 0.5.0 assets already load and pass a per-shape smoke on the tree loader (panel/compat-backward.lisp); item 3's pinned steps and its old-suite direction give the evidence; a compat-mode flag with per-test skip bookkeeping is open-ended for no new fact.
- **A committed `glue-prev` crate pinned to crates.io `=0.5.0`.** The released v0.5.0 wordbag and fetch assets *are* a glue crate built with the published 0.5.0 macros and exercise every shape (handle, error, borrowed and stored callback, `on_dump`); a second crate with a Cargo.lock to bump every release duplicates the corpus.
- **A Lisp API deprecation round — style-warnings in 0.6, removals in 1.0** (maintainer candidate f). The review found nothing to deprecate: all 32 exports are used by README, BOUNDARY §2, quickstart, the suite or the fetch veneer; argument conventions are consistent (path/dir positional, `:package` keyword everywhere). The one tightening (item 9) cannot be staged by a warning without keeping the gate-defeating `setf` alive one more minor. stability.md §3 stays "nothing is deprecated today" — a true sentence beats an exercised policy; item 14 records the negative result.
- **Renaming `invalid-argument` to `invalid-argument-error`** (or any suffix-consistency rename). Every exported name appears in README, BOUNDARY §2, quickstart and the suite; a rename breaks `handler-case` clauses for no user benefit and a class name cannot carry a style-warning. Names freeze as they are.
- **Promoting the aarch64 job and adding the linux-arm64 asset as scheduled work.** Promotion is gated on a documented green streak by the procedure at ci.yml:205-211 / stability.md:68-74 (ECL: 24 runs over 35 days); item 2 removes the blocker and restarts the clock, ROADMAP carries the one-commit checklist (asset count 16, not 13 or 15). The streak length is the procedure's to set, not the plan's. A blob without a required test host is the mistake v0.4 fixed.
- **Scalar coercion and range checks in codegen** (the second half of the `Option<float>` proposal: integers accepted for `f64`, `(signed-byte n)` checks signaling `invalid-argument`). No doc promises it and no user asked; it would add a promise to freeze beyond the demand and push the item past S. Item 11 fixes the NIL defect and documents the plain-scalar policy as host-checked; reopen on a user report. A Rust- or manifest-side widening token is refused with it — the defect is entirely Lisp-side.
- **Converting BOUNDARY §12's remaining "no test" runtime-check cells into tests** (non-string panic payload via a `panic_any` export with oracle/golden co-update; a `%check-manifest-abi` pure-function refactor; cache sweep; TLS destructor; `refuse_reentry`). Criterion 4 names GAP rows, and those rows are honestly labelled runtime-check enforced by code on the load/call path. Item 14 takes the two that are already false (the sweep and interior-NUL cells have tests) and the two-line invalid-UTF-8 test; the `:abi` row is taken by item 3's fixture, which is cheaper and more faithful than the refactor. Write the rest the day their cited code is touched.
- **A Quicklisp dist dry run premised on "rulisp/test needs cargo", or moving `rulisp/test` to its own `.asd`.** The premise is false (panel/dist-dryrun.lisp: every system loads with cargo off PATH) and a dist builder finds every `.asd` in the tarball, so a separate file excludes nothing. Item 13 proves the true fact in CI and fixes the contract sentence.
- **Quicklisp submission itself.** A 1.0 decision (stability.md §9; ROADMAP "deliberately deferred to 1.0"); items 13 and 14 make the submission day discover nothing.
- **A scheduled workflow that fails while Ultralisp lags the tag; anything else about Ultralisp serving 0.3.0.** An external queue (position 651, unchanged in three days) that nothing in the tree moves; a workflow that reds without a commit is the v0.5 refusal of the soak workflow. Item 14 turns releasing.md step 8 into a check and adds the honest installation.md sentence; the source cleanup is a recorded maintainer action.
- **An unlocked MSRV check** (`cargo +1.78` in a fresh consumer crate without Cargo.lock). Reds without a commit in the tree; rulisp-runtime has zero dependencies and rulisp-macros only proc-macro2/quote/syn; the locked job is what stability.md §6 promises. Reconsider on the first user report of a 1.78 build failure.
- **Running the fetch suite on Windows or ECL; promoting the macOS fetch step.** No claim in BOUNDARY §12 or docs/claims.md cites them (the §7/§10 fetch rows name the two Linux required jobs); examples/fetch/audit.sh:10-12 globs only `.so`/`.dylib`; the macOS step follows its own streak. Not a surface question.
- **Miri over the shims, cargo-fuzz, ASan/TSan, a soak workflow.** ROADMAP.md:69-74: Miri is re-proposed only with a concrete UB hypothesis — none appeared; the one UB found this cycle (item 1) is Lisp-side. Sanitizers cannot coexist with SBCL's signal-driven GC (v05-plan.md:83); the soak's RSS/fd bounds flake on shared runners. Item 3's fixture and corpus are this cycle's falsifiable checks.
- **`find-crate`/`list-crates`, exporting the parsed manifest structs, handle introspection (`handle-live-p`, `handle-crate`), a post-reload hook API, exporting `*rulisp-version*` or a `(rulisp:version)` function, `CallbackError`/`HandleType` in the Rust prelude.** No consumer in the tree or tracker for any; `describe` and `documentation` answer the REPL need; exporting the version special would let users `setf` what `%check-rulisp-version` compares against. All additive whenever a consumer appears, even after 1.0 — no reason to freeze a speculative signature now.
- **Structured error payloads beyond (type, message)** — the fetch veneer parses the message (examples/fetch/http.lisp:54-56). Needs a wire or manifest change: vocabulary round 2, refused with cause in v0.3, v0.4 and v0.5; the veneer pattern is the documented answer.
- **Stored-callback finalizer deadlock** (the finalizer takes `*stored-callbacks-lock*`, stored-callback.lisp:51, while `callback` holds it across a consing insert, :45-49). Probed on ECL 21.2.1 (panel/fin-lock.lisp): 400,000 tokens minted and dropped with 200 full GCs interleaved — no deadlock, registry drained. SBCL runs finalizers on its finalizer thread; CCL drains terminations off-thread. No observation, nothing to file.
- **Borrowed callback invoked from a Rust worker thread; restore order vs load order and `%run-crate-dump-hooks` without `*registry-lock*`; cache growth on Windows.** Unreachable from safe Rust (`Callback` is `!Send`/`!Sync` and lifetime-pinned, trybuild `ui/stored_callback.rs`, `ui/static_lifetime.rs`); restore order is unobservable and dump-time is single-threaded by the host's rule; the Windows cache is bounded to one copy per process lifetime (crate.lisp:365-366) — item 12 covers the only unbounded path.
- **Vocabulary round 2, zero-copy `:string`, constructor `&key`, push/doorbell callbacks, `dlclose`, on_dump reload semantics, Display-driven `print-object`, LispWorks/Allegro/ABCL/ocicl, macOS x86_64 blobs, any ABI bump, a retroactive `:schema 2`, CI benchmark gating or a benchmarks.md refresh.** Each is a surface change or an untested column in the one cycle criterion 3 requires to have neither, or has no code path this cycle touches (no measured row changes; benchmarks.md is "not a CI gate"). The refusals-with-cause from v0.4 and v0.5 (v04-plan.md:90-101, v05-plan.md:75-87) stand unchanged; no new demand case appeared.

## Start here

**Task:** item 1, one commit. Files: `/home/arche/devel/lisper/rulisp/lisp/src/crate.lisp` — `%stub-crate` (:384-390) additionally sets `(crate-lib-handle crate)`, `(crate-on-dump-ptr crate)`, `(crate-last-error-ptr crate)`, `(crate-dealloc-ptr crate)`, `(crate-handle-frees crate)` and `(crate-cache-path crate)` to nil, so the guard at :417 skips the crate; `describe-object` (:333-357) prints "stubbed: reload failed on restore" when `lib-handle` is nil. New `/home/arche/devel/lisper/rulisp/tests/suite/v06.lisp` with `(def-suite* :rulisp-v06)` and `v06.restore-failure-leaves-no-live-foreign-pointer`, cloned from `m7.dump-restore` (`tests/suite/m1.lisp:274-`): copy `target/debug/libwordbag.so` to a per-process temp name, `load-crate` it with `:crate "wordbag"` in the subprocess script, `uiop:dump-image` an executable whose `*image-entry-point*` calls `(rulisp::%run-crate-dump-hooks)` and prints `RESTORE-STUB-OK`, delete the temp artifact, run the executable capturing stdout+stderr, assert exit 0, the marker, `(not (search "Memory fault" log))` and `(not (search "CORRUPTION WARNING" log))`; `#-(or sbcl ccl)` named pass with cause. Register the file in `/home/arche/devel/lisper/rulisp/lisp/rulisp.asd` (`rulisp/test` components, after `v05` at :30) and `:rulisp-v06` in `/home/arche/devel/lisper/rulisp/tests/run-m4.lisp` (:23-31). `/home/arche/devel/lisper/rulisp/BOUNDARY.md` §10 (:201-207): the one sentence; §12: a row citing the test. `/home/arche/devel/lisper/rulisp/CHANGELOG.md` Unreleased → Fixed.

**Failing-first check:** run the new test against the unmodified loader — the subprocess log must show `CORRUPTION WARNING in SBCL … Memory fault at 0x… (pc=0x…)` exactly as panel/dump-phase1.lisp reproduced (restore warned "could not reload crate wordbag", `lib-handle`/`on-dump-ptr` still `#X630F69B50C10`/`#X746DC92FC9B0`, the hooks jumped to the dead address). After the `%stub-crate` change, `make test-m4` is green on SBCL and `make test-ccl` on CCL, and the log carries neither string.

**Second commit, the same day:** item 2 (the `m4.gc-finalization` fix and the ROADMAP.md:56-61 correction) — the aarch64 streak clock starts at the fix and promotion is bounded by calendar, not effort. Then item 3, before anything else in the tree moves, so every later commit of the cycle is compared to 0.5.0.