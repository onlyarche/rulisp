# Claims register

Every capability, host and performance claim in README.md and the six
docs/ pages, with what enforces or demonstrates it: a suite test by name
(`tests/suite/*.lisp`), a trybuild case (`crates/rulisp/tests/ui/`), a CI
job or step (`.github/workflows/`), a bench row (docs/benchmarks.md) or a
code site. Built by the v0.5 docs audit on 2026-09-03, over the pages as
they were before the audit's corrections; a **resolved** row says what
the audit changed. The next audit is a diff of this file against the
pages. A row marked **unverified** has no test, CI step or bench row
behind it and says what would verify it; its page is worded as such.
Dated wording ("since 0.2") is not a claim and is not listed.

## README.md

| Claim | Kind | Citation |
|---|---|---|
| Annotate plain Rust with a couple of macros, build a cdylib, load it from the REPL as an idiomatic Lisp package | capability | test m3.macro-crate-loaded (tests/suite/m3.lisp:45); naming rule crates/rulisp-macros/src/lib.rs:1187-1189 (make-<type>, <type>-<method>, kebab-case); examples/wordbag/src/lib.rs contains no extern "C" |
| CLOS handles | capability | test m4.handle-lifecycle (tests/suite/m1.lisp:124: typep bag 'rulisp:handle / (wb "WORD-BAG")); lisp/src/crate.lisp commit-bindings defclass |
| typed conditions | capability | test m3.typed-conditions (tests/suite/m3.lisp:11) |
| GC finalizers | capability | test m4.gc-finalization (tests/suite/m1.lisp:136); lisp/src/handle.lisp:111 tg:finalize |
| restarts | capability | tests fx.use-value-restart (tests/suite/m2.lisp:84), m3.transcript (m3.lisp:30); retry-build at lisp/src/build.lisp:104 |
| live reload included | capability | test m6.reload (tests/suite/m1.lisp:234) |
| use-crate = cargo build + load | capability | lisp/src/build.lisp:90-112 (use-crate: %run-cargo-build then load-crate); test m6.reload rebuilds via use-crate :features |
| REPL output #<RULISP:CRATE "wordbag" gen 1 abi 1 :: 4 fns, 1 handle, package WORDBAG> | capability | lisp/src/crate.lisp:33-41 print-object crate: format "~S gen ~D abi ~A :: ~D fns, ~D handle~:P, package ~A" |
| (wordbag:greet "리스퍼") => "Hello, 리스퍼!" | capability | test m3.strings (tests/suite/m1.lisp:105) asserts exactly this string |
| Handle prints as #<WORDBAG:WORD-BAG live gen 1 {…}> | capability | lisp/src/crate.lisp:43-53 print-object handle: "~(~A~) gen ~D" with :type t :identity t; class name word-bag from crates/rulisp-macros/src/lib.rs:883 snake_to_kebab |
| (wordbag:word-bag-add *bag* "hello") prints no value | capability | lisp/src/codegen.lisp:238-239 (:unit result returns (values)) |
| (wordbag:word-bag-len *bag*) => 1 after one add | capability | test m4.handle-lifecycle (tests/suite/m1.lisp:127) |
| Edit Rust, rebuild, reload without restarting the image; crate becomes gen 2 | capability | test m6.reload (tests/suite/m1.lisp:234-246: generation+1, new behaviour visible) |
| Macros generate extern "C" shims: panic-catching, status codes, (ptr,len) UTF-8 strings, opaque handles | capability | crates/rulisp-macros/src/lib.rs:916 catch_unwind; tests m1.panic (m1.lisp:57), m3.strings (m1.lisp:102), m4.handle-lifecycle; BOUNDARY.md §2 status table |
| An s-expression manifest is embedded in the cdylib | capability | test fx.golden-manifest (tests/suite/m2.lisp:125); cargo test examples/wordbag/tests/manifest_golden.rs (CI job "cargo tests (golden manifest + trybuild)") |
| The CL side dlopens a unique copy of the library | capability | lisp/src/crate.lisp:80-88 (%cache-copy-name), :151 (uiop:copy-file then dlopen*); test v04.cache-copy-names-are-process-unique (tests/suite/v04.lisp:277) |
| Reads the manifest and generates wrappers at load time: defuns, CLOS handle classes, typed conditions from Rust error types, trivial-garbage finalizers | capability | lisp/src/crate.lisp prepare-bindings/commit-bindings; tests m3.typed-conditions (m3.lisp:11), m4.gc-finalization (m1.lisp:136); handle.lisp:111 tg:finalize |
| No C headers, no hand-written FFI on either side | capability | grep: no `extern "C"`/`unsafe` in examples/{wordbag,rx,wasm,fetch}/src; Lisp wrappers are compiled from the manifest (lisp/src/codegen.lisp wrapper-form) |
| Bindings are derived by construction from the exact library that was just loaded | capability | lisp/src/ffi.lisp dlsym-ptr resolves against the specific handle; test m6.captured-wrapper-gate (tests/suite/m1.lisp:248); BOUNDARY §12 row "Loader resolves every symbol against the specific dlopen handle" |
| Panics become rulisp:rust-panic conditions | capability | test m1.panic (tests/suite/m1.lisp:57) |
| panic = "abort" builds fail to compile | capability | test m1.panic-abort-guard (tests/suite/m1.lisp:65); crates/rulisp-runtime/src/lib.rs:11-17 compile_error! |
| Handles are gated by a state machine (in-flight counting + deferred free) | capability | lisp/src/handle.lisp:30-97 (cell-begin-call, cell-end-call, %cell-free); test m4.free-vs-in-flight (tests/suite/m1.lisp:151) |
| Double frees signal named conditions | capability | **resolved** — reworded in the audit: a second free is refused (returns NIL), a free racing an in-flight call is deferred — tests m4.handle-lifecycle, m4.free-vs-in-flight |
| Use-after-free signals a named condition | capability | **resolved** — reworded in the audit: a second free is refused (returns NIL), a free racing an in-flight call is deferred — tests m4.handle-lifecycle, m4.free-vs-in-flight |
| Free-during-call races signal named conditions | capability | **resolved** — reworded in the audit: a second free is refused (returns NIL), a free racing an in-flight call is deferred — tests m4.handle-lifecycle, m4.free-vs-in-flight |
| Stale handles after reload signal a named condition | capability | test m6.reload (tests/suite/m1.lisp:242: signals rulisp:stale-handle-error); m6.captured-wrapper-gate |
| Stale handles after image restore signal a named condition | capability | test m7.dump-restore (tests/suite/m1.lisp:274; restored image asserts rulisp:stale-handle-error on the pre-dump handle) |
| …instead of corrupting memory (fuzzed) | capability | tests m4h.thread-race (tests/suite/m4.lisp:63), m4h.random-op-sequence (m4.lisp:135) with per-generation live-count reconciliation |
| Callbacks are borrowed, same-thread, lifetime-pinned (!Send): storing one is a compile error | capability | trybuild crates/rulisp/tests/ui/stored_callback.rs (+ static_lifetime.rs); crates/rulisp/src/lib.rs:166-169 PhantomData<(*mut (), &'a (), …)> |
| A Lisp condition tunnels through Rust (destructors run) and re-signals as the same object | capability | test m5.callback-condition-identity (tests/suite/m1.lisp:191: eq identity + CB_GUARD_DROPS incremented); m4h.nested-callbacks |
| Live reload never calls dlclose | capability | test m6.captured-wrapper-gate (tests/suite/m1.lisp:248: old-generation wrapper+handle still callable); grep: no dlclose/FreeLibrary in lisp/src; BOUNDARY §12 §9 row |
| Live reload always dlopens a fresh copy | capability | lisp/src/crate.lisp:151 (copy per load) + %cache-copy-name :80; test v04.cache-copy-names-are-process-unique |
| The reload cannot lie (new code is observably loaded) | capability | test m6.reload (tests/suite/m1.lisp:238-241: GREET changes from "Hello" to "Hi" after use-crate :features '("alt-greeting")) |
| Image dump/restore (save-lisp-and-die) invalidates every pre-dump handle via a session counter | capability | test m7.dump-restore (tests/suite/m1.lisp:274); lisp/src/crate.lisp:391-398 (%restore-all-crates incf *session*); handle.lisp:30-52 session gate |
| Regenerates all bindings on startup (image restore) | capability | test m7.dump-restore (post-restore GREET works, saved closure refuses with crate-not-loaded-error); lisp/src/crate.lisp:399-407 + uiop:register-image-restore-hook |
| docs/quickstart.md wraps the real regex crate; the finished example is examples/rx/ | capability | docs/quickstart.md; examples/rx/src/lib.rs (regex = "1" dependency); test v04.last-error-is-per-library loads examples/rx |
| …in 10 minutes | performance | **resolved** — removed in the audit ("in 10 minutes": no measurement) |
| docs/usage.md explains two ways to consume rulisp: prebuilt glue library (no Rust toolchain) vs building your own | capability | docs/usage.md Case A/Case B; test v02.blob-loading; CI step "load :rulisp with cargo stripped from PATH" (sbcl-linux job) |
| examples/wasm gives CL a WebAssembly runtime in ~190 lines of glue | performance | examples/wasm/src/lib.rs: 234 lines total, 180 non-blank non-comment (grep -cvE) |
| Load .wat/.wasm modules from the REPL | capability | examples/wasm/src/lib.rs:56-62 (Wasm::load: wat::parse_file for .wat, fs::read otherwise) |
| Exports run under a fuel-metered CPU budget; runaway guest code traps as a condition instead of hanging the image | capability | examples/wasm/src/lib.rs:63-69 (config.consume_fuel, store.set_fuel), :38-42 From<wasmi::Error> for WasmError → wasm:wasm-error condition via manifest :errors |
| Byte buffers move in and out of guest linear memory, bounds-checked; out-of-bounds is a condition | capability | examples/wasm/src/lib.rs:126-165 (memory_write/memory_read with checked_add + WasmError) |
| Host functions: guest code calls straight into Lisp closures (stored callbacks); a condition in the closure becomes a guest trap | capability | examples/wasm/src/lib.rs:73-86 (host.notify → StoredCallback::call, Err → wasmi::Error), :119-122 on_notify; examples/wasm/guest.wat imports host.notify |
| Wasm traps arrive as Lisp conditions | capability | examples/wasm/src/lib.rs:38-42 (wasmi::Error → WasmError) + typed-condition mechanism (m3.typed-conditions); examples/wasm/fib.wat "boom" (unreachable) |
| Built on the signal-handler-free wasmi interpreter (BOUNDARY §7) | capability | CI job "cargo tests (golden manifest + trybuild)" step "BOUNDARY §7 signal audit over every example (with self-test)" — make audit runs tools/rulisp-audit.sh over target/debug/libwasm.so; examples/wasm/Cargo.toml wasmi = "0.50" with rationale |
| DESIGN.md is in Korean | capability | DESIGN.md:1 ("# rulisp 구현 계획") |
| Measured boundary costs with the method are in docs/benchmarks.md | performance | docs/benchmarks.md "Method" + "Baseline — 2026-09-03" table (make bench, tests/bench.lisp) |
| docs/stability.md says what is stable, what 1.0 will promise, and how hosts are supported | capability | docs/stability.md §1 (surfaces), §5 (host support = required CI jobs), §8 (1.0 exit criteria) |
| SECURITY.md covers reporting a vulnerability and the threat model | capability | SECURITY.md sections "Reporting a vulnerability" and "Threat model" |
| CI-verified: SBCL on Linux x86-64 | host | .github/workflows/ci.yml job "SBCL / Linux x86-64 (required)" (make test-m2, test-m4, test-fetch, no-cargo load) |
| CI-verified: SBCL on macOS arm64 | host | .github/workflows/ci.yml job "SBCL / macOS arm64 (required)" (macos-latest, make test-m4) |
| CI-verified: SBCL on Windows x86-64 | host | .github/workflows/ci.yml job "SBCL / Windows x86-64 (required)" (cargo test + make test-m4) |
| CI-verified: Clozure CL 1.13 on Linux x86-64 | host | .github/workflows/ci.yml job "CCL / Linux x86-64 (required)" downloads ccl-1.13-linuxx86.tar.gz; make test-ccl + test-fetch-ccl |
| CI-verified: ECL 21+ on Linux | host | .github/workflows/ci.yml job "ECL / Linux (required)" (apt ecl = 21.2.1; run-m4 + make test-ecl-program); docs/stability.md §5 "ECL 21.2.1 on Linux x86-64" |
| races — all green | capability | test m4h.thread-race (tests/suite/m4.lisp:63, 8 threads × 10k ops); gh run list: latest main CI run 33741068668 success |
| nested callbacks — all green | capability | test m4h.nested-callbacks (tests/suite/m4.lisp:106) |
| reload — all green | capability | tests m6.reload (m1.lisp:234), m4h.reload-under-load (m4.lisp:176) |
| dump/restore — all green on the whole matrix | capability | test m7.dump-restore (tests/suite/m1.lisp:274; real on SBCL and CCL via uiop:dump-image) |
| 10k-op fuzz — all green | performance | test m4h.random-op-sequence (tests/suite/m4.lisp:146: dotimes 10000); m4h.thread-race (m4.lisp:79: 10000 per thread × 8) |
| ECL: a C toolchain is required for callbacks; rulisp natively compiles trampolines | host | lisp/src/codegen.lisp:89-116 (#+ecl compile-file branch of %define-callback-trampoline); test v04.ecl-toolchain-failure-is-named (tests/suite/v04.lisp:156) |
| …to dodge an upstream ECL GC bug we root-caused (docs/upstream/ecl-dynamic-callback-gc.md) | host | docs/upstream/ecl-dynamic-callback-gc.md (root cause in src/c/ffi.d si_make_dynamic_callback; reproduced on 21.2.1); CHANGELOG 0.2.0 Fixed |
| Foreign-thread stored callbacks are unsupported on ECL | host | test v02.stored-callback-cross-thread (tests/suite/v02.lisp:118-124: #+ecl skip); BOUNDARY.md §7 + §12 row (UB-by-design) |
| Requirement: a Rust toolchain (cargo) | capability | lisp/src/build.lisp:7-11 find-cargo (used only by use-crate); CI step "load :rulisp with cargo stripped from PATH"; docs/stability.md §9 |
| Requirement: CFFI-capable Lisp | capability | lisp/rulisp.asd :depends-on (#:cffi …); lisp/src/ffi.lisp defcfun/foreign-funcall-pointer |
| Quicklisp deps: cffi, babel, trivial-garbage, bordeaux-threads | capability | lisp/rulisp.asd :depends-on (#:cffi #:babel #:trivial-garbage #:bordeaux-threads #:uiop) |
| make test-m4 = full gate: cargo tests + all suites on SBCL | capability | Makefile target test-m4 (cargo test --workspace; sbcl --load tests/run-m4.lisp → suites m1–m4, v02–v05); CI step "full gate (macro pipeline + hardening)" |
| make test-ccl = same suites on Clozure CL | capability | Makefile target test-ccl ($(CCL) --batch --load tests/run-m4.lisp); CI job "CCL / Linux x86-64 (required)" runs make test-ccl |
| The "Scope" heading carried a 0.1 version stamp | stale | **resolved** — retitled in the audit: "What crosses the boundary" |
| In: scalars | capability | test m0.scalar-and-bench (tests/suite/m1.lisp:39); trybuild bad_type.stderr enumerates i8..i64, u8..u64, f32, f64 |
| In: bool | capability | test v04.free-shim-swallows-drop-panic (MAKE-GRENADE nil / t); lisp/src/codegen.lisp +scalar-types+ :bool |
| In: &str/String | capability | tests m3.strings, m3.string-allocations (tests/suite/m1.lisp:102,110); v05.utf8-fastpath-boundary |
| In: &[u8]/Vec<u8> byte buffers | capability | tests v02.bytes-in, v02.bytes-roundtrip (tests/suite/v02.lisp:10,20); v03.bulk-bytes-identity |
| In: Option<T> (NIL ↔ None) | capability | tests v02.option-result, v02.option-param (tests/suite/v02.lisp:52,59) |
| In: &[scalar]/Vec<scalar> vectors | capability | tests v02.vec-roundtrip (v02.lisp:69), v03.bulk-vec-identity, v03.bulk-vec-floats |
| In: opaque handles (&self methods, constructors) | capability | test m4.handle-lifecycle; trybuild crates/rulisp/tests/ui/mut_self.rs (no &mut self); v04.constructor-name-attribute |
| In: borrowed same-thread callbacks | capability | test m5.callback-basic (tests/suite/m1.lisp:183); trybuild ui/stored_callback.rs enforces !Send |
| In: stored any-thread callbacks (StoredCallback + rulisp:callback tokens) | capability | test v02.stored-callback-basic (tests/suite/v02.lisp:105); lisp/src/stored-callback.lisp |
| Stored callbacks: fail-safe lifetime | capability | test v02.stored-callback-dead-id (tests/suite/v02.lisp:139) |
| Stored callbacks verified cross-thread on SBCL and CCL | host | test v02.stored-callback-cross-thread (tests/suite/v02.lisp:118, #-ecl) — run by make test-m4 in the SBCL Linux/macOS/Windows jobs and by make test-ccl in the CCL job |
| Result errors → typed conditions | capability | tests m3.typed-conditions (m3.lisp:11), m2.fallible (m1.lisp:87) |
| Prebuilt-blob loading (load-blob-crate, no Rust toolchain needed) | capability | test v02.blob-loading (tests/suite/v02.lisp:87); lisp/src/build.lisp:77-88 (no cargo on that path); CI step "load :rulisp with cargo stripped from PATH"; .github/workflows/blobs.yml builds the release assets |
| live reload | capability | test m6.reload (tests/suite/m1.lisp:234) |
| image dump/restore | capability | test m7.dump-restore (tests/suite/m1.lisp:274); v04.on-dump-* for the declared hook |
| use-value restart | capability | tests fx.use-value-restart (tests/suite/m2.lisp:84), m3.transcript; lisp/src/codegen.lisp:531 |
| retry-build restart | capability | lisp/src/build.lisp:104 (restart-case … (retry-build ())); exported lisp/src/package.lisp:41 |
| Out for now: non-SBCL image dump | stale | **resolved** — replaced in the audit: ECL has no image dump, ship a program-op executable — CI step make test-ecl-program; CCL dump/restore is test m7.dump-restore |
| rulisp does not auto-bind arbitrary existing crates, by design | capability | ROADMAP.md "Non-goals"; DESIGN.md §1 non-goals; lisp/src/crate.lisp %open-and-verify refuses a non-rulisp library with abi-mismatch-error |
| License: MIT | capability | LICENSE (MIT License, 2026 arche); crates/*/Cargo.toml license = "MIT"; lisp/rulisp.asd :license "MIT" |

## docs/installation.md

| Claim | Kind | Citation |
|---|---|---|
| rulisp is set up and works on Linux, macOS and Windows | host | .github/workflows/ci.yml jobs 'SBCL / Linux x86-64 (required)', 'SBCL / macOS arm64 (required)', 'SBCL / Windows x86-64 (required)'; docs/stability.md §5 |
| The Rust toolchain is invoked by rulisp:use-crate at development time only — loading the rulisp system itself needs no Rust | capability | .github/workflows/ci.yml sbcl-linux step 'load :rulisp with cargo stripped from PATH'; lisp/src/build.lisp:7-11,98 (cargo resolved only inside use-crate) |
| Verified with Rust 1.97 | host | docs/benchmarks.md Baseline (cargo 1.97.0); local rustc 1.97.0; ci.yml uses dtolnay/rust-toolchain@stable |
| rulisp relies on catch_unwind | capability | crates/rulisp-runtime/src/lib.rs:21,112; test m1.panic (tests/suite/m1.lisp:57) |
| The crates use edition 2021 | capability | crates/rulisp/Cargo.toml:4, crates/rulisp-macros/Cargo.toml:4, crates/rulisp-runtime/Cargo.toml:4 (edition = "2021") |
| Verified with SBCL 2.1.11+ on Linux, macOS and Windows | host | ci.yml required jobs sbcl-linux / sbcl-macos / sbcl-windows (make test-m4); docs/benchmarks.md host row SBCL 2.1.11 (Debian); docs/stability.md §5 |
| Verified with Clozure CL 1.13 on Linux | host | ci.yml job 'CCL / Linux x86-64 (required)' (downloads ccl-1.13-linuxx86, runs make test-ccl + make test-fetch-ccl); docs/stability.md §5 |
| Verified with ECL 21+ on Linux | host | ci.yml job 'ECL / Linux (required)' (apt ecl on ubuntu-latest = 21.2.1; run-m4 + make test-ecl-program); docs/stability.md §5 'ECL 21.2.1' |
| Quicklisp pulls the CL dependencies; verified with the current dist | host | ci.yml 'install quicklisp' step in every Lisp job (fresh quicklisp.lisp from beta.quicklisp.org) |
| CL libraries needed: cffi (FFI), babel (UTF-8), trivial-garbage (finalizers), bordeaux-threads (locks) | capability | lisp/rulisp.asd:6 :depends-on; use sites lisp/src/ffi.lisp:211,272 (babel), lisp/src/handle.lisp:111 (tg:finalize), lisp/src/handle.lisp:24 and crate.lisp:54 (bt:make-lock) |
| fiveam is needed for the test suites | capability | lisp/rulisp.asd:17 (rulisp/test :depends-on (#:rulisp #:fiveam)); tests/run-m4.lisp:16 |
| A C toolchain/linker (gcc / Xcode CLT / MSVC build tools) is needed because Rust needs a system linker | host | ci.yml: every job builds glue crates with the runner's native toolchain (sbcl-linux, sbcl-macos, sbcl-windows, ccl-linux, ecl-linux) |
| On Debian/Ubuntu, `apt install sbcl` provides a working SBCL | host | ci.yml sbcl-linux step `sudo apt-get update && sudo apt-get install -y sbcl` |
| Clozure CL 1.13 for Linux x86 is the tarball ccl-1.13-linuxx86.tar.gz from the Clozure GitHub release, unpacked into $HOME | host | ci.yml ccl-linux step 'install ccl' (identical URL and tar -C $HOME) |
| The CCL binary is ~/ccl/lx86cl64 | host | Makefile:65 `CCL ?= $(HOME)/ccl/lx86cl64`; ci.yml `make test-ccl CCL="$HOME/ccl/lx86cl64"` |
| On musl distros glibc-built artifacts won't load; build glue crates with the musl target and -C target-feature=-crt-static | capability | **resolved** — external Rust/musl fact; the page now says "untested here" |
| CI-verified on macOS arm64 with SBCL, full suite green | host | ci.yml job 'SBCL / macOS arm64 (required)' runs `make test-m4` on macos-latest |
| Clozure CL has no native arm64 macOS build | host | External: github.com/Clozure/ccl/releases/tag/v1.13 — 'This release does not contain support for Apple Silicon (ARM64) processors' (Darwin/x86 64-bit only); ci.yml/stability.md §5 list CCL on Linux only |
| Locally built dylibs are ad-hoc signed automatically; only network-downloaded dylibs are quarantined by Gatekeeper; use-crate builds locally so development is unaffected | capability | ci.yml sbcl-macos: `make test-m4` builds every crate via use-crate and loads it (including reloads, m6.reload); docs/distribution.md Pattern A Gatekeeper caveat |
| rulisp loads libraries by absolute path, so SIP's stripping of DYLD_LIBRARY_PATH is irrelevant | capability | lisp/src/crate.lisp:95-98,150 (copy placed under the absolute uiop:xdg-cache-home dir); lisp/src/ffi.lisp:87 `%open-library (uiop:native-namestring path)` on that copy |
| CI-verified on Windows x86-64 with SBCL, full suite green | host | ci.yml job 'SBCL / Windows x86-64 (required)' (cargo test --workspace + make test-m4); CHANGELOG 0.3.0 'CI runs the full suite there, 177/177' |
| SBCL can be installed on Windows with `choco install sbcl` | host | ci.yml sbcl-windows step 'install sbcl (with retry)' — `choco install sbcl --no-progress -y` |
| On Windows artifacts are <name>.dll (cargo drops the lib prefix) and load-blob-crate looks for <name>-windows-x86_64.dll | capability | lisp/src/ffi.lisp:110-114 (artifact-file-name), lisp/src/build.lisp:58-75 (host-blob-suffix/blob-file-name); .github/workflows/blobs.yml matrix (windows: lib '' , suffix windows-x86_64, ext dll); test v02.blob-loading (tests/suite/v02.lisp:87, runs on the Windows job via test-m4) |
| A loaded DLL is locked, so an artifact cannot be overwritten in place | capability | lisp/src/ffi.lisp:63-66 and lisp/src/crate.lisp:146-149,364-365 (design comments relying on it) |
| rulisp loads a uniquely named copy per generation, so use-crate/reload-crate work on Windows anyway | capability | lisp/src/crate.lisp:80-89 (%cache-copy-name: process tag + counter + time), :146-151; test m6.reload (tests/suite/m1.lisp:234, unguarded, runs in the Windows job's make test-m4); BOUNDARY.md §12 §9 'unique copy' row |
| Stale cache copies are swept on a later run | capability | lisp/src/crate.lisp:358-377 (%sweep-crate-cache, called at :245); tests v04.cache-copy-names-are-process-unique, v04.cache-sweep-spares-other-processes-fresh-copies (tests/suite/v04.lisp:277,287) |
| The repository must be checked out with LF endings (shipped .gitattributes does this): manifest fixtures are byte-compared and a CR inside a Lisp format string breaks its line continuations | capability | .gitattributes:10-13 (`* text=auto eol=lf`); test fx.golden-manifest (tests/suite/m2.lisp:125) + examples/wordbag/tests/manifest_golden.rs; CHANGELOG 0.3.0 Fixed (CRLF broke the golden fixture and FORMAT-ERROR) |
| rulisp is on Ultralisp | capability | External: https://ultralisp.org/projects/onlyarche/rulisp lists systems rulisp and rulisp/test (source v3, 2026-08-15); docs/distribution.md 'Publishing rulisp itself' |
| Cloning into Quicklisp's local-projects lets ASDF find lisp/rulisp.asd automatically | capability | **resolved** — CI step "load :rulisp from quicklisp local-projects" (SBCL / Linux job), added in the audit |
| (ql:quickload :rulisp) pulls cffi/babel/trivial-garbage/bordeaux-threads | capability | lisp/rulisp.asd:6; ci.yml sbcl-linux no-cargo step `(ql:quickload :rulisp :silent t)` |
| Pushing /path/to/rulisp/lisp/ onto asdf:*central-registry* is enough for ASDF to find the system | capability | tests/run-m4.lisp:12-14, tests/bench.lisp:14-16, ci.yml sbcl-linux no-cargo step (exactly this push) |
| The smoke test builds and loads the bundled regex example; the first build downloads crates.io deps | capability | examples/rx/Cargo.toml (`regex = "1"`, cdylib, rulisp path dep); reproduced locally 2026-09-03 on SBCL 2.1.11 (use-crate on examples/rx → #<CRATE "rx" gen 1 abi 1 :: 7 fns, 1 handle, package RX>) |
| (rx:regex-count (rx:make-regex "[0-9]+") "1 22 333") ; => 3 | capability | Reproduced locally 2026-09-03 → 3; naming rule docs/quickstart.md:92-94; MAKE-REGEX used by test v04.last-error-is-per-library (tests/suite/v04.lisp:203); regex-count is the docs/benchmarks.md 'rx count over 1 MiB' row |
| `make test-m4` runs everything, on SBCL | capability | Makefile:19-21 (cargo test --workspace + tests/run-m4.lisp: suites m1, m2, m3, m4, v02, v03, v04, v05); ci.yml sbcl-linux 'full gate' |
| `make test-ccl CCL=~/ccl/lx86cl64` runs the same suites on Clozure CL (Linux) | capability | Makefile:65-67 (CCL --batch --load tests/run-m4.lisp; default CCL already ~/ccl/lx86cl64); ci.yml ccl-linux |
| `make test-fetch` runs the async HTTP example hermetically | capability | Makefile:23-25 (examples/fetch/audit.sh + tests/run-fetch.lisp); tests/run-fetch.lisp:2 'Hermetic — the crate starts its own loopback server'; examples/fetch/src/lib.rs:364-368 (TcpListener 127.0.0.1:0); ci.yml sbcl-linux step 'fetch suite' |
| `make bench` measures boundary throughput | capability | Makefile:60-61 (tests/bench.lisp); docs/benchmarks.md Method + Baseline 2026-09-03 |
| `cargo test --workspace` runs the manifest golden and compile-fail tests | capability | examples/wordbag/tests/manifest_golden.rs; crates/rulisp/tests/compile_fail.rs with crates/rulisp/tests/ui/{bad_type,ctor_name_bad,ctor_self,module_typo,mut_self,non_send_handle,static_lifetime,stored_callback}.rs; ci.yml job 'cargo tests (golden manifest + trybuild)' |
| use-crate finds cargo via $RULISP_CARGO, then ~/.cargo/bin/cargo, then `cargo` on PATH | capability | lisp/src/build.lisp:7-11 (find-cargo); reproduced locally: RULISP_CARGO=/nonexistent/cargo is honoured first |
| A build failure signals rulisp:build-error carrying cargo's stderr, with a retry-build restart | capability | lisp/src/build.lisp:49-56 (build-error :stderr err on non-zero exit), :99-105 (restart-case retry-build); lisp/src/conditions.lisp:56-61; lisp/src/package.lisp exports build-error-stderr/retry-build; CHANGELOG 0.1.0 |
| Release builds: (rulisp:use-crate dir :profile :release) | capability | lisp/src/build.lisp:90-91,46 (`--release` when profile is :release); tests/bench.lisp:22-23 via `make bench` |
| Every load dlopens a unique copy of the artifact; older copies are swept automatically | capability | **resolved** — corrected in the audit: UIOP cache dir, %LOCALAPPDATA%\cache\rulisp\ on Windows — lisp/src/crate.lisp (uiop:xdg-cache-home) |
| The library cache lives under ~/.cache/rulisp/ ($XDG_CACHE_HOME respected) | capability | **resolved** — corrected in the audit: UIOP cache dir, %LOCALAPPDATA%\cache\rulisp\ on Windows — lisp/src/crate.lisp (uiop:xdg-cache-home) |
| Loading rulisp itself never runs cargo and opens no foreign libraries — cargo is needed only for use-crate | capability | ci.yml sbcl-linux step 'load :rulisp with cargo stripped from PATH'; docs/stability.md §9; lisp/src (no load-foreign-library anywhere; dlopen/LoadLibraryA are defcfun'd against the already-linked libc/kernel32, ffi.lisp:36-80, and called only from crate.lisp:174 inside load-crate) |
| When Rust is not installed / not on PATH, use-crate signals rulisp:build-error "cargo: command not found" | capability | **resolved** — corrected in the audit: a missing cargo is the host run-program error, not build-error (reproduced on SBCL) |
| rulisp:crate-not-loaded-error with a dlopen message means an artifact for the wrong platform or missing system libs | capability | lisp/src/ffi.lisp:82-93 (dlopen* → crate-not-loaded-error with dlerror()/GetLastError text, naming the requested artifact and the cache copy); CHANGELOG 0.4.0 Fixed 'A failed dlopen names the artifact' |
| rulisp:abi-mismatch-error "not a rulisp crate" means the cdylib wasn't built with rulisp::module!, or the crate name doesn't match | capability | lisp/src/crate.lisp:173-179 (%open-and-verify: missing `<name>_rulisp_abi_version` → abi-mismatch-error "~A is not a rulisp crate (no ~Aabi_version)"); prefix derived from the Cargo package name / file name (build.lisp:96, crate.lisp:144-145), so a `module! { name: … }` that disagrees hits this path |
| rulisp:abi-mismatch-error "different target" means the artifact was built for another arch/OS | capability | lisp/src/crate.lisp:187-193 ("artifact was built for a different target"); lisp/src/manifest.lisp:106-118 (target-compatible-p); test fx.target-check (tests/suite/m2.lisp:71-78) |
| A Cargo profile with panic = "abort" fails to compile with `rulisp requires panic = "unwind"`; removing it is the fix, since abort would let a panic kill the Lisp image | capability | crates/rulisp-runtime/src/lib.rs:11-17 (compile_error!, exact text); test m1.panic-abort-guard (tests/suite/m1.lisp:65-81, asserts 'rulisp requires panic' in cargo's stderr); BOUNDARY.md §12 §8 row |
| A `; note: ... unknown type` on first use (SBCL) is a harmless forward-reference compiler note | capability | Reproduced 2026-09-03: `; note: can't open-code test of unknown type RX::REGEX` printed by SBCL during use-crate of examples/rx, once per wrapper taking the handle (5×); cause lisp/src/crate.lisp:281-297 (wrappers compiled before commit-bindings defines the class at :312-315; only `warning`s are muffled, sb-ext:compiler-note is not); the call then returns 3 |

## docs/quickstart.md

| Claim | Kind | Citation |
|---|---|---|
| The finished example lives in examples/rx/; every snippet below is taken from a real session | capability | **resolved** — snippet gained first_match and the transcript was regenerated (7 fns) in the audit |
| Prerequisites: a Rust toolchain, SBCL (or CCL) with Quicklisp | host | CI jobs `SBCL / Linux x86-64 (required)`, `CCL / Linux x86-64 (required)` (.github/workflows/ci.yml); cargo discovery lisp/src/build.lisp:7-11 |
| src/lib.rs is plain Rust: three macros, no `extern "C"` anywhere | capability | examples/rx/src/lib.rs contains no `extern`; shims are emitted by the macro (crates/rulisp-macros/src/lib.rs:721, BOUNDARY §12 row 'Shims are extern "C"'); m3.macro-crate-loaded proves the macro pipeline is what the suite runs against |
| Naming is mechanical: a constructor becomes rx:make-regex, methods become rx:regex-is-match, free fns keep their kebab-cased name | capability | crates/rulisp-macros/src/lib.rs:1181-1189 (`make-{kebab}` / `{kebab}-{method}`), :1061 (`snake_to_kebab` for free fns); fx.golden-manifest pins the wordbag names; v04.constructor-name-attribute (MAKE-WORD-BAG); reproduced export list on examples/rx |
| The macros add panic catching in every shim | capability | m1.panic (tests/suite/m1.lisp:57); catch_unwind in every generated shim, crates/rulisp-macros/src/lib.rs:916; BOUNDARY §8 |
| The macros add UTF-8 (ptr,len) string passing | capability | m3.strings (tests/suite/m1.lisp:102); v05.utf8-fastpath-boundary; BOUNDARY §4 |
| The macros add opaque-handle lifecycle with GC finalizers | capability | m4.handle-lifecycle, m4.gc-finalization (tests/suite/m1.lisp:124,136); lisp/src/handle.lisp:111 (tg:finalize) |
| The macros add error -> condition mapping | capability | m2.fallible (tests/suite/m1.lisp:87); m3.typed-conditions (tests/suite/m3.lisp:11) |
| The macros embed a manifest that the Lisp side reads | capability | fx.golden-manifest (tests/suite/m2.lisp:125); lisp/src/crate.lisp `%read-library-manifest`; BOUNDARY §1/§11 |
| A second constructor on the same type needs its own name via `#[rulisp(constructor, name = "...")]` because the derived one would collide | capability | v04.constructor-name-attribute (tests/suite/v04.lisp:313); trybuild crates/rulisp/tests/ui/ctor_name_bad.rs; crates/rulisp-macros/src/lib.rs:1181-1189; CHANGELOG 0.4.0 |
| Duplicate Lisp names are a load-time error | capability | v03.duplicate-lisp-name-rejected (tests/suite/v03.lisp:87): `rulisp:manifest-error` from parse-manifest; CHANGELOG 0.3.0 |
| `rulisp:use-crate` = cargo build + dlopen + codegen | capability | lisp/src/build.lisp:90-102 (use-crate: %run-cargo-build then load-crate); m6.reload drives use-crate; m1.panic-abort-guard exercises the cargo path |
| REPL prints `#<CRATE "rx" gen 1 abi 1 :: 6 fns, 1 handle, package RX>` | capability | **resolved** — transcript regenerated in the audit: #<RULISP:CRATE "rx" gen 1 abi 1 :: 7 fns, 1 handle, package RX> |
| A handle prints as `#<REGEX live gen 1 {1002934303}>` | capability | **resolved** — transcript corrected in the audit: #<RX:REGEX live gen 1 …> — lisp/src/crate.lisp print-object handle |
| regex-is-match -> T, regex-count -> 3, regex-replace-all -> "a#b#" | capability | Reproduced on SBCL 2.1.11 against examples/rx (T, 3, "a#b#"); string path m3.strings; docs/installation.md smoke test uses the same regex-count => 3 |
| Matches iterate straight into a Lisp closure via regex-for-each-match -> ("1" "22" "333") | capability | Reproduced ("1" "22" "333"); m5.callback-basic (tests/suite/m1.lisp:183); BOUNDARY §6.1 |
| Full unicode, both directions: count of [가-힣]+ over "한글 and 러스트 here" is 2 | capability | Reproduced (2); m3.strings (Korean/emoji echo round trip); v05.utf8-fastpath-boundary (tests/suite/v05.lisp:185) |
| A bad pattern doesn't crash anything — Rust's `Err` arrives as `rulisp:rust-error` with a `use-value` restart, carrying the Display message | capability | m2.fallible (tests/suite/m1.lisp:87), m3.transcript (tests/suite/m3.lisp:30), fx.use-value-restart (tests/suite/m2.lisp:84); reproduced: restarts list starts with USE-VALUE |
| Debugger shows RULISP:RUST-ERROR with 'Rust error Error in rx:make-regex: regex parse error: (unclosed ^ error: unclosed group' | capability | Reproduced byte-for-byte on examples/rx; report format lisp/src/conditions.lisp:5-12; message = Display output per BOUNDARY §3 (m2.fallible pins exact Display text) |
| A Rust panic arrives as `rulisp:rust-panic` the same way (use-value restart) and the image survives both | capability | m1.panic (tests/suite/m1.lisp:57: ADD works after the panic); fx.use-value-restart (use-value on rust-panic); v04.poisoned-mutex-reports-panic; BOUNDARY §8 |
| Error types named in Results (other than the generic rulisp::Error) become their own condition classes, e.g. ParseError -> rx:parse-error, a subclass of rulisp:rust-error | capability | **resolved** — corrected in the audit: a type NAMED Error folds into rulisp:rust-error — crates/rulisp-runtime/src/meta.rs render_manifest (:errors); test m3.typed-conditions for the named case |
| Re-running use-crate after editing Rust reloads as generation 2 without restarting the REPL | capability | m6.reload (tests/suite/m1.lisp:234: crate-generation +1, new behaviour visible); m4h.reload-under-load; reproduced gen 2 |
| Using a generation-1 handle after reload signals RULISP:STALE-HANDLE-ERROR — handle gen 1, crate gen 2 | capability | m6.reload (`signals rulisp:stale-handle-error`), m6.captured-wrapper-gate; report lisp/src/conditions.lisp:41-46; reproduced: 'Stale handle (in rx:regex-is-match): handle gen 1, crate gen 2' |
| A stale handle is still safely freeable; `(rulisp:free *re*)` returns T | capability | m6.reload (`(is (eq t (rulisp:free bag)))` after staleness), m6.captured-wrapper-gate; lisp/src/handle.lisp:65-75 (%maybe-foreign-free via birth generation); reproduced T (then NIL on a second free) |
| Old generations stay loaded (never dlclose'd), so stale handles fail politely and can always be freed | capability | m6.captured-wrapper-gate (old-gen wrapper+handle still callable after reload); BOUNDARY §12 row 'The loader never calls dlclose' (no dlclose/FreeLibrary in lisp/src); lisp/src/crate.lisp:3-6 |
| `rulisp:free` is optional — the GC finalizer releases unreachable handles too | capability | m4.gc-finalization (tests/suite/m1.lisp:136: 1000 abandoned handles reach live count 0 after GC); lisp/src/handle.lisp:100-112 (free docstring, tg:finalize) |
| The v1 type vocabulary is integers, floats, bool, &str/String, opaque handles (&self methods + constructors) and synchronous same-thread callbacks; patterns for things it doesn't have yet follow | stale | **resolved** — rewritten in the audit to the current vocabulary — BOUNDARY §11; tests v02.bytes-roundtrip, v02.option-result, v02.vec-roundtrip, v02.stored-callback-basic |
| Option<T> is supported (since 0.2): Lisp NIL <-> None for scalar, string and byte payloads | capability | v02.option-result, v02.option-param (tests/suite/v02.lisp:52,59); BOUNDARY §11 (:option T); CHANGELOG 0.2.0 |
| `rx:regex-first-match` returns the match or NIL | capability | examples/rx/src/lib.rs:38-41 (`first_match -> Option<String>`); reproduced: "12" / NIL |
| `Option<bool>` is rejected — nil can't distinguish None from Some(false) | capability | crates/rulisp-macros/src/lib.rs:228-229 (param) and :361-362 (result) compile errors; loader side v04.option-bool-rejected-by-the-loader (tests/suite/v04.lisp:174); BOUNDARY §11 |
| &[u8] parameters and Vec<u8> returns cross as (unsigned-byte 8) vectors (the :bytes type, since 0.2) | capability | v02.bytes-roundtrip (tests/suite/v02.lisp:20: result is `(simple-array (unsigned-byte 8) (*))`), v02.bytes-in, v03.bulk-bytes-identity; CHANGELOG 0.2.0; BOUNDARY §11 |
| Iterators/collections cross either as a callback (for_each_match) or as a handle wrapping the collection with accessor methods | capability | m5.callback-basic (FOR-EACH-WORD, tests/suite/m1.lisp:183); reproduced for_each_match; handle pattern used by examples/fetch (Req) and examples/wasm |
| `&mut self` is never exportable; use interior mutability | capability | trybuild crates/rulisp/tests/ui/mut_self.rs (+ .stderr); crates/rulisp-macros/src/lib.rs:1016 and :173 (&mut params); ROADMAP 'Non-goals'; BOUNDARY §5 |
| Concurrent calls on one handle are part of the thread contract | capability | BOUNDARY §7 / §5 (T: Sync supertrait, crates/rulisp/src/lib.rs:107); trybuild ui/non_send_handle.rs; m4h.thread-race (tests/suite/m4.lisp:63: 8 threads x 10k ops over a shared handle pool) |
| rulisp gives no crash isolation: a misbehaving native library can take the Lisp image down; run it out of process if you need isolation | capability | SECURITY.md:36-37 (out-of-scope: 'a Rust segfault or abort takes the image down'); BOUNDARY §8 (extern "C" shims: an escaped panic aborts the process) |
| In-process FFI trades isolation for ~50ns calls | performance | **resolved** — corrected in the audit to ~25 ns on SBCL — docs/benchmarks.md row "scalar call (add i64 i64)" |

## docs/usage.md

| Claim | Kind | Citation |
|---|---|---|
| rulisp has two entry points, `rulisp:load-crate` and `rulisp:use-crate`, one per situation | capability | lisp/src/package.lisp:5-8 (exports load-crate, use-crate; also reload-crate, load-blob-crate); lisp/src/crate.lisp:110, lisp/src/build.lisp:90 |
| Case A (prebuilt glue library) needs only Lisp + Quicklisp + rulisp — no Rust toolchain | capability | CI job 'SBCL / Linux x86-64 (required)', step 'load :rulisp with cargo stripped from PATH' (.github/workflows/ci.yml); test v02.blob-loading (tests/suite/v02.lisp:87); docs/stability.md §9 |
| Case B (your own glue crate) additionally needs cargo | capability | lisp/src/build.lisp:7-11 (find-cargo), 41-56 (%run-cargo-build); CI required jobs install dtolnay/rust-toolchain@stable before make test-m4 |
| use-crate = cargo build + load-crate; the .so case B builds is exactly what a case-A user is handed | capability | lisp/src/build.lisp:90-112 (use-crate: %run-cargo-build then load-crate); test v02.blob-loading (tests/suite/v02.lisp:87-102) copies the use-crate-built artifact and loads it via load-blob-crate |
| cargo build (via use-crate) produces target/debug/libmylib.so | capability | lisp/src/build.lisp:97,106-111 (target-dir = <crate>/target/, profile :dev → "debug", artifact-file-name); lisp/src/ffi.lisp:105-112 (lib prefix / .so) |
| load-crate dlopens the artifact, reads the embedded manifest and generates bindings, all at load time | capability | lisp/src/crate.lisp:139-171 (%load-crate-locked), 173-194 (%open-and-verify), 196-205 (%read-library-manifest), 248-324 (prepare/commit-bindings); tests m3.macro-crate-loaded (tests/suite/m3.lisp:45), fx.golden-manifest (tests/suite/m2.lisp:125) |
| REPL output `#<CRATE "mylib" gen 1 abi 1 :: 12 fns, 2 handles, package MYLIB>` is the crate's print form | capability | lisp/src/crate.lisp:33-41 (print-object: "~S gen ~D abi ~A :: ~D fns, ~D handle~:P, package ~A"); confirmed live: `#<RULISP:CRATE "rx" gen 1 abi 1 :: 7 fns, 1 handle, package RX>` |
| Blob naming convention `lib<name>-<os>-<arch>.<ext>`, e.g. `libwordbag-linux-x86_64.so` | capability | lisp/src/build.lisp:58-75 (host-blob-suffix, blob-file-name); .github/workflows/blobs.yml matrix (linux-x86_64/so, darwin-arm64/dylib); test v02.blob-loading (tests/suite/v02.lisp:87); v0.4.0 release asset `libwordbag-linux-x86_64.so` (gh release view) |
| On Windows the blob is named `<name>-windows-x86_64.dll` (no lib prefix) | capability | lisp/src/build.lisp:73-74; .github/workflows/blobs.yml windows-latest matrix row (lib: ''); v0.4.0 asset `wordbag-windows-x86_64.dll`; v02.blob-loading runs in the required 'SBCL / Windows x86-64' job (make test-m4) |
| `load-blob-crate` picks the right file for the host in one call, with a clear condition when the platform isn't covered | capability | lisp/src/build.lisp:77-88 (crate-not-loaded-error "no prebuilt artifact ~A for this platform (~A)"); test v02.blob-loading (tests/suite/v02.lisp:99-101 signals crate-not-loaded-error for a missing blob) |
| The four examples (wordbag, rx, wasm, fetch) ship as release blobs | capability | .github/workflows/blobs.yml (cargo build --release -p wordbag -p rx -p wasm -p fetch; release step asserts 12 assets); examples/ holds exactly those four; `gh release view v0.4.0` lists 12 assets |
| Blobs are built for Linux x86-64, macOS arm64 and Windows x86-64 | host | .github/workflows/blobs.yml matrix (ubuntu-latest/linux-x86_64, macos-latest/darwin-arm64, windows-latest/windows-x86_64); v0.4.0 asset names; README §Status (the three required SBCL hosts) |
| Release blobs are built by .github/workflows/blobs.yml and audited | capability | .github/workflows/blobs.yml step 'audit (BOUNDARY §7) and name for load-blob-crate' (tools/rulisp-audit.sh per artifact); docs/distribution.md 'Audit your glue crate'; BOUNDARY §12 §7 row |
| Downloading the blobs for your host into a directory is enough — Case A needs no Rust toolchain at all | capability | **resolved** — CI step "load a downloaded release asset with load-blob-crate" (SBCL / Linux job), added in the audit; macOS quarantine caveat added to the page |
| Not any Rust .so works: the library must be a rulisp glue crate built with #[rulisp::export] + rulisp::module!, which embeds the manifest the loader reads | capability | crates/rulisp-macros/src/lib.rs:1184-1208 (manifest static, per BOUNDARY §12); test fx.golden-manifest (tests/suite/m2.lisp:125); the embedded manifest read live from examples/rx (`(:rulisp-manifest :schema 1 …)`) |
| A non-rulisp library is refused with `rulisp:abi-mismatch-error` ("not a rulisp crate") | capability | lisp/src/crate.lisp:175-179 (%open-and-verify: abi-mismatch-error "~A is not a rulisp crate (no ~Aabi_version)"); BOUNDARY §12 §1 row |
| Arbitrary existing crates are not auto-bound; someone writes the glue crate once | capability | ROADMAP.md 'Non-goals': auto-binding arbitrary existing crates; DESIGN.md §1 |
| A Linux x86-64 .so won't load on macOS: rulisp's :target check refuses a mismatched artifact with a clear condition instead of an obscure dlopen error | capability | **resolved** — corrected in the audit: a binary the OS cannot map fails at dlopen (crate-not-loaded-error); a mapped one built for another target is refused by the :target check — test fx.target-check |
| `use rulisp::prelude::*` is the import for glue crates | capability | crates/rulisp/src/lib.rs:59-60 (pub mod prelude { export, handle, module, Callback, Error, StoredCallback }); CI 'cargo tests (golden manifest + trybuild)' compiles examples using it |
| `#[rulisp::export] pub fn greet(name: &str) -> String` is a valid export (a `&str` param and `String` result cross) | capability | examples/wordbag/src/lib.rs (greet with the identical signature); tests m3.strings (tests/suite/m1.lisp:102), m6.reload (m1.lisp:234, GREET → "Hello, W!"); BOUNDARY §12 §4 rows |
| `rulisp::module! { name: "mylib", handles: [], fns: [greet] }` is the registry grammar (empty handles allowed) | capability | crates/rulisp-macros/src/lib.rs:1260-1265 (keys name/handles/fns/on_dump); trybuild case crates/rulisp/tests/ui/module_typo.rs (uses `handles: []`, a typo in fns is a compile error) |
| Rebuild-and-reload with a second use-crate call, without restarting the REPL | capability | test m6.reload (tests/suite/m1.lisp:234-246: use-crate twice, generation+1, new behaviour visible); lisp/src/crate.lisp:116-117 ('Loading an already-loaded crate bumps its generation') |
| `(mylib:greet "world")` returns "Hello, world!" | capability | examples/wordbag/src/lib.rs greet body `format!("{GREETING}, {name}!")`; test m6.reload asserts "Hello, W!" (tests/suite/m1.lisp:237) |
| After a reload the crate is at generation 2 and old handles refuse politely | capability | tests m6.reload (tests/suite/m1.lisp:240-242: stale-handle-error, then free succeeds) and m6.captured-wrapper-gate (m1.lisp:248); lisp/src/handle.lisp:30-52 (generation gate); lisp/src/conditions.lisp:37-45 (stale-handle-error report) |
| A failed build signals `rulisp:build-error` carrying cargo's stderr, with a `retry-build` restart | capability | lisp/src/build.lisp:49-56 (build-error :stderr err), 99-105 (restart-case retry-build); lisp/src/conditions.lisp:56-61; lisp/src/package.lisp exports build-error, build-error-stderr, retry-build |
| Stored callbacks run the closure on whatever thread Rust invokes from | capability | test v02.stored-callback-cross-thread (tests/suite/v02.lisp:118-137; Rust-spawned thread, SBCL and CCL); BOUNDARY §6 'Stored callbacks' + §12 row 'Rust may invoke from any thread' |
| `rulisp:callback` wraps a Lisp closure into a token a stored-callback export accepts | capability | lisp/src/package.lisp:12-14 (callback, unregister-callback, callback-token); tests v02.stored-callback-basic (tests/suite/v02.lisp:105), v02.stored-callback-token-type-check (v02.lisp:179) |
| crates.io hosts `rulisp`, `rulisp-macros`, `rulisp-runtime`, fetched by cargo at compile time | capability | crates.io API: rulisp 0.4.0 (versions 0.1.0–0.4.0); crates/rulisp/Cargo.toml:14-15 pins rulisp-macros/rulisp-runtime 0.4.0 (a published `rulisp` requires them published); docs/distribution.md 'Publishing rulisp itself' |
| The runtime is statically linked into each glue .so | capability | BOUNDARY.md §1 ('Each cdylib statically links its own copy of rulisp-runtime'); test v04.last-error-is-per-library (tests/suite/v04.lisp:186) proves per-library TLS/allocator; BOUNDARY §12 §1 row |
| The `rulisp` ASDF system is served by Ultralisp/Quicklisp | stale | **resolved** — corrected in the audit: Ultralisp (Quicklisp at 1.0) |
| The glue .so carries everything Rust-side, so case A needs no Rust toolchain | capability | CI 'SBCL / Linux x86-64 (required)' step 'load :rulisp with cargo stripped from PATH'; test v02.blob-loading; docs/stability.md §9; BOUNDARY §1 (static runtime) |
| Every generated function and handle class carries a docstring | capability | test v05.docstrings-present (tests/suite/v05.lisp:127-147); lisp/src/codegen.lisp:378-405 (synthesize-fn-doc, synthesize-handle-doc); confirmed live on rx (function and class docstrings present) |
| A crate answers `describe` | capability | test v05.describe-crate (tests/suite/v05.lisp:170-175); lisp/src/crate.lisp:333-356 (describe-object) |
| REPL transcript: `(documentation 'rx:make-regex 'function)` returns exactly the three lines "(rx:make-regex pattern) / Rust: Regex::new(pattern: :string) -> rx:regex, Err(Error) / Signals: rx:rust-error (a rulisp:rust-error) on Err." | capability | **resolved** — transcript regenerated from the loader in the audit (the /// paragraph leads) — test v05.docstrings-present |
| A failed `rx:make-regex` signals `rx:rust-error` (a `rulisp:rust-error`) | capability | **resolved** — loader fixed in the audit: synthesize-fn-doc names rulisp:rust-error for a type named Error — test v05.docstrings-present |
| `(describe crate)` starts with `#<RULISP:CRATE "rx" gen 1 ...> is a rulisp crate.` | capability | lisp/src/crate.lisp:338; live output `#<RULISP:CRATE "rx" gen 1 abi 1 :: 7 fns, 1 handle, package RX> is a rulisp crate.`; test v05.describe-crate searches "rulisp crate" |
| describe prints `Package: RX` and `Generation: 1 (session 0)` | capability | lisp/src/crate.lisp:339-340; lisp/src/handle.lisp:12 (*session* starts at 0); live output identical |
| describe prints `Artifact: .../examples/rx/target/debug/librx.so` | capability | lisp/src/crate.lisp:341 (crate-source-path); live output `/home/…/examples/rx/target/debug/librx.so` |
| describe prints `Exports (5):` for examples/rx | capability | **resolved** — transcript regenerated in the audit (Exports (7)) |
| describe lists each export as its signature, first `(rx:make-regex pattern)` | capability | **resolved** — loader fixed in the audit: describe prints the call shape (%call-shape) for every export — test v05.describe-crate |
| A `///` comment on an exported fn or a #[rulisp::handle] struct leads its docstring; the macros carry it in the manifest as `:doc` | capability | tests v05.docstrings-present (tests/suite/v05.lisp:141-147: SLOW-SUM and GRENADE `///` text found) and v05.doc-escaping (v05.lisp:149); crates/rulisp-macros/src/lib.rs:53 (doc_of), 761, 877, 1059, 1179; tests/golden/wordbag.manifest.sexp carries 9 :doc keys; rx manifest read live shows :doc on three fns |

## docs/distribution.md

| Claim | Kind | Citation |
|---|---|---|
| every claim on this page is cited in docs/stability.md's claims register | stale | **resolved** — true since the audit: this file |
| Quicklisp cannot practically accept a cargo-requiring system: the dist build machine compiles every candidate with ASDF; a .asd that shells out to cargo fails there | capability | **resolved** — external Quicklisp dist policy (DESIGN.md §11 research record); rulisp's own system needs no cargo to load — CI step "load :rulisp with cargo stripped from PATH" |
| Target Ultralisp or ocicl instead (both distribute source; your users build locally) | capability | **resolved** — external; the page now says ocicl is untried here |
| Developers using rulisp:use-crate have cargo by definition | capability | lisp/src/build.lisp:7-10 (find-cargo), :41-56 (%run-cargo-build), :90-108 (use-crate runs cargo build first) |
| The Shirakumo pattern (cl-mixed et al.): build the cdylib in CI per platform, commit or attach the artifacts under names like static/libmycrate-lin-amd64.so | capability | **resolved** — external convention (cl-mixed); the page now points at rulisp's own naming and load-blob-crate — test v02.blob-loading |
| load with rulisp:load-crate picking the file by (uiop:architecture)/(uiop:operating-system) | capability | lisp/src/crate.lisp:106-116 (load-crate accepts any artifact path); lisp/src/manifest.lisp:106-120 (the same uiop predicates in target-compatible-p); docs/usage.md Case A |
| macOS Gatekeeper quarantines downloaded dylibs — blobs fetched over the network at load time will be refused; committed-in-repo blobs installed via git/quicklisp-style tarballs are fine | capability | **resolved** — external Gatekeeper behaviour (DESIGN.md research record); no CI job downloads a blob on macOS |
| musl hosts need separate artifacts built with -C target-feature=-crt-static | host | **resolved** — external Rust/musl fact; untested here |
| The manifest :target check will refuse a wrong-platform artifact with a clear condition instead of a confusing dlopen error | capability | **resolved** — corrected in the audit: dlopen failure first, then the :target check — test fx.target-check |
| Deploy discovers every open foreign library, copies them next to the binary at asdf:make time, closes before the image dump and reopens at boot | capability | **resolved** — third-party Deploy behaviour; the page now says the recipe is not built in CI |
| rulisp's model is 'regenerate the bindings from whichever artifact we load now' — its own restore hook bumps the session counter and re-runs load-crate | capability | m7.dump-restore (tests/suite/m1.lisp:274); lisp/src/crate.lisp:391-407 (%restore-all-crates: incf *session* precedes %load-crate-locked), :431 (uiop:register-image-restore-hook); BOUNDARY.md §12 §10 rows |
| Let rulisp own the reload and tell Deploy to leave the artifact alone (deploy-op system + load-blob-crate from an image-restore hook) | capability | **resolved** — reworded in the audit: keep the crate unloaded during asdf:make and load it from the restore hook; not built in CI |
| Crates that own threads MUST be quiesced before the dump: save-lisp-and-die does not see foreign threads and will happily dump with them running (BOUNDARY.md §10) | capability | BOUNDARY.md:209-214; BOUNDARY.md §12 row 'No guardrail against dumping with live foreign threads' (UB-by-design); fetch.dump-restore-refuses (tests/suite/fetch.lisp:499) dumps with a tokio transfer live under the hook |
| mark a zero-arg export as the crate's dump hook and the loader wires it up | capability | v04.on-dump-declared-hook-runs (tests/suite/v04.lisp:222); crates/rulisp-macros/src/lib.rs:1260 (on_dump key), :1339-1355 (zero-arg guard); lisp/src/crate.lisp %commit-generation resolves on-dump-ptr, :409-428 (%run-crate-dump-hooks) |
| Snippet grammar: `on_dump: shutdown_all` in rulisp::module!, naming a #[rulisp::export] zero-arg fn that is also listed in fns | capability | examples/fetch/src/lib.rs:767-796 (identical shape); crates/rulisp-macros/src/lib.rs:1265 (key list), :1345 ('on_dump must name a free fn that also appears in fns'); loader side v04.on-dump-manifest-validation (tests/suite/v04.lisp:240); manifest.lisp:172 (%validate-on-dump) |
| The loader calls every loaded crate's declared hook in load order, immediately before any uiop:dump-image-driven dump | capability | lisp/src/crate.lisp:91 (*crate-load-order*), :409-428, :430 (uiop:register-image-dump-hook); v04.on-dump-declared-hook-runs; fetch.dump-restore-refuses (a real uiop:dump-image on SBCL/CCL) |
| a failing hook is warned and skipped | capability | v04.on-dump-failing-hook-warns-and-proceeds (tests/suite/v04.lisp:229); lisp/src/crate.lisp:409-428 |
| examples/fetch is the worked example: its hook stops every live tokio runtime; the suite dumps an image with a request in flight and restores it | capability | examples/fetch/src/lib.rs:767-781 (shutdown_all), :795 (on_dump); fetch.dump-hook-quiesces (tests/suite/fetch.lisp:466), fetch.dump-restore-refuses (:499); CI ci.yml:66 (SBCL/Linux required) and :110 (CCL/Linux required) run make test-fetch |
| For a crate WITHOUT the declaration, keep the manual pattern: uiop:register-image-dump-hook calling the crate's shutdown export | capability | BOUNDARY.md:228-230 (normative text); lisp/src/crate.lisp:430 uses the same uiop hook API |
| Name the artifact lib<name>-<os>-<arch>.<ext> (<name>-windows-x86_64.dll on Windows) — what load-blob-crate looks for | capability | v02.blob-loading (tests/suite/v02.lisp:87); lisp/src/build.lisp:58-75 (host-blob-suffix, blob-file-name); blobs.yml:42-44 (lib '' on Windows) |
| what .github/workflows/blobs.yml attaches to each GitHub Release for the four examples | capability | .github/workflows/blobs.yml:65-68 (wordbag rx wasm fetch), :101 (expects 12 assets = 3 hosts × 4 crates), :104 (gh release upload); docs/releasing.md steps 6-7 |
| SBCL refuses to dump with several Lisp threads but cannot see the ones a glue crate spawned, so the dump succeeds and the threads are simply gone in the restored image | capability | BOUNDARY.md:209-214 and §12 UB-by-design row; probe on SBCL 2.1.11: save-lisp-and-die with a second Lisp thread → "Can't save image with more than one executing thread"; fetch.dump-restore-refuses dumps with tokio workers live |
| ECL has no image dump: (uiop:dump-image … :executable t) signals 'Dumping an executable is not supported on this implementation! Aborting.' | host | tests/suite/m1.lisp:282 and tests/suite/fetch.lisp:505 (#-(or sbcl ccl) skips citing this); probe on ECL 21.2.1 / ASDF 3.1.8.8 reproduces the exact message; ROADMAP.md v0.4 item 4 ('verified') |
| An ECL application ships as an asdf:program-op executable instead, and rulisp works unchanged inside one | capability | CI job 'ECL / Linux (required)' step 'program-op executable (the ECL deployment story)' (.github/workflows/ci.yml:186-187) = make test-ecl-program (Makefile:38-46); tests/ecl-program/ |
| tests/ecl-program/ is the smallest such consumer, built and run by the ECL CI job (make test-ecl-program) | capability | .github/workflows/ci.yml:186-187; Makefile:38-46; tests/ecl-program/rulisp-ecl-smoke.asd, build.lisp, main.lisp |
| build.lisp recipe: ecl --norc --load build.lisp; the script loads Quicklisp (rulisp's dependencies come from there), pushes rulisp's lisp/ and the app dir onto asdf:*central-registry*, loads the dependencies, calls (asdf:make "my-app") | capability | tests/ecl-program/build.lisp:3-19; Makefile:41 (`$(ECL) --norc --load tests/ecl-program/build.lisp`) |
| The executable lands next to the .asd | capability | tests/ecl-program/rulisp-ecl-smoke.asd (:build-pathname "rulisp-ecl-smoke"); Makefile:44 runs tests/ecl-program/rulisp-ecl-smoke; the binary is present there after the target |
| all verified against ECL 21.2.1 as packaged by Debian/Ubuntu (bundled ASDF 3.1.8.8) | host | .github/workflows/ci.yml:170-175 (ubuntu-latest, apt-get install ecl); README.md §Status (ECL 21.2.1 required); local probe: `ecl --version` = ECL 21.2.1, (asdf:asdf-version) = "3.1.8.8" |
| ASDF must exist at startup: cffi's compiled code references ASDF packages at object-load time (its lazy cffi-libffi loader), and rulisp calls UIOP at runtime | capability | cffi src/functions.lisp:104-109 (*foreign-structures-by-value* initform's restart calls asdf:operate 'asdf:load-op 'cffi-libffi); lisp/rulisp.asd :depends-on uiop; lisp/src/crate.lisp uses uiop:copy-file / uiop:xdg-cache-home; tests/ecl-program/rulisp-ecl-smoke.asd:7-9; positive path exercised by make test-ecl-program |
| ASDF's default on ECL is to link cmp — and uiop/asdf when it finds a prebuilt one — statically into the program | capability | ASDF source, component-depends-on (program-op): `(unless (no-uiop c) (list (linkable-system "cmp") … (linkable-system "uiop") (linkable-system "asdf")))` (~/quicklisp/asdf.lisp:11344-11350); tests/ecl-program/rulisp-ecl-smoke.asd:9-14 |
| The distro package ships only the .fas files, neither libcmp.a nor libasdf.a, yet its cmp.asd still declares :lib #P"SYS:LIBCMP.A" | host | local Ubuntu ecl 21.2.1 (the package ci.yml:175 installs): /usr/lib/x86_64-linux-gnu/ecl-21.2.1/ contains no *.a, and cmp.asd:3 reads `:lib #P"SYS:LIBCMP.A"`; tests/ecl-program/rulisp-ecl-smoke.asd:10-12 |
| so the link fails with ld: cannot find …/ecl-21.2.1/libcmp.a | capability | **resolved** — observed by hand on ECL 21.2.1 (Debian) while building tests/ecl-program; the positive path is CI step make test-ecl-program, the failure itself is not reproduced by CI |
| :no-uiop t turns the static link off and the prologue requires asdf.fas before any linked module initializes | capability | tests/ecl-program/rulisp-ecl-smoke.asd:18-19; ASDF source ~/quicklisp/asdf.lisp:11345 (no-uiop skips the cmp/asdf link); CI make test-ecl-program; ecl-program-build.log shows asdf.fas loaded |
| The executable already needs the ECL runtime (libecl.so, per ldd) from the same installation, so this adds no new deployment dependency | host | `ldd tests/ecl-program/rulisp-ecl-smoke` → libecl.so.21.2; tests/ecl-program/rulisp-ecl-smoke.asd:15-17 |
| With :no-uiop, ASDF stops wiring the entry point too — the program would fall through into ECL's REPL; :entry-point does nothing under :no-uiop | capability | ASDF create-image: `(if no-uiop epilogue-code (append … (setf *image-entry-point* ',entry-point) …))` (~/quicklisp/asdf.lisp:4800-4813); tests/ecl-program/rulisp-ecl-smoke.asd:20-24 |
| Call main from the epilogue (interned at runtime; the package does not exist when the .asd is read) and end it with uiop:quit | capability | tests/ecl-program/rulisp-ecl-smoke.asd:24; tests/ecl-program/main.lisp:16,36 (uiop:quit); CI make test-ecl-program |
| Bindings are generated at load time by design, and on ECL compile goes through the C compiler | capability | lisp/src/crate.lisp:292-297 (prepare-bindings: (compile nil form) per wrapper); probe on ECL 21.2.1: `compile` autoloads cmp.fas and strace shows /usr/bin/gcc execve'd (8 times for two compile calls) |
| a crate with callbacks additionally native-compiles its trampolines (BOUNDARY §7) | capability | v04.ecl-toolchain-failure-is-named (tests/suite/v04.lisp:156); lisp/src/codegen.lisp:89-118 (#+ecl compile-file branch); BOUNDARY.md:158-164 (§7 ECL specifics) and §12 row |
| strace shows a gcc invocation per generated wrapper | performance | probe: strace -f -e execve on `ecl --norc --eval "(compile nil '(lambda (x) x))"` ×2 → 8 successful execve("/usr/bin/gcc") (compile + link steps, 4 per compile) |
| The deployed machine therefore needs gcc, exactly as the REPL does | capability | lisp/src/crate.lisp:292-297 + codegen.lisp:89-118; v04.ecl-toolchain-failure-is-named; README.md §Status ('a C toolchain is required for callbacks') |
| load-crate copies the artifact under $XDG_CACHE_HOME/rulisp/ (default ~/.cache/rulisp/) | capability | lisp/src/crate.lisp:95-98 (cache-directory = uiop:xdg-cache-home "rulisp/"), :151 (uiop:copy-file); v04.cache-sweep-spares-other-processes-fresh-copies works in cache-directory; probe: (uiop:xdg-cache-home "rulisp/") → ~/.cache/rulisp/ |
| With HOME unset the cache resolves to /.cache and the program fails with `Could not create directory "/.cache" … Permission denied`; setting XDG_CACHE_HOME alone is enough | capability | lisp/src/crate.lisp:95-98 (ensure-directories-exist on the xdg dir); probe `env -i /usr/bin/ecl`: path #P"/.cache/rulisp/", error `Could not create directory "/.cache"` / `C library error: "Permission denied"`; with only XDG_CACHE_HOME set the XDG path is used |
| rulisp keeps a successful run quiet (*compile-verbose* is bound off around its own compiles) | capability | lisp/src/crate.lisp:292 and lisp/src/codegen.lisp:107 bind *compile-verbose*/*compile-print* to nil; CHANGELOG.md:171; ecl-program-run.log holds only the two expected lines |
| when gcc is missing, ECL itself prints `;;; Internal error: ** Error code 1 when executing (EXT:RUN-PROGRAM "gcc" …)` before load-crate signals | capability | probe `PATH=/nonexistent /usr/bin/ecl … (compile …)` prints exactly `;;; Internal error:` / `** Error code 1 when executing` / `(EXT:RUN-PROGRAM "gcc" …)`; the signal is v04.ecl-toolchain-failure-is-named (manifest-error 'callbacks on ECL require the native compiler', codegen.lisp:114-118) |
| With bundled ASDF 3.1.8.8, (asdf:make) from a cold output cache dies with COMPILE-FILE-ERROR … WARN-IF-KW-OR-BELONGS-TO-CL is undefined: program-op compiles a dependency's files without loading the earlier ones | capability | **resolved** — observed by hand on ECL 21.2.1 (Debian); tests/ecl-program/build.lisp carries the workaround, the failure is not reproduced by CI |
| dies about two minutes in | performance | **resolved** — number removed in the audit |
| A rerun on that half-built cache fails faster (There exists no package with name "CFFI") | capability | **resolved** — observed by hand on ECL 21.2.1 (Debian); not reproduced by CI |
| (ql:quickload '(:cffi :babel :trivial-garbage :bordeaux-threads)) — or (asdf:load-system "rulisp") — before asdf:make sidesteps it | capability | tests/ecl-program/build.lisp:17 (the quickload form) + CI make test-ecl-program |
| Load the glue artifact from main with rulisp:load-crate / rulisp:load-blob-crate | capability | tests/ecl-program/main.lisp:19 (load-crate); CI make test-ecl-program |
| The crate's package (WORDBAG — the manifest crate name, upcased) exists only after that call, so main.lisp cannot name wordbag:greet literally; reach the bindings late-bound with find-symbol (or uiop:symbol-call) | capability | lisp/src/crate.lisp:164-169 (ensure-crate-package (string-upcase canonical)); tests/ecl-program/main.lisp:20-23 (find-symbol) |
| There is no pre-dump state, so rulisp's image-restore hook has nothing to invalidate | capability | tests/ecl-program/main.lisp:19 loads the crate at run time (the linked image holds no crate, *crates* is empty); lisp/src/crate.lisp:391-407 iterates *crates*, :431 |
| Several instances may share one cache: each copy is named with a per-process tag, and the sweep leaves other processes' fresh copies alone | capability | v04.cache-copy-names-are-process-unique (tests/suite/v04.lisp:277), v04.cache-sweep-spares-other-processes-fresh-copies (:287); lisp/src/crate.lisp:62-90, :358-375; CHANGELOG.md:159 |
| an error that reaches its debugger with stdin at EOF exits 0 | host | probe: a program-op executable built with the same .asd shape whose main signals an unhandled error, run </dev/null, exits 0; `echo '(error "boom")' \| ecl --norc` exits 0; Makefile:34-36 and ci.yml:181-185 gate on markers for this reason |
| a memory fault instead aborts with exit 134 and a core dump | host | **resolved** — observed by hand on ECL 21.2.1 (Debian); not reproduced by CI (Makefile gates on the printed marker for this reason) |
| a wrapper script or CI step must gate on a printed marker, never on the exit code alone | capability | Makefile:42,46 (BUILD-DONE / ECL-PROGRAM-OK greps); .github/workflows/ci.yml:184-185 ('Pass: ' grep) |
| BOUNDARY.md §7 asks every glue author to keep signal handlers out of the dependency graph | capability | BOUNDARY.md:171-177 ('Audit glue-crate dependencies for sigaction') |
| tools/rulisp-audit.sh ARTIFACT [CRATE_DIR] fails if the artifact imports a signal-disposition symbol (sigaction, signal, sigprocmask, …), if tokio's signal or process features are on, or if block_on appears in the glue sources | capability | tools/rulisp-audit.sh:16 (SYMS), :36-40 (import sweep), :44-52 (tokio features, block_on); tools/rulisp-audit-selftest.sh (pattern both ways, fixture rejection); CI ci.yml:31 |
| make audit runs it over every example in CI, after a self-test that builds a library which deliberately imports signal() and requires the audit to reject it | capability | Makefile:50-54; CI job 'cargo tests (golden manifest + trybuild)' step 'BOUNDARY §7 signal audit over every example (with self-test)' (ci.yml:31-32); tools/rulisp-audit-selftest.sh:24-31; tools/audit-fixture/src/lib.rs |
| Linux and macOS; on Windows it prints a SKIP line (dumpbin /imports is the manual equivalent) | host | tools/rulisp-audit.sh:31-35; .github/workflows/blobs.yml:6-7,67 (runs on windows-latest → SKIP); BOUNDARY.md:363 (GAP on Windows row) |
| rulisp:use-crate is the dev path: cargo build + load, with rulisp:build-error carrying cargo's stderr and a retry-build restart | capability | lisp/src/build.lisp:90-108 (restart-case retry-build), :41-56 (build-error :stderr); lisp/src/conditions.lisp (build-error, build-error-stderr); lisp/src/package.lisp exports retry-build |
| Publish your glue crate to crates.io so cargo can fetch it, and your ASDF system to Ultralisp/ocicl | capability | **resolved** — a recommendation, not a claim |
| cargo publish order: rulisp-runtime, rulisp-macros, rulisp (dependencies first) | capability | crates/rulisp/Cargo.toml:14-15 (depends on both); crates/rulisp-macros/Cargo.toml:16-19 and crates/rulisp-runtime/Cargo.toml:13 (no intra-workspace deps); docs/releasing.md:29-31 (same order) |
| The ASDF system (lisp/rulisp.asd) goes to Ultralisp (GitHub app or manual project registration) | capability | docs/installation.md:97 ('rulisp is on Ultralisp'); docs/releasing.md:44 (step 8, checked with ql:update-dist) |
| and/or ocicl | stale | **resolved** — ocicl removed in the audit |

## docs/benchmarks.md

| Claim | Kind | Citation |
|---|---|---|
| Numbers quoted anywhere in this repository trace to this page. | capability | **resolved** — reworded in the audit: the user-facing pages (DESIGN.md estimates are history) |
| The rule (docs/design/v03-async-plan.md §7): if a number ships, it comes from a release build with a documented method — otherwise no number ships. | capability | docs/design/v03-async-plan.md §7, bullet "A release-profile benchmark suite" |
| `make bench` runs `tests/bench.lisp` on SBCL. | capability | Makefile target `bench:` — `$(SBCL) --non-interactive --load tests/bench.lisp` |
| The other hosts run the same file: `~/ccl/lx86cl64 --batch --load tests/bench.lisp`, `ecl --norc --load tests/bench.lisp`. | capability | This audit ran both commands to `;;; done` (exit 0); shapes match Makefile `CCL ?= $(HOME)/ccl/lx86cl64` + `--batch --load` (test-ccl) and ci.yml ecl-linux `ecl --norc --load` |
| It builds `examples/wordbag` and `examples/rx` with `(rulisp:use-crate … :profile :release)`. | capability | tests/bench.lisp:22-24 (wordbag), :65-67 (rx); lisp/src/build.lisp:46 passes `--release` for :profile :release, :109 picks target/release |
| Times each boundary path with `get-internal-real-time` over enough iterations to amortize the clock (ns/call = elapsed / N). | capability | tests/bench.lisp `timed` macro (:29-37) |
| The Lisp side is the host's default compilation policy; nothing is tuned. | capability | **resolved** — reworded in the audit: the loader's typed ASCII loops carry (optimize speed) — lisp/src/ffi.lisp |
| Numbers vary by machine and are not a CI gate. | capability | `grep -ri bench .github/workflows/` is empty; no ci.yml job calls `make bench`; m0.scalar-and-bench (tests/suite/m1.lisp:39-51) only bounds a scalar call below 20000 ns |
| The shape should hold: scalar calls in tens of nanoseconds on SBCL, byte and vector transfers scaling linearly at memcpy speed, a borrowed callback a small multiple of a call. | performance | bench rows 35 (25 ns), 46/48/50 (550 ns → 22.5 µs → 319.5 µs for 1 KiB → 64 KiB → 1 MiB), 55 (390 ns/invocation); m0.scalar-and-bench |
| Host: GitHub-hosted-class VM, Intel Xeon @ 2.20 GHz, 4 vCPU (2 threads/core), 15 GiB. | host | lscpu on the audit machine: "Intel(R) Xeon(R) CPU @ 2.20GHz", CPU(s) 4, Thread(s) per core 2; `free -g` 15 GiB; kernel 6.8.0-1065-gcp |
| Lisp: SBCL 2.1.11 (Debian); Clozure CL 1.13; ECL 21.2.1 (Debian). | host | `sbcl --version` → SBCL 2.1.11.debian (dpkg 2:2.1.11-1); `~/ccl/lx86cl64 --version` → 1.13; `ecl --version` → 21.2.1 (dpkg 21.2.1+ds-4); ci.yml:101 pins ccl-1.13, :175 apt ecl |
| OS: Linux 6.8 (x86-64, glibc). | host | `uname -srm` → Linux 6.8.0-1065-gcp x86_64; `ldd --version` → glibc 2.35 |
| One host at a time, each run alone (the same tests/bench.lisp; the SBCL column is `make bench`). | capability | Makefile `bench:`; this audit reproduced the three columns sequentially with the same file |
| Columns: SBCL 2.1.11 \| CCL 1.13 \| ECL 21.2.1 (the benchmarked hosts). | host | ci.yml jobs sbcl-linux, ccl-linux, ecl-linux — all "(required)"; docs/stability.md §5 |
| scalar call (add i64 i64): 25 ns / 36 ns / 1.2 µs. | performance | tests/bench.lisp "scalar call (add i64 i64)" (wordbag ADD); audit re-run: 25.0 / 37.0 / 1175 ns |
| scalar call, no args: 25 ns / 33 ns / 795 ns. | performance | tests/bench.lisp "scalar call, no args" (TEST-LIVE-WORD-BAGS); audit re-run: 25.0 / 32.1 / 680 ns |
| handle method (gate + call): 275 ns / 1.0 µs / 2.9 µs. | performance | tests/bench.lisp "handle method (gate + call)" (WORD-BAG-LEN); audit re-run: 235 ns / 1030 ns / 2720 ns |
| handle create + free: 650 ns / 8.6 µs / 14.7 µs. | performance | tests/bench.lisp "handle create + free" (MAKE-WORD-BAG + rulisp:free); audit re-run: 800 ns / 8200 ns / 14950 ns |
| string round trip (8 B ASCII): 600 ns / 5.0 µs / 10.3 µs. | performance | tests/bench.lisp "string round trip (8 B ASCII)" (ECHO); audit re-run: 600 ns / 5013 ns / 11600 ns |
| string round trip (1024 B ASCII): 11.1 µs / 36.8 µs / 74.0 µs. | performance | tests/bench.lisp "string round trip (1024 B ASCII)"; audit re-run: 10.7 / 37.8 / 70.6 µs |
| string round trip (65536 B ASCII): 317.6 µs / 1.55 ms / 4.29 ms. | performance | tests/bench.lisp "string round trip (65536 B ASCII)"; audit re-run: 305 µs / 1505 µs / 3864 µs; commit 43f3c9c body "1015 -> 318 us (acceptance <= 450)" |
| string round trip (1 KiB non-ASCII): 38.8 µs / 103.5 µs / 3.93 ms. | performance | tests/bench.lisp "string round trip (1 KiB non-ASCII)" (1024 × #\한); audit re-run: 38.5 / 99.7 / 3337 µs |
| rx count over 1 MiB (&str in, 1 match): 2.31 ms / 11.83 ms / 32.28 ms. | performance | tests/bench.lisp:65-77 "rx count over 1 MiB (&str in, 1 match)" (RX REGEX-COUNT); audit re-run: 2.33 / 10.45 / 30.5 ms |
| bytes in (sum, 8 B): 200 ns / 1.9 µs / 2.8 µs. | performance | tests/bench.lisp "bytes in (sum, 8 B)" (SUM); audit re-run: 250 ns / 1977 ns / 2400 ns |
| bytes round trip (rev, 8 B): 500 ns / 9.1 µs / 7.9 µs. | performance | tests/bench.lisp "bytes round trip (rev, 8 B)" (REV); audit re-run: 500 ns / 3512 ns / 6700 ns |
| bytes in (sum, 1024 B): 550 ns / 8.6 µs / 2.2 µs. | performance | tests/bench.lisp "bytes in (sum, 1024 B)"; audit re-run: 500 ns / 2211 ns / 2550 ns |
| bytes round trip (rev, 1024 B): 1.1 µs / 15.3 µs / 8.6 µs. | performance | tests/bench.lisp "bytes round trip (rev, 1024 B)"; audit re-run: 1000 ns / 4357 ns / 7850 ns |
| bytes in (sum, 65536 B): 22.5 µs / 28.3 µs / 22.9 µs. | performance | tests/bench.lisp "bytes in (sum, 65536 B)"; audit re-run: 19.6 / 25.0 / 22.65 µs |
| bytes round trip (rev, 65536 B): 44.1 µs / 63.4 µs / 70.5 µs. | performance | tests/bench.lisp "bytes round trip (rev, 65536 B)"; audit re-run: 43.1 / 56.0 / 67.2 µs |
| bytes in (sum, 1048576 B): 319.5 µs / 504.8 µs / 365.0 µs. | performance | tests/bench.lisp "bytes in (sum, 1048576 B)" (N=2000); audit re-run: 316 / 413 / 323 µs |
| bytes round trip (rev, 1048576 B): 702.0 µs / 873.1 µs / 1.67 ms. | performance | tests/bench.lisp "bytes round trip (rev, 1048576 B)"; audit re-run: 695 / 822 / 1536 µs |
| vec i64 round trip (8 elts): 600 ns / 6.0 µs / 8.4 µs. | performance | tests/bench.lisp "vec i64 round trip (8 elts)" (DELTAS); audit re-run: 600 ns / 5599 ns / 7300 ns |
| vec i64 round trip (1024 elts): 10.6 µs / 10.6 µs / 16.4 µs. | performance | tests/bench.lisp "vec i64 round trip (1024 elts)"; audit re-run: 10.4 / 9.0 / 12.1 µs |
| vec i64 round trip (65536 elts): 191.0 µs / 304.3 µs / 744.5 µs. | performance | tests/bench.lisp "vec i64 round trip (65536 elts)" (N=2000); audit re-run: 189 / 288 / 672 µs |
| borrowed callback (100 invocations): 39.0 µs / 235.3 µs / 481.5 µs. | performance | tests/bench.lisp:96-104 "borrowed callback (100 invocations)" (FOR-EACH-WORD over a 100-word bag); audit re-run: 41.5 / 236.5 / 459 µs |
| stored callback (same thread): 170 ns / 610 ns / 1.6 µs. | performance | tests/bench.lisp:106-111 "stored callback (same thread)" (SET-NOTIFIER + NOTIFY); audit re-run: 160 ns / 615 ns / 1330 ns |
| On SBCL a boundary call costs about 25 ns. | performance | rows 35-36 SBCL; audit re-run 25.0 ns; m0.scalar-and-bench prints the same measurement |
| The handle gate (session and generation checks, in-flight counting) adds ~250 ns. | performance | row 37 − row 35 = 275 − 25 = 250 ns; gate mechanism lisp/src/handle.lisp:30-52 `cell-begin-call` (session check :40, generation :42, in-flight incf :45); BOUNDARY §12 rows for §5/§10; tests m6.captured-wrapper-gate, m7.dump-restore |
| Create+free is dominated by the Rust allocation and the finalizer registration. | performance | **resolved** — softened in the audit: "includes", not "dominated by" |
| CCL is close on scalars. | performance | rows 35-36: CCL 36/33 ns vs SBCL 25 ns; audit re-run 37/32 ns |
| ECL pays ~1 µs per call in its CFFI backend. | performance | rows 35-36: ECL 1.2 µs / 795 ns; audit re-run 1175 / 680 ns |
| ECL's finalizer registration is the costliest of the three. | performance | row 38: ECL 14.7 µs vs CCL 8.6 µs vs SBCL 650 ns; audit re-run 14.95 / 8.2 / 0.8 µs |
| "Host note (CCL). These are SBCL numbers." | stale | **resolved** — sentence removed in the audit (the table has three host columns) |
| On CCL the inbound side is a memcpy into a heap buffer rather than a zero-copy borrow. | capability | **resolved** — sentence removed in the audit (the table has three host columns) |
| CCL's vector pin is `without-gcing` and a whole-call pin would stop every other thread's collections. | host | CHANGELOG.md:74-82 ("measured: 0 collections completed on another thread during a 600 ms borrow; ~30 unpinned"); commit 33a3f22 body; lisp/src/ffi.lisp:154-158; BOUNDARY §4; test v05.pin-does-not-stop-the-world on SBCL/CCL/ECL |
| Expect the CCL inbound rows to cost roughly one extra memcpy. | performance | row 50: CCL 504.8 µs vs SBCL 319.5 µs (+185 µs for 1 MiB); row 48: 28.3 vs 22.5 µs |
| 1 MiB in at 320 µs is ~3.3 GB/s on SBCL. | performance | row 50 SBCL 319.5 µs → 1048576 B / 319.5 µs = 3.28 GB/s; audit re-run 316 µs |
| ECL is within 15%; CCL pays its extra memcpy. | performance | row 50: ECL 365.0 / 319.5 = +14.2%; CCL 504.8 = +58%; audit re-run ECL 323 µs (+2%), CCL 413 µs (+31%) |
| The inbound side is a true zero-copy borrow (the Lisp vector is pinned; BOUNDARY §4). | capability | lisp/src/ffi.lisp:230-232 `#-ccl (cffi:with-pointer-to-vector-data (ptr octets) …)` lends the Lisp vector in place; BOUNDARY §4 ("on SBCL the Lisp vector is pinned and Rust borrows it in place"); tests v03.pinning-probe, v03.bulk-bytes-identity (tests/suite/v03.lisp:14,22) |
| The 0.3.0 CHANGELOG's 24×/307× compare rows 50 and 54 (0.32 ms, 0.19 ms, SBCL) with the element-wise marshaller they replaced (7.67 ms, 57.3 ms, same host, before the change — see the v0.3 groundwork commit). | performance | CHANGELOG.md:195-197; rows 50 (319.5 µs) and 54 (191.0 µs); commit 902bac7 body: "a 1 MiB transfer took 7.6 ms and a 65k-element i64 vector took 57 ms … 1 MiB bytes 24x faster, 65k i64 vec 307x" |
| Strings: ASCII takes a typed loop, the rest goes through babel. | capability | lisp/src/ffi.lisp:183-211 (%octets-to-ascii-string, babel:octets-to-string fallback at :210-211), :240-272 (%ascii-octets, babel:string-to-octets fallback at :271-272); test v05.utf8-fastpath-boundary (tests/suite/v05.lisp:185); CHANGELOG.md:11-22 |
| A `:string` crosses as UTF-8 with a copy in each direction (BOUNDARY §4); zero-copy `:string` is refused-with-cause in the v0.3 plan. | capability | BOUNDARY §4 (Lisp→Rust: encoded octets lent for the call; Rust→Lisp: "copies then releases exactly once"); docs/design/v03-async-plan.md §7 "Zero-copy `:string`" bullet; ROADMAP.md:72-73 ("Not in v0.5 … zero-copy `:string`"); tests m3.strings, m3.string-allocations |
| On SBCL a 64 KiB ASCII round trip costs ~0.32 ms. | performance | row 41 SBCL 317.6 µs; audit re-run 305 µs |
| The 2026-09-02 baseline (in git history) measured ~1 ms. | performance | `git show e91f54d:docs/benchmarks.md` → "string round trip (65536 B ASCII) 1015066.3 ns/call"; ROADMAP.md:58-59 "1015 → 318 µs" |
| The same bytes as `:bytes` cost 44 µs. | performance | row 49 SBCL 44.1 µs; audit re-run 43.1 µs |
| Octet payloads still belong in `:bytes`. | capability | rows 41 vs 49: 317.6 µs vs 44.1 µs (7.2×) on SBCL |
| Non-ASCII text costs what it did (the 1 KiB row). | performance | row 42 SBCL 38.8 µs vs e91f54d's 39452.6 ns; commit 43f3c9c body "1 KiB non-ASCII 39.5 -> 38.8 us" |
| The typed loop is faster than babel on every host (measured directly at 64 KiB: 1.4–2× on SBCL and CCL, ~14× on ECL). | performance | **resolved** — ratios removed in the audit; direction measured on all three hosts (scratch run, not a bench row) |
| ECL's babel path is the slow one — see its non-ASCII row. | performance | row 42 ECL 3.93 ms vs row 40 ECL 74.0 µs (53×) and vs SBCL 38.8 µs (100×); audit re-run 3.34 ms; direct measurement: ~27 ms per 64 KiB babel conversion on ECL |
| The rx row is a real `&str` consumer: 1 MiB into `regex-count` with the digits placed once at the end. | capability | tests/bench.lisp:65-77 (131071 × "abc def " + "abc 123 " = 1 MiB, pattern "[0-9]+"); examples/rx/src/lib.rs:29-31 `count(&self, text: &str)` → `self.inner.find_iter(text).count()` |
| The regex side is a memchr sweep, so the row is the boundary. | performance | **resolved** — memchr attribution removed in the audit |
| ~2.3 ms on SBCL, in line with 16× the 64 KiB row. | performance | **resolved** — corrected in the audit: 16× the inbound half of the 64 KiB round trip |
| A borrowed callback invocation is ~390 ns (100 per export call) on SBCL. | performance | row 55 SBCL 39.0 µs / 100; tests/bench.lisp:96-104 (100 words added, per-invocation line printed); audit re-run 415 ns |
| A stored callback from the same thread is ~170 ns. | performance | row 56 SBCL 170 ns; audit re-run 160 ns |
| On ECL a borrowed callback goes through a natively compiled trampoline (BOUNDARY §7). | capability | lisp/src/codegen.lisp:89-105 `%define-callback-trampoline` (#+ecl compile-file branch); BOUNDARY §7; BOUNDARY §12 row citing test v04.ecl-toolchain-failure-is-named (tests/suite/v04.lisp:156); CHANGELOG.md:253-256; docs/upstream/ecl-dynamic-callback-gc.md |
| ~4.8 µs per invocation on ECL. | performance | row 55 ECL 481.5 µs / 100; audit re-run 4.59 µs |
| The cross-thread stored path adds foreign-thread adoption, not measured here. | capability | tests/bench.lisp has only "stored callback (same thread)" (NOTIFY, not NOTIFY-FROM-THREAD); adoption: BOUNDARY §6 (:136) and §12 row citing v02.stored-callback-cross-thread (tests/suite/v02.lisp:118); lisp/src/stored-callback.lisp:22; examples/wordbag/src/lib.rs:149 `notify_from_thread` |
| To refresh: run the bench on each host on an idle machine, replace the table, update the date and commit, keep the previous baseline in git history rather than in this file. | capability | c260f03 did exactly this (rewrote the table; the 2026-09-02 baseline lives only at e91f54d) |

---

347 rows: 299 cited as they stand, 48 resolved by the audit, 0 unverified.
